<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.targets', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <VersionPrefix Condition="'$(PackageVersion)' == '0.1.0-preview.41'">0.1.0</VersionPrefix>
    <VersionSuffix Condition="'$(PackageVersion)' == '0.1.0-preview.41'">preview.41</VersionSuffix>
    <Version Condition="'$(Version)' != '$(PackageVersion)' And '$(PackageVersion)' != ''">$(PackageVersion)</Version>
  </PropertyGroup>
</Project>
