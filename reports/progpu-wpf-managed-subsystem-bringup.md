# ProGPU WPF Managed Subsystem Bring-up

## Porting Decision

For this port, "port" means reusing as much upstream managed WPF code as possible wherever it can compile and execute. ProGPU and Silk.NET are platform/rendering replacements at the boundary, not a reason to rewrite `System.Xaml`, `PresentationBuildTasks`, `PresentationCore`, `PresentationFramework`, controls, themes, resources, or the XAML compiler in the ProGPU bridge. New porting slices should first preserve the real managed subsystem path, then add the smallest native adapter or ProGPU primitive needed at the platform/render boundary.

The current architecture keeps this boundary:

- `System.Xaml` remains the real WPF XAML reader, schema, object writer, and markup-extension implementation.
- `PresentationBuildTasks` remains the real WPF XAML/BAML compiler path (`MarkupCompilePass1`, `MarkupCompilePass2`, `MarkupCompiler`, BAML writer/records).
- `PresentationFramework` remains the real WPF application, window, resource, style/template, control, document, and BAML/XAML loader implementation.
- `PresentationUI` remains the real WPF document/application support assembly. Windows builds keep the native `System.Printing` C++/CLI implementation, while non-Windows bring-up uses the existing `System.Printing-ref` project so the managed theme/XAML graph can compile without Visual C++ targets.
- Theme assemblies remain real WPF theme projects with `Page` XAML and internal markup compilation.
- `ProGPU.Wpf` supplies adapter services for rendering, retained scene ownership, portable windowing, input, dispatcher wakeups, and platform services without adding ProGPU references to the managed WPF subsystem projects.

## Basic App Smoke Ladder

1. **Managed core build gate**
   - Build `WindowsBase`, `System.Xaml`, `PresentationCore`, `PresentationFramework`, `PresentationUI`, `PresentationBuildTasks`, and at least one theme assembly.
   - Purpose: prove the upstream managed subsystem graph remains reusable on non-Windows.

2. **Code-only app gate**
   - Instantiate `Application`, `Window`, layout controls, text controls, resource dictionaries, and drawing visuals without XAML.
   - Route `Window.Show` through `PortableWindowActivationService` into the ProGPU/Silk.NET host.

3. **XAML compiler gate**
   - Build a tiny app with `ApplicationDefinition` and `Page` items using the real `PresentationBuildTasks`.
   - Verify generated BAML and code-behind connector paths are produced by WPF's compiler, not a ProGPU-local compiler.

4. **XAML runtime gate**
   - Load the generated app resources through real `PresentationFramework` BAML/XAML loaders.
   - Verify `ResourceDictionary`, `Style`, `ControlTemplate`, `VisualStateManager`, `StaticResource`, `x:Shared`, `DynamicResource`, and namescope behavior.

5. **Application run gate**
   - Call real `Application.Run()` on the compiled XAML app.
   - Verify WPF's own `StartupUri`, startup dispatcher work, root `Window.Show`, portable activation, run-loop handoff, shutdown, close, and dispose ordering without replacing managed app lifetime code.

6. **Theme gate**
   - Compile and load the Fluent theme project and representative style dictionaries (`Button`, `Window`, `TextBox`, `RichTextBox`).
   - Verify themed controls render through the ProGPU retained scene bridge.

7. **Rich framework gate**
   - Exercise controls, routed input/commands, input bindings, focus, selection, `TextBox`, `RichTextBox`, `FlowDocument`, bindings, resources, and templates.
   - Keep failures attached to the real WPF subsystem that produced them instead of replacing the subsystem.

8. **Rendering integration gate**
   - Continue lowering WPF drawing, effects, brushes, text, 3D, retained invalidation, and dirty-branch replay to native ProGPU primitives.
   - Managed WPF should own semantics; ProGPU should own GPU execution.

## Current Checked Gate

`WpfManagedProjectGraphTests.ManagedSubsystemBringupReusesRealWpfXamlFrameworkAndThemeProjects` now locks down the initial managed-subsystem graph:

- real `System.Xaml` source files for reader/object-writer/markup-extension support exist in the graph;
- real `PresentationBuildTasks` markup compiler and BAML writer sources remain in the graph;
- real `PresentationFramework` application/window/resource/style/template/BAML/rich-text sources remain in the graph;
- real Fluent theme XAML remains marked for internal markup compilation;
- the real `PresentationCore` compatibility harness references WPF as an external subsystem while `ProGPU.Wpf` stays outside the managed WPF projects.

`WpfManagedProjectGraphTests.RealPresentationFrameworkHarnessExercisesManagedFrameworkAndProGpuBridge` now locks down the first real `PresentationFramework` code-only smoke harness. `src/ProGPU.Wpf.RealPresentationFrameworkHarness` loads the built `PresentationFramework.dll` and `PresentationCore.dll` in an isolated assembly load context, constructs real `Application`, `Window`, `StackPanel`, `TextBox`, `RichTextBox`, `FlowDocument`, `ResourceDictionary`, `Style`, and `ControlTemplate` instances, registers portable window activation, and verifies real `DrawingVisual.RenderOpen()` rectangle, rounded-rectangle, line, ellipse, geometry, `GeometryDrawing` resource, direct `GlyphRun`, WPF `FormattedText.DrawText(...)`, managed `BitmapSource` image draws, `ImageBrush` rectangle fills, and `DrawingBrush` rectangle fills plus `PushOpacity`, `PushClip`, `PushTransform`, `PushOpacityMask`, `PushGuidelineSet`, and balanced `Pop` scopes route through `WpfRenderDataSinkProviderBridge` into retained ProGPU owner-branch `DrawRect`, `DrawRoundedRect`, `DrawLine`, `DrawEllipse`, `DrawPath`, `DrawGlyphRun`, `DrawTexture`, opacity, geometry-clip, transformed draw, opacity-mask commands with a native brush, guideline-snapped primitive coordinates, a native path emitted from WPF's managed `Drawing` hierarchy, native glyph indices/position/font-size emitted from WPF's managed text object, a WPF-generated formatted-text glyph run emitted by the real managed formatted-text pipeline, a native ProGPU texture uploaded from the real WPF `BitmapSource` shape through `WpfBitmapSourceImageAdapter`, and source-level `ImageBrush`/`DrawingBrush` replay through the same reflected tile-brush path used for drawing resources.

`WpfManagedProjectGraphTests.RealXamlCompilerHarnessUsesWpfApplicationDefinitionAndPagePipeline` now locks down the first app-style XAML compiler smoke. `src/ProGPU.Wpf.RealXamlCompilerHarness` uses `InternalMarkupCompilation=true`, an `ApplicationDefinition` (`App.xaml`), a compiled resource dictionary page (`SmokeResources.xaml`), a compiled nested `UserControl` page (`SmokeUserControl.xaml`), and a `Page` (`MainWindow.xaml`) with code-behind partial classes. Building this project invokes the real `PresentationBuildTasks` `MarkupCompilePass1`/resource pipeline and emits generated partial classes, BAML, and `ProGPU.Wpf.RealXamlCompilerHarness.g.resources` under `artifacts/obj/ProGPU.Wpf.RealXamlCompilerHarness/Debug/net11.0`.

`WpfManagedProjectGraphTests.RealXamlRuntimeHarnessLoadsCompiledBamlThroughRealPresentationFramework` now locks down the first runtime XAML/BAML smoke. `src/ProGPU.Wpf.RealXamlRuntimeHarness` builds the compiled XAML app as a build-only project reference, loads that app assembly with the real `PresentationFramework` and `PresentationCore` assemblies in an isolated load context, invokes the generated `App.InitializeComponent()` and `MainWindow.InitializeComponent()` paths, verifies real runtime resources, compiled merged resource dictionaries, static-resource resolution across dictionary BAML, compiled `x:Shared="False"` resource instance semantics, implicit `Style TargetType` lookup by control type key from a merged dictionary, `Style.BasedOn` inheritance, style `EventSetter` routed-event hookup, dynamic-resource invalidation, generated namescope connection, generated XAML event-handler hookup for `Click`, routed event dispatch, nested compiled `UserControl` resources/fields/element-name binding/event hookup, custom `MarkupExtension` `ProvideValue`, `TextBox` selection/replacement APIs, `RichTextBox`, richer `FlowDocument` content (`Paragraph`, `Bold`, `Italic`, `Underline`, `Span`, `LineBreak`, `Hyperlink`, `Figure`, `Floater`, `InlineUIContainer` with child `Button`, `BlockUIContainer` with child `Button`, `Section`, `Table`, `List`, `ListItem`, `TextSelection`, and `TextRange`), compiled `{Binding ...}` updates through `INotifyPropertyChanged`, `PriorityBinding` fallback through `PriorityBindingExpression`, `MultiBinding` with `StringFormat`, `IValueConverter` and `IMultiValueConverter` resources with converter parameters, `RelativeSource AncestorType` lookup after show, `IDataErrorInfo` and custom `ValidationRule` validation through `BindingExpression.UpdateSource()`, `ObjectDataProvider` construction/method invocation/binding, `XmlDataProvider` inline `x:XData` parsing and XPath binding, compiled `EventTrigger`/`BeginStoryboard`/`Storyboard`/`DoubleAnimation` object creation and post-`Loaded` zero-duration opacity application through WPF animation clocks, direct `ICommand` binding/execution, `RoutedUICommand` dispatch through a compiled `Window.CommandBindings` route, `ControlTemplate` application, `TemplateBinding`, compiled `ControlTemplate` property triggers, compiled `Style`/`DataTrigger`/`MultiDataTrigger` activation, `Style.Trigger`/`MultiTrigger` state, enter/exit trigger actions, and invalidation, `ListBox.ItemsSource`, explicit `ItemsPanelTemplate`, two-way `SelectedItem`, `DisplayMemberPath` item display metadata, `SelectedValuePath` item projection with two-way `SelectedValue` binding, `ItemContainerStyle` metadata and generated-container triggers, item-container alternation metadata and generated `AlternationIndex` values, `ItemStringFormat` metadata plus generated formatted item text, `DataTemplateSelector` metadata and generated-template selection, `ContentTemplateSelector` metadata and generated content selection, `ItemContainerStyleSelector` metadata and generated-container style selection, implicit `DataTemplate DataType` lookup for a `ContentControl` data object, `HierarchicalDataTemplate` metadata and generated `TreeViewItem` root/child content, `ObservableCollection` updates, `CollectionViewSource` resource creation with XAML sort/filter/group definitions, `CompositeCollection`/`CollectionContainer` flattening, sorted/filtered/grouped view identity/order/update, current-item synchronization, and item-property refresh behavior, `GroupStyle.HeaderTemplate` metadata and generated group-header content, and compiled `DataTemplate` binding-path plus generated-item `DataTemplate.Triggers` active/inactive selectivity. The harness validates WPF's own `TextRange` serialization for inline typography and anchored figure/floater text plus section/table text while keeping inline and block UI child content on the document object graph, calls real `Window.Show()` through `PortableWindowActivationService`, lets WPF apply the real window template/layout/visibility path, verifies the ProGPU host-backed activation, and exercises real `Keyboard.Focus(...)`/`Keyboard.ClearFocus()` against the XAML-created `TextBox`. This keeps the XAML compiler/runtime, markup extensions, data binding/converters, priority binding, validation rules, validation, data providers, animation/storyboard object model and first property-clock application, resource dictionaries, resource expressions, routed events, nested controls, rich document object model, style inheritance/triggers/trigger actions/event setters/multi-condition triggers, collection views/sorting/filtering/grouping/composite flattening/current-item sync/refresh/group headers, selector display/value projection, templating/template triggers/data-template triggers/template selectors/content-template selectors/container-style selectors/implicit template lookup/hierarchical templates, item-container style/alternation/string-format management, commanding, selection, and child keyboard-focus ownership in WPF while ProGPU owns only the native window/rendering boundary.

`WpfManagedProjectGraphTests.RealApplicationRunHarnessExercisesStartupUriThroughPortableActivation` now locks down the first real `Application.Run()` smoke. `src/ProGPU.Wpf.RealApplicationRunHarness` reuses the compiled XAML app as a build-only dependency, loads it with the real WPF assemblies in an isolated assembly load context, invokes generated `App.InitializeComponent()`, registers the real `PortableWindowActivationService` with recording delegates plus the same `PortableMediaContextRenderService` callback contract used by the ProGPU activation, and then calls real `Application.Run()`. The harness verifies that WPF's own startup dispatcher work loads `StartupUri`, creates and shows the compiled `MainWindow`, keeps `Application.MainWindow` populated, resolves the same compiled merged-dictionary, compiled `x:Shared="False"` resource instance semantics, implicit merged-dictionary style, `Style.BasedOn` inheritance, generated XAML event-handler, nested compiled `UserControl` resources/fields/element-name binding/event hookup, custom `MarkupExtension` `ProvideValue`, dynamic binding, `PriorityBinding` fallback, `MultiBinding`, value and multi-value converters with converter parameters, post-show `RelativeSource`, `IDataErrorInfo` and custom `ValidationRule` validation, `ObjectDataProvider` data-provider invocation, `XmlDataProvider` inline `x:XData` parsing/XPath binding, compiled `EventTrigger`/`BeginStoryboard`/`Storyboard`/`DoubleAnimation` metadata and post-`Loaded` opacity application, direct command, routed-command, control-template, dynamic-resource, style/data-trigger, collection binding, collection-view sorting/filtering/grouping, `GroupStyle.HeaderTemplate` generated header content, selection binding, `DisplayMemberPath` item display metadata, `SelectedValuePath` item projection with two-way `SelectedValue` binding, item-container style metadata/generated-container triggers, item-container alternation metadata/generated alternation indexes, item string-format metadata/generated formatted text, `DataTemplateSelector` metadata/generated-template selection, `ContentTemplateSelector` metadata/generated content selection, `ItemContainerStyleSelector` metadata/generated-container style selection, implicit `DataTemplate DataType` lookup, `HierarchicalDataTemplate`/`TreeView` root and child generation, `TextBox` selection/replacement APIs, rich `FlowDocument` object model including inline typography, figure/floater anchored blocks, inline UI, block UI, section, table, `TextSelection`, and `TextRange`, and item-template binding-path plus generated `DataTemplate.Triggers` active/inactive state, attaches the portable activation, calls the registered run delegate, and completes shutdown/close/dispose ordering without a Win32 message pump.

The real XAML runtime and `Application.Run()` harnesses now also cover the loose XAML loader path through `System.Windows.Markup.XamlReader.Parse(...)`. Both harnesses parse a runtime XAML `StackPanel` with a local `ResourceDictionary`, `SolidColorBrush`, `Style`, `StaticResource`, named `Button`, named `TextBox`, and `RelativeSource Self` binding, then validate namescope lookup, style application, resource identity, and binding path/value. This keeps loose XAML parsing, object writing, resource lookup, and binding markup-extension behavior in WPF's managed loader instead of adding a ProGPU-local parser.

The same real XAML runtime and `Application.Run()` harnesses now cover WPF's loose XAML serializer path through `System.Windows.Markup.XamlWriter.Save(...)`. Both harnesses parse a runtime `LinearGradientBrush` with `GradientStop` children, serialize it with the real WPF writer, parse the serialized XAML back through the real reader, and validate opacity, spread method, start/end points, stop count, stop colors, and stop offsets. They also serialize a real `ResourceDictionary` keyed by `MenuItem.SeparatorStyleKey`, parse it back, and validate keyed `Style` lookup/target-type identity. The live-style graph smoke parses a runtime `ResourceDictionary` with a base `Button` `Style`, a derived `BasedOn` style, and `Setter` values for `Tag`, `Content`, and `MinWidth`, serializes it with WPF's writer, parses it back, validates style keys, target types, `BasedOn`, setters, and then applies the round-tripped style to a real `Button`. The live-control-template graph smoke parses a runtime `ResourceDictionary` with a `Button` `ControlTemplate`, named `Border`/`ContentPresenter` parts, and a template trigger, serializes it with WPF's writer, parses it back, validates template target type and trigger/setter metadata, and applies the round-tripped template to a real `Button` to verify the named parts are instantiated through WPF's public template path. The live-data-template graph smoke parses a runtime `ResourceDictionary` with a `DataTemplate`, named generated `StackPanel`/`TextBlock` content, and a binding-driven `DataTrigger`; it validates original loose-reader binding paths before serialization, then validates the round-tripped template key, trigger metadata, and named generated content after the real writer/reader cycle. It records WPF's live writer behavior without pretending text-block `Binding` expressions survive this particular round trip. The live-items-panel-template graph smoke parses a runtime `ResourceDictionary` with an `ItemsPanelTemplate` containing a named `WrapPanel`, serializes it with WPF's writer, parses it back, and validates the generated panel name, tag, orientation, and item sizing metadata. The live-framework graph smoke now parses a runtime `StackPanel` with a named `Button`, named `TextBox`, default template/style state, local layout values, and brush/text properties, serializes it with WPF's writer, parses it back through WPF's reader, and validates the child graph and property values. The live rich-document graph smoke now parses a runtime `FlowDocument` with a named `Paragraph`, inline `Bold`/`Italic`/`Underline`, `Hyperlink`, `Section`, `Table`, `List`, and `ListItem` content, serializes it with WPF's writer, parses it back through WPF's reader, and validates names, tags, block structure, table/list metadata, and `TextRange` text extraction. The implementation fixes stay in WPF's serializer path: internal `SystemResourceKey` and `SystemThemeKey` values are explicitly routed through WPF's existing `SystemKeyConverter`, and empty runtime `Name` values on `FrameworkElement`/`FrameworkContentElement` are skipped so the real reader does not try to register invalid empty names. This keeps loose XAML save/load checkpoints on WPF's managed serializer, Freezable graph, resource-key model, style/setter manager, control-template manager, data-template trigger/content path, items-panel template path, framework-element graph, rich document object model, and default template/style objects rather than adding a ProGPU-local serializer. Remaining serializer follow-ups are deeper live control graphs with advanced expressions, events, custom controls, and theme-specific resources beyond the current explicit smoke.

The managed XAML scope path now also covers the real `System.Xaml.NameScopeDictionary` dictionary contract. The internal wrapper no longer leaves the generic `IDictionary<string, object>` and `ICollection<KeyValuePair<string, object>>` members as throwing stubs; both the standalone namescope and the wrapper over an external `INameScope` now support count, clear, copy, add, remove, contains, indexer, key/value enumeration, and try-get behavior while forwarding registration/unregistration to the underlying namescope where one exists. External names already present in a wrapped namescope stay visible through `FindName(...)`, but the generic dictionary view only counts, enumerates, and clears names registered through the wrapper. The XAML runtime and `Application.Run()` harnesses instantiate the built internal type by reflection and exercise both modes. This keeps name fixup, external namescope, loose XAML, and object-writer infrastructure in `System.Xaml` instead of papering over missing behavior in a ProGPU parser or loader shim.

`WpfManagedProjectGraphTests.RealThemeRuntimeHarnessLoadsFluentThemeBamlThroughRealPresentationFramework` now locks down the first real theme runtime smoke. `src/ProGPU.Wpf.RealThemeRuntimeHarness` builds `PresentationFramework.Fluent`, loads `/PresentationFramework.Fluent;component/Themes/Fluent.xaml` through real `ResourceDictionary.Source`, merges it into the compiled app resources, applies representative `Window`, `Button`, `ContextMenu`, `ToolTip`, `TextBox`, `ComboBox`, `PasswordBox`, `Slider`, `ProgressBar`, `TabControl`, `ListView`, `TreeView`, `Calendar`, `DatePicker`, `Menu`, `ToolBarTray`/`ToolBar`, `StatusBar`, `CheckBox`, and `RichTextBox` Fluent styles/templates, locates the compiled `DocumentBox` rich-text control through WPF namescope lookup, verifies those templates can apply without Windows native DLLs, measures/arranges the themed XAML content through WPF layout, replays that visual tree into a retained ProGPU scene through `ProGpuWpfCompositionTarget.ReplayVisualSubtreeRetained(...)`, and attaches the themed XAML-created `Window` to portable ProGPU activation. The measured replay now keeps the implicit-style `CheckBox` visible: the non-Windows managed classifier treats private-use icon-font characters as simple fast text, and `Typeface.CheckFastPathNominalGlyphs` lets missing private-use icon glyphs continue through WPF's null-glyph unshaped `GlyphRun` placeholder when Segoe Fluent/MDL2 is unavailable. That keeps Fluent check glyph layout out of native `TextFormatterContext.LoCreateContext` without editing the theme. `DefaultTextBoxStyle`/`DefaultTextBoxControlTemplate`, `DefaultContextMenuStyle`, `DefaultToolTipStyle`, `DefaultComboBoxStyle`, `DefaultPasswordBoxStyle`, `DefaultTabControlStyle`, `DefaultListViewStyle`, `DefaultTreeViewStyle`, `DefaultCalendarStyle`, `DefaultDatePickerStyle`, `DefaultMenuStyle`, `DefaultMenuItemStyle`, `ToolBar` static resource-key styles, implicit `StatusBar`, and the implicit Fluent `Slider` and `ProgressBar` styles now apply to real controls through the same theme BAML path. `FlowDocumentView` now skips native PTS formatter creation on non-Windows, so `RichTextBox.ApplyTemplate()` can run for basic app/theme bring-up. The real text stack also has non-Windows managed bring-up fallbacks for font-file opening, basic Unicode classification, LineServices escape-string bootstrap, and basic nominal-glyph `SimpleTextLine` layout; full `TextFormatterContext`/LineServices paragraph layout, complex shaping, exact icon-font fallback, bidi/caret services, and full `FlowDocument` layout/rendering still require a ProGPU-backed or cross-platform PTS/LineServices replacement.

The Fluent theme runtime smoke now also covers WPF's real `DefaultCheckBoxStyle`, `DefaultRadioButtonStyle`, `DefaultToggleButtonStyle`, `DefaultRepeatButtonStyle`, `DefaultExpanderStyle`, `DefaultGroupBoxStyle`, `DefaultScrollViewerStyle`, and `DefaultScrollBarStyle` resources. The harness creates real controls, validates keyed and implicit style lookup, applies templates, checks WPF-owned toggle/header/content/scroll state, and verifies retained ProGPU replay without adding a ProGPU-side control manager. `ScrollBar` intentionally follows the upstream Fluent pattern where `HorizontalScrollBarTemplate` and `VerticalScrollBarTemplate` are selected by `Style.Triggers` on `Orientation`, so the port guard validates trigger-based template selection instead of assuming a direct style setter.

The same theme gate now covers the broader basic-control style surface: `DefaultButtonStyle`, `DefaultItemsControlStyle`, `DefaultListBoxStyle`, `DefaultListBoxItemStyle`, `DefaultLabelStyle`, `DefaultSeparatorStyle`, `DefaultGridSplitterStyle`, `DefaultResizeGripStyle`, and `DefaultThumbStyle`. These assertions keep default button, items, list selection, label/separator metadata, splitter, resize grip, and thumb template behavior in real WPF theme/control code while ProGPU remains only the measured visual replay boundary. `Label` is tracked as a style/state assertion because the Fluent label style intentionally has no direct `Template` setter.

The same theme gate now covers WPF's real `DataGrid` Fluent style path. The harness resolves `DefaultDataGridStyle` plus row, cell, row-header, column-header, column-header presenter, cells presenter, checkbox element, floating-header, and drop-separator resources from real Fluent BAML, applies a real `DataGrid` with text and checkbox columns, validates the upstream style setter graph, checks binding paths and selected item state, and replays the measured themed visual tree through ProGPU. This keeps DataGrid column/style/selection semantics in reused WPF code while ProGPU remains the renderer boundary.

The theme gate now also checks representative upstream Fluent `Style.BasedOn` chains and dynamic style-resource values instead of treating theme resources as flat lookups. It verifies implicit type-key `Button`, `Calendar`, `DataGrid`, `DatePicker`, and `TextBox` styles derive from their default keyed styles, verifies `DatePickerCalendarStyle` derives from `DefaultCalendarStyle`, verifies `DataGridCheckBoxEditingElementDefaultStyle` derives from `DataGridCheckBoxElementDefaultStyle`, and verifies the real `DatePicker.CalendarStyle` dynamic resource resolves back to the Fluent `DatePickerCalendarStyle`. This keeps WPF theme inheritance and resource-expression behavior in the managed framework/theme stack.

The same theme gate now covers popup-owned theme resources without opening native popup windows. The harness attaches a real `ContextMenu` and `ToolTip` to the themed button, resolves `DefaultContextMenuStyle` and `DefaultToolTipStyle` plus implicit type-key styles from real Fluent BAML, applies both templates, verifies the context-menu item hierarchy and tooltip content, and then replays the owning window visual tree through ProGPU. This keeps popup/tooltip style, template, resource, and ownership state in WPF while leaving actual cross-platform popup top-level creation, placement, capture, transparency, and monitor constraints in the Silk.NET/ProGPU windowing boundary.

The companion `ManagedWpfSubsystemProjectsDoNotReferenceProGpuBridge` test guards the reuse boundary by keeping `ProGPU.Wpf`, `ProGPU.Scene`, and direct `external/ProGPU` references out of `System.Xaml`, `PresentationBuildTasks`, `PresentationFramework`, `PresentationUI`, and Fluent theme projects. `PresentationUiUsesManagedPrintingReferenceForNonWindowsBringup` locks down the current non-Windows build edge: native `System.Printing.vcxproj` is Windows-only, and non-Windows managed bring-up references `System.Printing-ref`.

The new real-framework harness intentionally references `ProGPU.Wpf`, `ProGPU.Backend`, `ProGPU.Scene`, and `ProGPU.Vector` only in the harness process, not in the managed WPF framework projects. It also references the real `PresentationCore` and `PresentationFramework` projects with `ReferenceOutputAssembly=false` and `PrivateAssets=all` so the harness builds the real assemblies but loads them explicitly. That keeps type identity honest while the bridge still adapts real WPF objects through reflection/object-sink registration.

## Portability Decisions Opened By The Harness

- Registry reads, ETW, Win32 system metrics/colors, keyboard-layout queries, cursors, font directories, DPI discovery, high-contrast/theme metadata, and hidden theme-notification HWNDs are now guarded with conservative non-Windows defaults only where the real framework smoke proved they were needed.
- UI Automation identifier initialization and composite-font parsing now have non-Windows guards for the shared OS-version helper and reserved-value probes. Basic selector/list controls and themed font metadata can initialize without loading `PresentationNative` or `UIAutomationCore`, while full provider/client integration remains a later platform-services item.
- Font-cache file mapping, Unicode classification table lookup, private-use icon-glyph classification/null-glyph placeholder layout, LineServices escape-string bootstrap, and the basic nominal-glyph `SimpleTextLine` quality gate now keep the Windows native paths on Windows and use managed non-Windows fallbacks for the real theme smoke. These are bring-up fallbacks for basic controls, layout, and visual replay; they are not a complete shaping, bidi, line-breaking, exact icon fallback, or paragraph-layout implementation.
- The non-Windows dispatcher path no longer creates the Win32 message-only HWND and now drains queued managed dispatcher work for `Application.Run()` startup/shutdown markers without calling Win32 `GetMessageW` or `MsgWaitForMultipleObjectsEx`. This is a bring-up gate, not the final loop. The final implementation should replace polling/drain behavior with the existing Silk.NET/ProGPU native-loop wake and dispatcher drain services.
- The real XAML runtime harness can now focus the shown XAML-created `TextBox` through WPF's own `Keyboard.Focus(...)` over `PortablePresentationSource`. The fix reuses WPF's window style/template, layout, inherited visibility, namescope, control, and focus logic; the portable branch supplies only the missing non-HWND root activation, root measure/arrange, and presentation-source boundary.
- The compiled storyboard slice verifies WPF's real BAML-created animation object model (`EventTrigger`, `BeginStoryboard`, `Storyboard`, and `DoubleAnimation`), real `Loaded` routed-event dispatch through WPF's own pending-loaded dispatcher path after portable show, the first zero-duration animated dependency-property value application through the disconnected non-Windows `MediaContext`/`TimeManager` path, a non-Loaded `Button.Click` `EventTrigger` that starts a storyboard clock after an explicit render-priority drain, a `Style.Trigger` enter/exit action pair that starts zero-duration storyboard clocks when `IsEnabled` toggles, a binding-driven `DataTrigger` enter/exit action pair that starts storyboard clocks after WPF data binding updates the trigger condition, and a binding-driven `MultiDataTrigger` enter/exit action pair that waits for both bound conditions before starting its storyboard clock. Broader animation timing, pause/seek/control actions, continuous clocks, and dirty-region policy remain open.
- The compiled template slice now verifies WPF's real `VisualStateManager` BAML path: `ControlTemplate` visual-state groups, `VisualState` storyboard metadata, and post-show `GoToElementState(...)` pressed-state application after a render-priority drain. Template-state semantics stay in WPF; ProGPU should only render the resulting visual state.
- The compiled input-binding slice verifies WPF's real `Window.InputBindings` BAML path: `KeyBinding`, `KeyGesture`, command parameter storage, and routed command execution through the existing `CommandBinding`. The latest extension raises `Ctrl+F6` through both the `ProGpuWpfWindowHost` platform-input callback and PresentationFramework's real `Window.HandlePortableInput(...)`, proving Silk.NET-normalized keyboard events can flow into WPF `InputManager`, `KeyBinding`, and `CommandManager` without a ProGPU command shortcut. IME/stylus and remaining key-routing edge cases stay in the same portable input boundary.
- Portable text input now reaches WPF's real `TextCompositionManager` and `TextBox` editor. The runtime and `Application.Run` harnesses focus the compiled `InputBox`, raise normalized text input through the ProGPU host and through PresentationFramework's portable input args, and verify WPF inserts the character, advances the caret, and clears selection. `SystemParameters.MouseVanish` now follows the same non-Windows fallback pattern as the earlier keyboard/caret metrics so `TextEditorTyping.HideCursor(...)` does not load `user32.dll`.
- Portable mouse input now reaches WPF's real `MouseDevice`, `InputManager`, capture manager, `ButtonBase` route, and `MouseWheel` routed event route. The runtime and `Application.Run` harnesses target the compiled `EventButton`, raise normalized move/down/up and wheel input through the ProGPU host and through PresentationFramework's portable input args, and verify the generated XAML `GotMouseCapture`, `LostMouseCapture`, `Click`, and `MouseWheel` handlers see WPF capture, `ButtonBase.Click`, and a one-line wheel delta of 120. The supporting source changes keep stylus/touch registry probing Windows-only, add a non-HWND global hit-test fallback for portable sources, and keep the new `RectangleGeometry` fill hit-test fallback narrow until ProGPU native vector hit testing replaces the remaining `MilCoreApi` helper dependency.
- The compiled section-control slice verifies WPF's real `TabControl`/`TabItem` BAML path: tab item headers/content, selected index/item/content state, generated selected content after portable show/run, and a selected-index change through WPF's selector/content-presenter machinery. The TabControl template also opened a line/polygon bounds dependency during hit-test precompute; non-Windows now uses a conservative managed `Geometry.GetBoundsHelper(...)` polygon-bounds fallback before `MilUtility_PolygonBounds`, with exact WPF-compatible vector bounds still assigned to ProGPU/native vector work.
- The section-control slice now also verifies WPF's real `Expander` and `GroupBox` `HeaderedContentControl` paths from BAML. The compiled app binds headers to the existing `SmokeDetail`, applies compiled `HeaderTemplate` resources, binds section content, and the runtime plus `Application.Run` harnesses validate generated header/content visuals and `Expander.IsExpanded` state changes without adding ProGPU-side control behavior.
- The compiled validation-manager slice now verifies WPF's real `BindingGroup` BAML path. The compiled app declares `StackPanel.BindingGroup` with a group-level custom `ValidationRule` and two explicit-update `TextBox` bindings; both real-WPF harnesses validate rule metadata, binding paths, rejected commits that preserve source values and set `Validation.HasError`, and accepted commits that update both source properties through WPF's binding engine. This keeps validation aggregation in WPF instead of adding a ProGPU-side validation shim.
- The compiled binding-transfer slice now verifies WPF's real binding `SourceUpdated` and `TargetUpdated` event paths. A two-way explicit `TextBox` binding with transfer notifications drives `BindingExpression.UpdateSource()` and source-property updates while generated handlers inspect WPF's `DataTransferEventArgs`, keeping data-transfer event construction in the binding engine.
- The compiled validation routed-event slice now verifies WPF's real `Validation.Error` path for both `IDataErrorInfo` and custom `ValidationRule` failures. Generated handlers observe `ValidationErrorEventArgs` added/removed actions, routed-event metadata, error content, and rule identity as bindings transition between invalid and valid values.
- The compiled namescope slice now verifies WPF's real runtime `NameScope` manager over the BAML-created window namescope. Both real-WPF harnesses drive `RegisterName`, `FindName`, duplicate registration failure, `UnregisterName`, and re-registration without introducing a ProGPU-side namescope table.
- The compiled live collection-view slice now verifies WPF's real `ICollectionViewLiveShaping` path. The compiled app declares live-sorting, live-filtering, and live-grouping `CollectionViewSource` resources with live property lists, and both real-WPF harnesses verify requested/capability/state metadata plus post-show property-change reorder, filter, and group-move updates after WPF `DataBind` dispatcher work runs. This keeps live item shaping, notification handling, and view refresh policy in WPF while ProGPU only renders the resulting item visuals.
- The compiled Freezable resource slice now verifies WPF's real `SolidColorBrush`/`Freezable` lifecycle from BAML resources: BAML-frozen resource load, idempotent freeze, clone, mutable clone property change, and clone-current-value semantics remain in WPF.
- The compiled gradient Freezable slice extends that resource coverage to WPF's real `LinearGradientBrush`, `GradientStopCollection`, and child `GradientStop` Freezables. Both harnesses verify BAML-frozen gradient resources, deep mutable clones, stop offset mutation, and clone-current-value preservation, keeping gradient resource semantics in WPF while ProGPU maps the resulting native gradient brush at render time.
- The real code-only `DrawingVisual.RenderOpen()` bridge smoke now verifies WPF-owned linear and radial gradient brushes cross the object-sink retained ProGPU path as native gradient brush commands. This ties WPF's managed gradient brush graph to ProGPU's render-boundary gradient mapping without introducing a managed replacement brush model.
- The ProGPU `PresentationCore` shim now exposes WPF-shaped gradient resource objects for code that is already decoupled from Windows WPF assemblies: `GradientStop`, `GradientStopCollection`, `GradientBrush`, `LinearGradientBrush`, and `RadialGradientBrush`. These objects lower to ProGPU native vector gradient brushes and raise `Changed`/`ChangeVersion` notifications so retained WPF invalidation can track the same resource graph shape as real WPF.
- The retained dependency tests now cover gradient brush subobjects explicitly. Tracker coverage validates stop graph enumeration, stop mutation, and collection refresh, and drawing-frame coverage verifies a dirty brush, stop collection, or individual stop maps back to the original owner branch for targeted replay.
- Copied retained-resource ownership now stays in ProGPU instead of becoming a WPF bridge workaround. ProGPU command-list snapshots and appends carry shared retained-resource leases for deferred native textures, while immediate offscreen appenders clear copied leases after render. WPF can continue to reuse its managed drawing/resource lifetime semantics and let ProGPU own GPU resource lifetime at the command boundary.
- The ProGPU `PresentationCore` path shim now follows the same reuse rule for path data: WPF-facing figures and segments expose `Point`, `Size`, and `double` where WPF does, while ProGPU vector conversion happens only when lowering into native vector path geometry. This keeps source-shared or ported managed WPF path code API-compatible without giving up ProGPU's native arc segment and shader-backed rendering path.
- The WPF bridge now treats path conversion as a native-boundary concern. Reflection-adapted WPF path geometries, transformed geometry groups, and command-sink replay all convert WPF-shaped path data to `ProGPU.Vector` data only at clone/transform/emit time, so managed framework code remains reusable and ProGPU continues to own arc normalization, transform fidelity, and backend rendering.
- The latest ProGPU PR #17 backend fixes keep the same boundary. HiDPI/offscreen mask sizing, WebGPU queued-resource cleanup, extension-frame lifetime, oversized pixel-buffer validation, and cached-layer physical/logical extent rounding were fixed in ProGPU itself, because those are native renderer/backend responsibilities and should not be patched around by managed WPF replay code.
- The compiled menu slice now verifies WPF's real `Menu`/`MenuItem` BAML path. The compiled app declares menu item hierarchy, separator, generated click handler, and command item metadata; both real-WPF harnesses validate the object graph, raise WPF's `MenuItem.ClickEvent`, and execute the compiled routed command from the compiled command target. The menu default template opened `SystemParameters.MenuPopupAnimation`; `SystemParameters.MenuAnimation` now follows the same non-Windows fallback pattern as the earlier text/input metrics before `SPI_GETMENUANIMATION`.
- The compiled popup-ownership slice now verifies WPF's real `ContextMenu` and `ToolTip` BAML path. The compiled app declares a button-owned context menu and tooltip; both real-WPF harnesses validate menu item hierarchy, separator, generated click handler, routed-command metadata/execution against the owner button, and tooltip placement/content. This deliberately reuses WPF's managed ownership, item, command, and tooltip object model; actual popup opening, native placement, monitor constraints, transparency, capture edge cases, and cross-platform top-level popup windows remain in the Silk.NET/ProGPU windowing boundary. `SystemParameters.DropShadow` now follows the non-Windows fallback pattern before `SPI_GETDROPSHADOW` so the default popup style can initialize without `user32.dll`.
- The compiled toolbar/status slice now verifies WPF's real `ToolBarTray`/`ToolBar` and `StatusBar` BAML paths. The compiled app declares toolbar command, separator, and toggle items plus status bar item/text content; both real-WPF harnesses validate the generated item collections, routed-command metadata/execution, toggle state, and status binding. These remain WPF-owned control managers; ProGPU should render the resulting visuals and provide native input/window services, not replace the toolbar/status control model. `SystemParameters.ComboBoxAnimation` now follows the non-Windows fallback pattern before `SPI_GETCOMBOBOXANIMATION` so the toolbar default template can initialize without `user32.dll`.
- The compiled ComboBox slice now verifies WPF's real `ComboBox` selector path over the existing item collection. Both real-WPF harnesses validate item generation, collection-change propagation, `DisplayMemberPath`, `SelectedValuePath`, selected item/index/value resolution, and two-way selected-value updates. This keeps selector/value projection in WPF while popup opening and native placement remain part of the broader portable popup/windowing boundary.
- The compiled GridView slice now verifies WPF's real `ListView`/`GridView` view manager path over the existing item collection. Both real-WPF harnesses validate item generation, collection-change propagation, `GridViewColumn` metadata, `DisplayMemberBinding` paths, and selector state changes. This keeps tabular item-view semantics in WPF while ProGPU remains responsible for rendering the resulting visuals. GridView column headers also opened the custom cursor stream path; non-Windows `Cursor` now uses a conservative managed arrow fallback for `.cur`/`.ani` streams/files until a Silk.NET/OS cursor service owns cursor decoding and application.
- The compiled range-control slice now verifies WPF's real `RangeBase` path through `Slider` and `ProgressBar`. Both real-WPF harnesses validate range metadata, tick/snap state, compiled value bindings, two-way source updates from `Slider.Value`, WPF-owned `Slider` routed command execution for small/large/end changes, post-show template `Track`/`Thumb` drag handling through `Thumb.DragDelta`, and propagated target updates into `ProgressBar.Value`. This keeps range coercion, command handling, template thumb-drag semantics, and value binding in WPF while ProGPU only renders the resulting visual state.
- The compiled password-input slice now verifies WPF's real `PasswordBox` path. Both real-WPF harnesses validate compiled `MaxLength`/`PasswordChar`, set and clear password content through WPF's API, inspect copied `SecurePassword` length, and verify generated `PasswordChanged` routed-event hookup. This keeps secure text-control semantics in WPF while native text input remains in the portable input boundary.
- The compiled layout-manager slice now verifies additional WPF-owned layout metadata beyond the existing `StackPanel`, `Grid`, and `Border` coverage. Both real-WPF harnesses validate nested `DockPanel`, `Canvas`, `WrapPanel`, `UniformGrid`, Grid shared-size scopes, and `GridSplitter` child collections, attached properties, post-show shared column sizing, splitter resize metadata, and panel settings from compiled BAML. The post-show GridSplitter smoke also raises real `Thumb.DragStarted`/`DragDelta`/`DragCompleted` event args against the compiled splitter and verifies WPF's own class handlers resize neighboring column definitions. This keeps core layout and splitter drag semantics in WPF while ProGPU focuses on rendering the arranged visual tree.
- The compiled scrolling slice now verifies WPF's real `ScrollViewer` and `ScrollBar` paths. Both real-WPF harnesses validate compiled scroll metadata, overflowing content, scrollbar range/change settings, direct scrollbar value updates, WPF-owned `ScrollBar` routed command execution for line/page/end scrolling plus `ScrollEvent` payloads, and post-show `ScrollViewer.ScrollToVerticalOffset(...)` after WPF layout. This keeps scroll state, range logic, and scrollbar command managers in WPF while native wheel/touchpad input and platform scroll gestures stay in the portable input boundary.
- The compiled date-selection slice now verifies WPF's real `Calendar` and `DatePicker` manager paths. Both real-WPF harnesses validate compiled display ranges, first-day-of-week, selected-date metadata, `Calendar.SelectedDates`, and `SelectedDate` mutation through WPF APIs. Popup opening and platform placement remain in the portable popup/windowing boundary.
- `DatePickerTextBox` measurement now remains on WPF's managed `SimpleTextLine` path on non-Windows by ignoring `TextBox` justification before it falls through to native LineServices. This keeps real WPF date-picker templates reusable in the portable harness while the full LineServices replacement remains a dedicated text subsystem item.
- The compiled navigation/content-hosting slice now verifies WPF's real `Frame` and `Page` source-navigation paths. Both real-WPF harnesses validate a hidden-navigation-UI `Frame` sourced from compiled `SmokePage.xaml`, page title metadata, page namescope lookup, and page child content without replacing WPF's managed navigation/control model.
- The navigated `SmokePage.xaml` is now a compiled `x:Class` page with code-behind. Both real-WPF harnesses validate the generated page type, page namescope fields, and generated `Button.Click` handler after `Frame` navigation, keeping page codegen and routed-event hookup in WPF.
- The compiled navigation slice now also verifies WPF's real `Frame` journal manager. Both real-WPF harnesses navigate to a second compiled page via `Frame.Source`, validate the second page object graph, then drive `CanGoBack`, `GoBack`, `CanGoForward`, and `GoForward` through the managed frame journal.
- The compiled decorator/adorner slice now verifies WPF's real `AdornerDecorator` and `AdornerLayer` manager. Both real-WPF harnesses validate an adorned compiled button, instantiate the compiled managed `SmokeAdorner`, and drive `AdornerLayer.Add(...)`, `GetAdorners(...)`, and `Remove(...)` through WPF.
- The compiled access-key/focus-scope slice now verifies WPF's real `Label`, `AccessText`, `FocusManager`, `AccessKeyManager`, and `Keyboard` focus path. Both real-WPF harnesses validate compiled focus-scope metadata, label target resolution, Label access-key registration, and mnemonic focus transfer to the target `TextBox` over the portable presentation source.
- The compiled toggle-choice slice now verifies WPF's real `ToggleButton`, `CheckBox`, and `RadioButton` manager paths. Both real-WPF harnesses drive `ButtonBase.OnClick()` against compiled controls, observe WPF `Checked`/`Unchecked` routed events, and validate same-group radio selection transfer without introducing ProGPU-side control state.
- The compiled thumb-drag slice now verifies WPF's real `Thumb` drag routed-event manager path. The compiled app declares a real `Thumb`, direct typed drag handlers, and a root-panel attached `Thumb.DragDelta` handler; both real-WPF harnesses raise WPF-owned `DragStartedEventArgs`, `DragDeltaEventArgs`, and `DragCompletedEventArgs` through `UIElement.RaiseEvent(...)` and validate direct plus bubbled event payloads. Silk.NET pointer capture still needs to feed the real WPF input path, but the drag event manager itself stays in WPF.
- The compiled explicit-tree slice now verifies WPF's real `TreeView`/`TreeViewItem` manager paths beyond generated hierarchical containers. Both real-WPF harnesses drive expansion/collapse and selection state, observe WPF routed events, and validate single-selection transfer between sibling items without adding ProGPU-side tree control logic.
- The compiled selector-event slice now verifies WPF's real `Selector.SelectionChanged` manager path for isolated `ListBox` and `ComboBox` controls. Both real-WPF harnesses drive `SelectedIndex`, observe generated XAML handlers, and validate sender/routed-event metadata plus WPF-owned added/removed item payloads.
- `DataStreams.SaveSubStreams(...)` now uses WPF's existing managed formatter path directly for journaled dependency-property values, allowing frame journal entry creation to save WPF state without referencing `System.Private.Windows.Core`.
- `SafeNativeMethods.GetTickCount()` now uses `Environment.TickCount` on non-Windows before the `kernel32.dll` path, so WPF journal timing can initialize without a Win32 timer call.
- `Application.PlaySound(...)` now no-ops on non-Windows before registry and native `PlaySound` access, allowing navigation state-change events to run without a Win32 sound service.
- `AccessKeyManager` now uses the current portable `PresentationSource` list instead of `GetActiveWindow()` on non-Windows, so text composition/access-key post-processing from navigated content stays in WPF without loading `user32.dll`.
- `CaretElement` now no-ops Win32 caret create/destroy/position sync outside Windows, keeping WPF's managed caret adorner render path active for focused portable `TextBox` controls without calling user32 caret APIs.
- The non-Windows theme-default path uses WPF's existing `Aero2` theme state for portable windows, while explicit theme harnesses can still load Fluent dictionaries through real `ResourceDictionary.Source`. `SystemResources` keeps the Windows `XamlAccessLevel.AssemblyAccessTo(...)` path on Windows and skips that unsupported platform call outside Windows.
- Scrollbar, caret, caret-blink, and primary-screen metrics, geometry bounds, bidi language checks, and TSF/text-services loading now have narrow non-Windows bring-up fallbacks opened by the real layout/focus/binding paths. Durable replacements should be Silk.NET/ProGPU platform metrics, ProGPU vector bounds, and cross-platform text services, not replacement XAML/framework code.
- `CompositionEngineLock` skips `wpfgfx_cor3.dll` on non-Windows so managed `MediaContext` startup can run disconnected while ProGPU owns render-data execution. Windows still uses the original MILCore lock and startup path.
- The default non-Windows DPI path is 96 DPI until a platform metrics service supplies real monitor/window DPI through the Silk.NET host.

## Current Verification

- Built `System.Xaml`, `PresentationBuildTasks` (`net11.0`), `PresentationCore`, and `PresentationFramework` directly.
- Built `PresentationFramework.Fluent` with internal markup compilation; this exercises the real theme XAML and `PresentationUI` graph.
- Built and ran `ProGPU.Wpf.RealPresentationCoreHarness`; the harness registered the real `PresentationCore` render-data provider successfully.
- Built and ran `ProGPU.Wpf.RealPresentationFrameworkHarness`; the harness constructed the real framework code-only app surface and verified retained ProGPU owner-branch `DrawingVisual.RenderOpen()` routing for rectangle, rounded rectangle, line, ellipse, geometry, drawing-resource, direct glyph-run, formatted-text-to-glyph-run, managed bitmap-source-to-texture, source-level image-brush texture fill, source-level drawing-brush nested drawing fill, opacity, clip, transform, opacity-mask, and guideline-set scopes.
- Built `ProGPU.Wpf.RealXamlCompilerHarness`; the real markup compiler emitted generated partial classes, BAML, and `.g.resources` for `ApplicationDefinition`, compiled `ResourceDictionary`, nested `UserControl`, and `Page` inputs.
- Built and ran `ProGPU.Wpf.RealXamlRuntimeHarness`; the harness loaded generated BAML through real `PresentationFramework`, verified runtime resources/namescopes/content plus compiled merged dictionaries, compiled `ComponentResourceKey` lookup/application, compiled `x:Shared="False"` resource instance semantics, implicit merged-dictionary styles, `Style.BasedOn` inheritance, generated XAML event hookup, nested compiled `UserControl` resources/fields/element-name binding/event hookup, custom `MarkupExtension`, dynamic binding, `PriorityBinding` fallback, `MultiBinding`, binding converter and multi-binding converter output/metadata, post-show `RelativeSource`, `IDataErrorInfo` and custom `ValidationRule` validation, `ObjectDataProvider` invocation/binding, `XmlDataProvider` inline `x:XData`/XPath binding, compiled storyboard/timeline metadata and post-`Loaded` opacity animation value, direct command, routed-command, control-template/template-scoped resources, dynamic-resource, style/data-trigger, style trigger actions, data-trigger actions, multi-data-trigger actions, collection binding, `CollectionViewSource` sorting/filtering/grouping/composite/current-item/property-refresh/live-shaping, generated `GroupStyle.HeaderTemplate` header content, selection binding, `DisplayMemberPath`/`SelectedValuePath` metadata and two-way selected-value projection, item-container style metadata/generated-container triggers, item-container alternation indexes, `DataTemplateSelector` metadata/generated-template selection, `ContentTemplateSelector` metadata/generated content selection, `ItemContainerStyleSelector` metadata/generated-container style selection, implicit `DataTemplate DataType` lookup, `HierarchicalDataTemplate`/`TreeView` root and child generation, `TextBox` selection/replacement, rich `FlowDocument` content (`Bold`, `Italic`, `Underline`, `Span`, `LineBreak`, `Hyperlink`, `Figure`, `Floater`, `InlineUIContainer`, `BlockUIContainer`, `Section`, `Table`, `List`, `ListItem`, `TextSelection`, `TextRange`), and item-template binding-path plus generated `DataTemplate.Triggers` active/inactive state, called real `Window.Show()` through portable ProGPU activation, and verified `Keyboard.Focus(...)`/`Keyboard.ClearFocus()` against the XAML-created `TextBox` over `PortablePresentationSource`.
- Built and ran `ProGPU.Wpf.RealApplicationRunHarness`; the harness called real `Application.Run()`, loaded the compiled `StartupUri` window through WPF startup, registered the portable MediaContext render callback, verified the same compiled merged-dictionary, compiled `ComponentResourceKey` lookup/application, compiled `x:Shared="False"` resource instance semantics, implicit merged-dictionary style, `Style.BasedOn` inheritance, generated XAML event hookup, nested compiled `UserControl` resources/fields/element-name binding/event hookup, custom `MarkupExtension`, dynamic binding, `PriorityBinding` fallback, `MultiBinding`, binding converter and multi-binding converter output/metadata, post-show `RelativeSource`, `IDataErrorInfo` and custom `ValidationRule` validation, `ObjectDataProvider` invocation/binding, `XmlDataProvider` inline `x:XData`/XPath binding, compiled storyboard/timeline metadata plus post-`Loaded` opacity animation value, direct command, routed-command, control-template/template-scoped resources, dynamic-resource, style/data-trigger, style trigger actions, data-trigger actions, multi-data-trigger actions, collection binding, `CollectionViewSource` sorting/filtering/grouping/composite/current-item/property-refresh/live-shaping, generated `GroupStyle.HeaderTemplate` header content, selection binding, `DisplayMemberPath`/`SelectedValuePath` metadata and two-way selected-value projection, item-container style metadata/generated-container triggers, item-container alternation indexes, `DataTemplateSelector` metadata/generated-template selection, `ContentTemplateSelector` metadata/generated content selection, `ItemContainerStyleSelector` metadata/generated-container style selection, implicit `DataTemplate DataType` lookup, `HierarchicalDataTemplate`/`TreeView` root and child generation, `TextBox` selection/replacement, rich `FlowDocument` content including inline typography, figure/floater anchored blocks, inline UI, block UI, section, table, selection, and text extraction, and item-template binding-path plus generated `DataTemplate.Triggers` active/inactive state, entered the portable run delegate, and completed WPF shutdown without a Win32 dispatcher loop.
- Latest focused verification also covers portable `Loaded` dispatch and first property-clock application: the runtime harness verifies the compiled storyboard target's `Loaded` handler, sender, routed-event name, loaded state, and animated opacity after `Window.Show()`, clicks a compiled storyboard trigger button and drains `Render` priority to verify the button-owned `Button.Click` `EventTrigger` applies its animated opacity, toggles a compiled style-trigger-action button to verify WPF applies `Trigger.EnterActions` and `Trigger.ExitActions` storyboard clocks, toggles a view-model property and drains `DataBind`/`Render` priorities to verify binding-driven `DataTrigger.EnterActions` and `DataTrigger.ExitActions` clocks, then toggles two view-model properties to verify `MultiDataTrigger.EnterActions` waits for both conditions and `ExitActions` restores when either condition breaks. The `Application.Run` harness attaches the recorded startup window to a real `PortablePresentationSource`, registers the portable MediaContext render service, verifies the same `Loaded` handler and opacity during the run delegate, and exercises the same click-trigger, style-trigger-action, data-trigger-action, and multi-data-trigger-action storyboard paths before unload/dispose.
- Latest focused input/command verification also covers compiled `Window.InputBindings`, portable text input, portable mouse capture, portable mouse `ButtonBase.Click`, and portable mouse wheel routing: the runtime and `Application.Run` harnesses validate the `Ctrl+F6` `KeyBinding`, generated `KeyGesture`, static routed command reference, command parameter, routed execution through the WPF `CommandBinding`, portable key-event activation through WPF `InputManager`, text insertion through WPF `TextCompositionManager`/`TextEditorTyping`, generated XAML `GotMouseCapture`/`LostMouseCapture` handling through WPF's capture manager, generated XAML `Click` handling through WPF's `MouseDevice`/`ButtonBase` route, and generated XAML `MouseWheel` handling with WPF's normalized one-line delta.
- Latest focused command-source verification also covers WPF's real `ICommand.CanExecuteChanged` reuse: both real-WPF harnesses validate a compiled `Button.Command` binding to `SmokeToggleCommand`, initial disabled state when `CanExecute` is false, event-driven enablement after `CanExecuteChanged`, command execution with the compiled `CommandParameter`, and event-driven disablement when `CanExecute` returns false again.
- Latest focused command-manager verification also covers WPF's real `CommandManager.RequerySuggested` reuse: the compiled smoke binds `RequeryCommandButton` to a command whose `CanExecuteChanged` delegates to `CommandManager.RequerySuggested`, then both real-WPF harnesses call `CommandManager.InvalidateRequerySuggested()`, drain WPF `DispatcherPriority.Background`, and verify `ButtonBase` enablement, command execution, and disablement without adding a ProGPU-side requery manager.
- Latest focused command-manager verification also covers WPF's real class command-binding table: the compiled smoke adds `SmokeClassCommandTextBox`, registers `SmokeClassRoutedCommand` through `CommandManager.RegisterClassCommandBinding`, and both real-WPF harnesses validate `CanExecute`/`Execute` routing from a compiled `Button.Command` target to the class handler without adding a ProGPU-side class-command table.
- Latest focused input/command verification also covers WPF's real class input-binding table over portable input: `SmokeClassCommandTextBox` registers `Ctrl+F7` through `CommandManager.RegisterClassInputBinding`, and both real-WPF harnesses focus the compiled target, send normalized portable `KeyDown`/`KeyUp` events, and verify WPF's `InputManager` and command manager execute the class binding with the registered command parameter.
- Latest focused section-control verification also covers real `TabControl`/`TabItem` manager reuse: both real-WPF harnesses verify selected tab object state before show, generated selected content after portable show/run, selected-index changes, and restored selected state. Source guards now keep the non-Windows `Geometry.GetBoundsHelper(...)` managed polygon-bounds fallback ahead of `MilUtility_PolygonBounds`.
- Latest focused section-control verification also covers real `Expander`/`GroupBox` manager reuse: both real-WPF harnesses verify compiled header-template resources, header/content bindings, generated header/content visuals, and live `Expander.IsExpanded` collapse/restore state through WPF's own templates.
- Latest focused validation-manager verification also covers real `BindingGroup` reuse: both real-WPF harnesses verify compiled group-level validation rules, explicit-update child bindings, rejected commit source preservation/error state, and accepted commit source updates without replacing WPF's binding validation manager.
- Latest focused binding-engine verification also covers real `SourceUpdated`/`TargetUpdated` reuse: both real-WPF harnesses verify transfer notification flags, explicit source update routing, target refresh routing, and `DataTransferEventArgs` sender/property/target metadata through WPF's binding manager.
- Latest focused validation-event verification also covers real `Validation.Error` reuse: both real-WPF harnesses verify added/removed validation routed events and `ValidationErrorEventArgs` payloads for data-error and custom-rule binding failures through WPF's validation manager.
- Latest focused XAML namescope verification also covers real runtime name registration over a compiled window namescope, including duplicate rejection and re-registration after unregistering the name.
- Latest focused resource verification also covers real `Freezable` brush reuse: both real-WPF harnesses verify a compiled `SolidColorBrush` resource loads frozen from BAML, clones back to mutable state, accepts clone property mutation, and preserves current values through `CloneCurrentValue()`.
- Latest focused resource verification also covers real gradient Freezable reuse: both real-WPF harnesses verify a compiled `LinearGradientBrush` and its gradient stop collection load frozen from BAML, clone deeply into mutable brush/stop objects, accept stop offset mutation, and preserve current values through `CloneCurrentValue()`.
- Latest focused rich-framework verification also covers real `RichTextBox` formatting command reuse: both real-WPF harnesses select compiled `FlowDocument` text through WPF `TextSelection`, execute `EditingCommands.ToggleBold`, `ToggleItalic`, `ToggleUnderline`, `ToggleSubscript`, `ToggleSuperscript`, `ApplyFontSize`, `IncreaseFontSize`, `DecreaseFontSize`, `ApplyFontFamily`, `ApplyForeground`, `ApplyBackground`, `ApplyInlineFlowDirectionRTL`, `ApplyInlineFlowDirectionLTR`, `AlignCenter`, `AlignRight`, `AlignJustify`, `AlignLeft`, `ApplyParagraphFlowDirectionRTL`, `ApplyParagraphFlowDirectionLTR`, `ToggleBullets`, `ToggleNumbering`, `IncreaseIndentation`, `DecreaseIndentation`, and `RemoveListMarkers`, and verify `TextElement.FontWeightProperty`, `TextElement.FontStyleProperty`, `TextElement.FontSizeProperty`, `TextElement.FontFamilyProperty`, `TextElement.ForegroundProperty`, `TextElement.BackgroundProperty`, `Inline.TextDecorationsProperty`, `Typography.VariantsProperty`, `Inline.FlowDirectionProperty`, `Paragraph.TextAlignment`, `Paragraph.FlowDirection`, `List.MarkerStyle`, nested/restored `ListItem` structure, and list-to-`Paragraph` replacement changes through WPF's `TextEditorCharacters`, `TextEditorParagraphs`, `TextEditorLists`, and `TextRangeEditLists` command handlers without adding a ProGPU-side rich-text command manager.
- Latest focused rich-framework verification also covers WPF-owned table editing reuse: both real-WPF harnesses enable the upstream `TextEditor.IsTableEditingEnabled` gate, register `TextEditorTables` command handlers for the real `RichTextBox`, execute `EditingCommands.InsertRows`, `InsertColumns`, `DeleteRows`, `DeleteColumns`, `MergeCells`, and `SplitCell` against the compiled `FlowDocument` table, and verify WPF's `TextSelection.InsertRows(...)`, `TextSelection.InsertColumns(...)`, `TextSelection.DeleteRows(...)`, `TextSelection.DeleteColumns(...)`, `TextSelection.MergeCells(...)`, `TextSelection.SplitCell(...)`, and `TextRangeEditTables` paths add copied table row/cell structure, delete the inserted row/column, merge/split column spans, and preserve the original compiled cell content until WPF's own destructive merge semantics apply.
- Latest focused rendering verification also covers WPF-owned gradient brush rendering: the real presentation harness now draws linear and radial gradients through `DrawingVisual.RenderOpen()` and verifies the retained ProGPU commands carry mapped native gradient brush payloads.
- Latest native-boundary verification also covers WPF-shaped path/arc reuse over ProGPU native vector arcs: ProGPU `PathArcShimTests` passed 8/8, `ProGPU.Wpf.Tests` builds cleanly, and focused WPF replay/reflection bridge tests passed 155/155 after the bridge conversion update.
- Latest native-boundary verification also covers the reviewed ProGPU backend fixes needed by WPF replay: `CompositorReviewRegressionTests` passed 10/10 for physical mask sizing and extension-frame cleanup, and `PixelDataConverterTests` passed 11/11 for non-throwing oversized source validation.
- Latest native-boundary verification also covers the reviewed ProGPU gradient and Skia texture fixes needed by WPF replay. WPF gradient semantics remain in the WPF-shaped managed API and ProGPU receives draw-bounds-mapped native vector gradient coordinates at the render boundary, while `SKCanvas.DrawImage(...)` now rejects cross-`WgpuContext` image textures before backend resource ownership can cross devices.
- Latest native-boundary verification also covers copied retained-resource ownership in ProGPU. `GpuPictureRecorder.EndRecording()` and `DrawingContext.Append(...)` now carry shared retained-resource leases for deferred image textures, temporary offscreen appenders release copied leases in `finally`, and compositor pooled render contexts clear leases before returning to the pool. ProGPU PR #17 review validation passed `SkCanvasStateTests` 35/35 and `CompositorReviewRegressionTests` 10/10 for commit `e55883f`; WPF-side bridge validation passed focused replay/reflection/graph coverage with 183 tests.
- Latest native-boundary verification also covers rectangle stroke hit testing: non-Windows `RectangleGeometry` now uses a managed conservative stroke-containment fallback before `MilUtility_PolygonHitTest`, while Windows keeps the MILCore path.
- Latest focused menu verification also covers real `Menu`/`MenuItem` reuse: both real-WPF harnesses verify compiled menu hierarchy, generated click routed-event hookup, command item metadata, routed command execution from the compiled target, and the non-Windows `SystemParameters.MenuAnimation` fallback needed by the default menu template.
- Latest focused popup ownership verification also covers real `ContextMenu`/`ToolTip` reuse: both real-WPF harnesses verify compiled context-menu hierarchy, generated click routed-event hookup, routed-command metadata/execution against the owner button, tooltip placement/content, and the non-Windows `SystemParameters.DropShadow` fallback needed by the default popup style.
- Latest focused toolbar/status verification also covers real `ToolBarTray`/`ToolBar` and `StatusBar` reuse: both real-WPF harnesses verify toolbar/status item collections, routed toolbar command execution, toggle state, status text binding, and the non-Windows `SystemParameters.ComboBoxAnimation` fallback needed by the default toolbar template without introducing ProGPU-side control-manager shims.
- Latest focused selector verification also covers real `ComboBox` reuse: both real-WPF harnesses verify ComboBox item generation, collection-change propagation, selected-value binding metadata, selected item/index/value resolution, and source updates through WPF's selector manager.
- Latest focused item-view verification also covers real `ListView`/`GridView` reuse: both real-WPF harnesses verify ListView item propagation, GridView column metadata, display-member binding paths, selected-index/selected-item state changes through WPF's item-view manager, and the non-Windows custom cursor fallback needed by GridView header grippers.
- Latest focused item-view verification also covers real compiled `DataGrid` reuse: the compiled XAML app now declares a `DataGrid` with explicit text and checkbox columns, collection binding, column binding metadata, selected-item binding, and selected-index mutation. Both real-WPF harnesses validate the generated BAML object graph, settable boolean source property, checkbox-column binding, two-way selected-item source update, post-show `ItemContainerGenerator` row realization/selected-state propagation, `DataGridCellsPresenter` discovery, and `DataGridColumn.GetCellContent(DataGridRow)` cell content through WPF's DataGrid/selector managers without adding a ProGPU-side grid manager.
- Latest focused range-control verification also covers real `Slider`/`ProgressBar` reuse: both real-WPF harnesses verify range metadata, compiled value bindings, two-way slider source updates, routed slider commands for small/large/min/max value changes, post-show template thumb drag through WPF's `Track.ValueFromDistance(...)` and `Thumb.DragDelta` class handler, and progress-bar target propagation through WPF's `RangeBase` and binding managers.
- Latest focused password-input verification also covers real `PasswordBox` reuse: both real-WPF harnesses verify password metadata, secure-password copy length, clear behavior, and generated `PasswordChanged` routed-event hookup through WPF's password control manager.
- Latest focused layout-manager verification also covers real `DockPanel`, `Canvas`, `WrapPanel`, `UniformGrid`, Grid shared-size scope, and `GridSplitter` reuse: both real-WPF harnesses verify compiled child collections, attached layout properties, panel sizing/orientation metadata, uniform grid row/column/first-column metadata, shared-size group metadata plus post-show equalized column widths, splitter resize/preview/increment metadata, and post-show splitter drag-resize column changes through WPF's layout managers.
- Latest focused scrolling verification also covers real `ScrollViewer`/`ScrollBar` reuse: both real-WPF harnesses verify scroll metadata, scrollbar range/value mutation, standalone `ScrollBar` routed commands with `ScrollEvent` payloads, and post-show vertical offset changes through WPF's scrolling controls.
- Latest focused date-selection verification also covers real `Calendar`/`DatePicker` reuse: both real-WPF harnesses verify date display ranges, selected-date collection state, date-picker format metadata, and selected-date updates through WPF's control managers.
- Latest text-boundary fix keeps `DatePickerTextBox` from crossing into native LineServices during portable measurement by forcing non-Windows `TextBoxLine` formatting through the managed simple-line path for justified/stretched text.
- Latest focused navigation/content-hosting verification also covers real `Frame`/`Page` reuse: both real-WPF harnesses verify frame source metadata, hidden navigation UI metadata, navigated page title, page content tree, and page namescope lookup through WPF's managed classes.
- Latest focused XAML compiler verification also covers real generated `Page` code-behind reuse: both real-WPF harnesses verify the navigated page is the generated `SmokePage` partial class, resolve named page content, invoke a compiled `Button.Click` handler, and assert page-owned handler state.
- Latest focused navigation-manager verification also covers real `Frame` journal reuse: both real-WPF harnesses verify a second compiled page navigation plus `CanGoBack`/`GoBack`/`CanGoForward`/`GoForward` state transitions through WPF's managed journal.
- Latest focused framework-manager verification also covers real adorner reuse: both real-WPF harnesses verify `AdornerDecorator`, `AdornerLayer`, and a compiled managed `SmokeAdorner` add/get/remove path over an adorned button.
- Latest focused input-manager verification also covers real access-key and focus-scope reuse: both real-WPF harnesses verify compiled `FocusManager` attached state, `Label.Target`, standalone `AccessText`, Label access-key registration, and mnemonic-driven `Keyboard.FocusedElement` transfer to the target `TextBox`.
- Latest focused control-manager verification also covers real toggle/radio reuse: both real-WPF harnesses verify `CheckBox` checked/unchecked routed events and same-group `RadioButton` checked/unchecked state transfer through WPF's own `ToggleButton`/`RadioButton` code.
- Latest focused item-manager verification also covers real explicit `TreeViewItem` reuse: both real-WPF harnesses verify expansion/collapse routed events, selection/unselection routed events, and sibling selection transfer through WPF's `TreeView` manager.
- Latest focused selector verification also covers `SelectionChanged` added/removed payloads for isolated `ListBox` and `ComboBox` selection changes, keeping routed selection-event construction in WPF instead of adding a ProGPU-side event shim.
- Latest native-boundary verification also covers WPF journal state streams: `DataStreams` no longer references the Windows-private binary writer, so the frame journal can create entries on non-Windows through the existing managed formatter path.
- Latest native-boundary verification also covers WPF journal timing: `SafeNativeMethods.GetTickCount()` now returns `Environment.TickCount` outside Windows before the `kernel32.dll` path.
- Latest native-boundary verification also covers WPF navigation sounds: `Application.PlaySound(...)` returns outside Windows before registry lookup and native `PlaySound`.
- Latest native-boundary verification also covers `AccessKeyManager` active-source resolution: non-Windows now resolves the active source from `PresentationSource.CriticalCurrentSources` before the Win32 `GetActiveWindow()` path, preserving managed WPF access-key/text-composition behavior over the portable presentation source.
- Latest native-boundary verification also covers WPF caret sync: `CaretElement` now guards Win32 `CreateCaret`, `DestroyCaret`, and `SetCaretPos` outside Windows while preserving the managed caret visual used by WPF text controls.
- Built and ran `ProGPU.Wpf.RealThemeRuntimeHarness`; the harness loaded real Fluent theme BAML through `ResourceDictionary.Source`, applied representative `Window`, `Button`, `TextBox`, `ComboBox`, `PasswordBox`, `Slider`, `ProgressBar`, `TabControl`, `ListView`, `TreeView`, `Calendar`, `DatePicker`, `Menu`, `ToolBarTray`/`ToolBar`, `StatusBar`, `CheckBox`, `RadioButton`, `ToggleButton`, `RepeatButton`, `Expander`, `GroupBox`, `ScrollViewer`, `ScrollBar`, `ItemsControl`, `ListBox`, `Label`, `Separator`, `GridSplitter`, `ResizeGrip`, `Thumb`, `DataGrid`, and `RichTextBox` templates/styles using namescope lookup for the compiled `DocumentBox`, measured/arranged the themed content with the implicit CheckBox visible on the portable private-use icon-glyph `SimpleTextLine`/null-glyph path, replayed the themed content visual tree into a retained ProGPU scene, and attached the themed XAML `Window` to portable ProGPU activation.
- Built `ProGPU.Wpf.Tests` and ran focused graph/bridge tests; `WpfManagedProjectGraphTests` plus `WpfRenderDataSinkProviderBridgeTests` cover the real `PresentationFramework` drawing-primitive/scoped-state/opacity-mask/rounded-rectangle/guideline-set/drawing-resource/direct-glyph-run/formatted-text bridge, the real XAML compiler/resource-dictionary/nested-control/event-hookup/markup-extension/rich-document/binding-validation/data-provider/collection-view/priority-binding/style-inheritance harness, the real XAML runtime merged-dictionary/nested-control/generated-event/markup-extension/dynamic-binding/priority-binding/multibinding/relative-source/validation/data-provider/direct-command/routed-command/control-template/dynamic-resource/style-trigger/style-inheritance/collection-view-filtering/display-member-path/selected-value-path/item-container-style/item-container-style-selector/data-template-selector/content-template-selector/item-template/selection/TextBox-focus/rich-document harness, the real `Application.Run` merged-dictionary/nested-control/generated-event/markup-extension/dynamic-binding/priority-binding/multibinding/relative-source/validation/data-provider/direct-command/routed-command/control-template/dynamic-resource/style-trigger/style-inheritance/collection-view-filtering/display-member-path/selected-value-path/item-container-style/item-container-style-selector/data-template-selector/content-template-selector/item-template/selection/rich-document harness, the real Fluent theme runtime harness, the retained ProGPU theme replay/layout check, the `FlowDocumentView` native PTS guard, managed font/classification/LineServices/typeface bootstrap guards, primary-screen metric fallback, and native-entrypoint guard coverage.
- Latest focused XAML/style verification also covers real compiled `Style.Trigger` property-trigger reuse: both real-WPF harnesses validate a BAML style keyed as `PropertyTriggeredButtonStyle`, its `IsEnabled=False` trigger metadata, active setter values, and restored values after reenabling the compiled button without adding a ProGPU-side style manager.
- Latest focused XAML/style verification also covers real compiled `MultiTrigger` reuse: both real-WPF harnesses validate a BAML style keyed as `MultiPropertyTriggeredButtonStyle`, its `IsEnabled`/`IsDefault` condition metadata, active setter values, deactivation when either property condition changes, and restored values when both conditions are true again.
- Latest focused XAML/style verification also covers real compiled `Style.Trigger` `EnterActions`/`ExitActions` reuse: `TriggerActionButtonStyle` carries BAML-created `BeginStoryboard`/`Storyboard`/`DoubleAnimation` action metadata, and both real-WPF harnesses flip `IsEnabled` after portable show/startup activation to verify WPF's style and animation managers apply and restore the animated `Opacity` value.
- Latest focused XAML/style verification also covers real compiled `DataTrigger` `EnterActions`/`ExitActions` reuse: `DataTriggerActionButtonStyle` binds to `SmokeViewModel.IsTriggerActionActive`, carries BAML-created action/storyboard metadata, and both real-WPF harnesses raise `INotifyPropertyChanged`, drain `DataBind`/`Render`, and verify WPF's binding, style, and animation managers apply and restore the animated `Opacity` value.
- Latest focused XAML/style verification also covers real compiled `MultiDataTrigger` `EnterActions`/`ExitActions` reuse: `MultiDataTriggerActionButtonStyle` binds to `SmokeViewModel.IsMultiTriggerActionReady` and `IsMultiTriggerActionArmed`, carries BAML-created condition/action/storyboard metadata, and both real-WPF harnesses raise both `INotifyPropertyChanged` notifications, verify the partial-condition inactive state, then verify WPF's binding, multi-condition style, and animation managers apply and restore the animated `Opacity` value.
- Latest focused XAML/style verification also covers real compiled `MultiTrigger` `EnterActions`/`ExitActions` reuse: `MultiTriggerActionButtonStyle` carries property-condition/action/storyboard metadata, and both real-WPF harnesses toggle `IsDefault` and `IsEnabled` after portable show/startup activation to verify WPF's property-condition style and animation managers apply, exit, and re-enter the animated `Opacity` value without a ProGPU-side trigger-action manager.
- Latest native-boundary verification also covers the refreshed ProGPU PR #17 backend review state at submodule commit `4ff9dc2`: malformed GDI-compatible bitmap construction now fails at the ProGPU shim boundary, format 12 cmap misses can fall back to format 4 glyph lookup, and all addressed PR review threads are resolved. These fixes support the managed-subsystem reuse rule by keeping bitmap and font compatibility in ProGPU while WPF continues to own XAML, framework, theme, text-object, control, command, and document managers.
- Latest native-boundary verification also covers ProGPU PR #17 GDI image interpolation at submodule commit `106930d`: `System.Drawing.Graphics.DrawImage(...)` now records `DrawTexture` with sampling derived from `Graphics.InterpolationMode`, and focused `CompositorReviewRegressionTests` passed 28/28. This keeps GDI-compatible image API behavior in ProGPU's shim/backend layer rather than adding managed WPF replay workarounds.
- Latest native-boundary verification also covers ProGPU PR #17 opacity-mask alpha output at submodule commit `6419567`: mask render passes use ProGPU-native `fs_mask` shader entries for `R8Unorm` vector/text/texture targets, so opaque black/blue masks and partial brush opacity follow WPF alpha semantics at full GPU speed instead of requiring managed bridge workarounds.
