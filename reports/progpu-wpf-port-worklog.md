# ProGPU WPF Port Worklog

## 2026-06-14

- Created WPF integration branch `progpu-rendering-port`.
- Added ProGPU as a submodule at `external/ProGPU`, tracking `fix/render-invalidation-and-leaks`.
- Added `external/Directory.Build.props` and `external/Directory.Build.targets` so submodule projects do not inherit the WPF Arcade SDK imports from the superproject.
- Added `src/ProGPU.Wpf`, a cross-platform port lane for ProGPU-backed WPF rendering.
- Added `ProGpuWpfCompositionTarget` and `ProGpuWpfWindowHost`.
- Added `IWpfCompositionCommandSink` and `ProGpuCompositionCommandSink` as the first DUCE/MIL replacement seam.
- Added `WpfMilRenderDataDecoder` for primitive generated WPF `RenderData` records:
  - line
  - rectangle
  - rounded rectangle
  - ellipse
  - geometry
  - image
  - glyph run
  - clip
  - opacity
  - opacity mask
  - transform
  - pop
- Added `WpfMilResourceRegistry` for WPF's one-based dependent resource token convention.
- Added `WpfReflectionResourceResolver` to adapt common real-WPF-shaped media resources to ProGPU shim types during `RenderData` replay.
- Added `IWpfImageSourceAdapter` and `WpfBitmapSourceImageAdapter` so WPF-shaped 32-bit `BitmapSource` pixels can be copied into ProGPU-backed `WriteableBitmap` instances when a replay caller opts into image upload.
- Added native ProGPU vector brush wrapping so WPF-shaped linear and radial gradient brushes can replay through the existing WPF `Brush` abstraction.
- Extended `WpfReflectionResourceResolver` geometry adaptation to cover WPF-shaped line geometry, ellipse geometry, and geometry groups in addition to rectangle/path geometry.
- Extended WPF-shaped `GeometryGroup` adaptation to preserve group fill rule and bake child geometry transforms into copied path figures.
- Extended WPF-shaped `PathGeometry` reflection adaptation to preserve figures, fill rule, line/polyline, quadratic/polyquadratic bezier, cubic/polycubic bezier, and arc segment metadata.
- Added WPF-shaped `CombinedGeometry` reflection adaptation, preserving union/intersect/xor/exclude boolean intent as ProGPU combined vector paths.
- Added `IWpfDrawingResourceResolver` and reflection replay for `MilDrawDrawing`, covering WPF-shaped `GeometryDrawing` and simple `DrawingGroup` resources.
- Added `WpfRenderDataReflectionBridge` to snapshot WPF `RenderData` private buffers and dependent resources, then replay them through the decoder.
- Added `WpfVisualContentReflectionBridge` to extract `DrawingVisual._content` and replay `RenderData` content through the same bridge.
- Added `WpfVisualTreeReflectionRenderer` for limited recursive visual subtree replay with transform, offset, clip, and opacity support.
- Added `WpfVisualInvalidationTracker` to observe WPF-shaped visual/resource `Changed`, `Invalidated`, `INotifyPropertyChanged`, and `INotifyCollectionChanged` signals and mark the ProGPU retained root dirty.
- Added retained `ProGPU.Scene.Visual.ChangeVersion` counters that advance on property invalidation, child invalidation propagation, and child collection changes, while render-time dirty clearing leaves the version stable.
- Fixed ProGPU `RenderOffscreen(...)` to compile visuals with a render-only offset override instead of mutating `Visual.Offset`, preserving `ChangeVersion` stability while native effect/layer rendering targets padded offscreen textures.
- Exposed `ProGpuWpfCompositionTarget.SceneChangeVersion`, `RetainedWpfChangeVersion`, and `FlatDrawingChangeVersion`, and changed WPF source invalidation to dirty the retained WPF root as well as the scene and flat drawing roots.
- Moved arc stroke shader parameter construction into public `ProGPU.Vector.ArcShaderParameters`/`ArcSegmentGeometry.TryCreateShaderParameters(...)`, so WPF-shaped arcs can use a stable ProGPU native shader ABI for transformed ellipse axes instead of compositor-private math.
- Added `IWpfRenderScheduler` and `CoalescingWpfRenderScheduler` as the first MediaContext-style render request boundary for the Silk.NET host.
- Routed visual-level transform and clip replay through `WpfReflectionResourceResolver` so WPF-shaped matrix transforms and rectangle/path geometry clips are adapted consistently with `RenderData` resources.
- Added `ProGpuWpfCompositionTarget.ReplayVisualSubtree` and `ProGpuWpfWindowHost.WpfRootVisual` to connect reflected WPF visual replay to the Silk.NET/WebGPU frame path.
- Threaded optional image-source upload through `WpfRenderDataReflectionBridge`, `WpfVisualContentReflectionBridge`, `WpfVisualTreeReflectionRenderer`, and `ProGpuWpfCompositionTarget`.
- Enabled `ProGpuWpfWindowHost` to use `WpfBitmapSourceImageAdapter` by default for transition visual replay, while still allowing callers to replace or disable the adapter.
- Wired `ProGpuWpfWindowHost` root changes, resize, load, and ProGPU target invalidations into the coalescing render scheduler.
- Added WPF-shaped `GlyphRun` adaptation for MIL `DrawGlyphRun` and reflected `GlyphRunDrawing`, preserving glyph indices, baseline origin, font size, advance widths, and glyph offsets through the ProGPU glyph-run command path.
- Added ProGPU sink push-stack tracking for native opacity-mask scopes so decoded WPF `PushOpacityMask` records emit matching `PushOpacityMask`/`PopOpacityMask` render commands.
- Stored the active WPF transform on native opacity-mask commands so transformed visual and drawing mask scopes replay correctly in ProGPU.
- Added reflected `DrawingGroup.OpacityMask` replay when the mask brush can be adapted and the drawing exposes finite bounds.
- Added bounded visual-level `OpacityMask` replay when a reflected visual exposes usable bounds, content bounds, render size, or actual size.
- Replaced the ProGPU sink's default rectangular clip fallback with native `PushGeometryClip`/`PopGeometryClip` command emission for path-convertible WPF clip geometries.
- Replaced the ProGPU sink's default shim geometry draw path with native `DrawPath` command emission for path-convertible WPF geometries, preserving untransformed arc metadata.
- Extended native path conversion so translated, rotated/uniformly scaled, and axis-aligned positively scaled WPF arc segments remain ProGPU arc segments instead of degrading to endpoint lines.
- Replaced the ProGPU sink's primitive line, rectangle, rounded-rectangle, and ellipse draw paths with direct native `RenderCommand` emission carrying the active WPF transform.
- Replaced shim-compatible bitmap image drawing with direct native `DrawTexture` command emission carrying the active WPF transform.
- Replaced shim-compatible glyph-run drawing with direct native `DrawGlyphRun` command emission carrying the glyph-run transform composed with the active WPF transform.
- Replaced shim-compatible formatted text drawing with direct native `DrawText` command emission carrying the active WPF transform.
- Replaced shim opacity-scope handling with direct native `PushOpacity`/`PopOpacity` command emission.
- Added initial balance-only guideline-scope replay for `MilPushGuidelineSet`, `MilPushGuidelineY1`, and `MilPushGuidelineY2` so WPF render-data push/pop balance is preserved.
- Added reflected `DrawingGroup.GuidelineSet` replay as a matching no-op guideline scope around children.
- Added reflected visual snapping guideline replay as a matching no-op guideline scope around visual content and children.
- Added reflected visual `ScrollableAreaClip` replay as a rectangular clip scope around visual content and children.
- Added explicit unsupported-state accounting for reflected visual effects, bitmap effects, cache mode, edge mode, then-unsupported bitmap scaling, ClearType, and text rendering or hinting modes so transition replay does not silently drop those WPF visual states.
- Hardened reflected `DrawingGroup` replay so non-adaptable transform/clip state and then-unsupported effect/cache state skip the drawing group instead of replaying children with incorrect visual state.
- Split reflected drawing replay results into applied, skipped, and unsupported states so decoded `MilDrawDrawing` records report unsupported drawing resources separately from unresolved resource tokens.
- Added `IWpfPlatformServices` and default unsupported service implementations as the initial Win32 abstraction boundary.
- Added `CrossPlatformWpfPlatformServices` and `ProcessWpfLauncher` so the Silk.NET host has a portable `ShellExecute` replacement for URI and file launch requests.
- Added `ProcessWpfClipboard` so the Silk.NET host has a cross-platform clipboard service behind `IWpfClipboard` using `pbcopy`/`pbpaste`, PowerShell `Set-Clipboard`/`Get-Clipboard`, or Linux `wl-copy`/`wl-paste` with `xclip`/`xsel` fallbacks.
- Added `ProcessWpfFileDialogService` so the Silk.NET host has a cross-platform file open, file save, and folder picker service behind `IWpfFileDialogService` using `osascript`, PowerShell Windows Forms dialogs, or `zenity`.
- Added `SilkNetWpfMonitorService` so the Silk.NET host can enumerate monitor bounds and primary monitor identity without Win32 monitor APIs.
- Added `IWpfInputService` and `SilkNetWpfInputService` so the Silk.NET host exposes normalized key, text, mouse, and wheel events without depending on Win32 messages.
- Added `IWpfWindowEventService` and `SilkNetWpfWindowEventService` so the Silk.NET host exposes activation/deactivation and external file-drop events without depending on Win32 window messages.
- Added `IWpfTimerService`/`IWpfTimer` and `ThreadPoolWpfTimerService` so the port lane has a cross-platform timer boundary for future MediaContext, animation, hover, and scheduling work without Win32 timers.
- Added `IWpfViewport3DCommandSink` and `WpfViewport3DReflectionBridge` so reflected WPF-shaped `Viewport3DVisual` content can compile into ProGPU's `Mesh3D` compositor extension payload.
- Added reflected Viewport3D support for projection cameras, `ModelVisual3D`, `Model3DGroup`, `GeometryModel3D`, `MeshGeometry3D`, diffuse/specular/material-group color extraction, model transforms, directional/ambient light hints, generated mesh normals, and front/back material entries.
- Added `WpfViewport3DTextureCache`, owned by `ProGpuWpfCompositionTarget`, to reuse and dispose Viewport3D offscreen color/MSAA/depth textures across replay frames.
- Routed `WpfVisualTreeReflectionRenderer` so `Viewport3DVisual.Children` stay inside the 3D bridge instead of being recursively treated as 2D visual children.
- Extended `ProGpuCompositionCommandSink` to emit `DrawExtension(CompositorBuiltInExtensions.Mesh3D)` followed by a transformed `DrawTexture` composite for WPF-shaped Viewport3D content when a context-backed texture cache is available.
- Added `WpfCompositionDrawingContext` as the first source-level command sink adapter for managed WPF draw calls that should bypass `RenderData.WriteDataRecord` and write directly to `IWpfCompositionCommandSink`.
- Added stack-depth tracking and close-time auto-pop behavior to the source-level adapter to match `RenderDataDrawingContext.EnsureCorrectNesting`.
- Added source-level drawing-context operation counters and explicit unsupported `DrawVideo`/`PushEffect` hooks so generator redirection can report unsupported operations without silently dropping them.
- Added `ProGpuWpfCompositionTarget.OpenCompositionDrawingContext` and `CreateCompositionDrawingContext` so source-level managed draw calls can write into a ProGPU frame through `WpfCompositionDrawingContext`.
- Added `ProGpuWpfWindowHost.WpfDraw` and `LastSourceDrawingResult` so the Silk.NET host can run source-level managed WPF drawing callbacks during each frame before lower-level ProGPU draw callbacks.
- Added `src/ProGPU.Wpf.Tests` with focused decoder tests.
- Added GPU-free integration coverage that decodes WPF-shaped records through `ProGpuCompositionCommandSink` into real `ProGPU.Scene.RenderCommand` output.
- Added GPU-free Viewport3D reflection coverage for WPF-shaped mesh payload compilation and replay routing.
- Added GPU-free source-level drawing-context adapter coverage for direct managed draw forwarding, push/pop balancing, unsupported video/effect accounting, and unsupported push-scope balance.
- Added GPU-free host callback coverage for source-level draw invocation, result capture, close-time auto-pop, and no-callback reset behavior.
- Documented the initial architecture and ADR in `reports/`.
- Verified with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj` from the project directory using the local net10 SDK boundary.
- Verified with `dotnet test src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --verbosity minimal`.

## 2026-06-15

- Added `IWpfCursorService` and `WpfCursor` as a local cursor abstraction for replacing Win32 cursor APIs in the ProGPU WPF lane.
- Added `SilkNetWpfCursorService` so WPF-shaped cursor requests map to Silk.NET `StandardCursor` values and apply through Silk.NET input cursor objects.
- Added `ProGpuWpfWindowHost.SetCursor` as the host-level cursor update path.
- Extended `CrossPlatformWpfPlatformServices` and `UnsupportedWpfPlatformServices` with cursor-service slots.
- Added GPU-free cursor coverage for cursor translation, unsupported input-source validation, and default host/service composition.
- Added `IWpfDispatcherService`, `IWpfDispatcherOperation`, and `WpfDispatcherPriority` as a local dispatcher-posting abstraction for replacing Windows message-loop posting and WPF dispatcher assumptions in the ProGPU lane.
- Added `QueuedWpfDispatcherService`, preserving owner-thread callback processing, WPF-shaped priority order, FIFO behavior within a priority, and pending-operation cancellation.
- Added `ProGpuWpfWindowHost.ProcessDispatcherQueue` and host pumping from `DoEvents` and render callbacks so Silk.NET can drain pending WPF-shaped dispatcher work.
- Added GPU-free dispatcher coverage for owner-thread access, invalid arguments, priority order, cancellation, cross-thread posting, host pumping, and default host/service composition.
- Extended `WpfCompositionDrawingContext` with generated animated overload shapes for line, rectangle, rounded rectangle, ellipse, image, video, and opacity so generator redirection can target the source-level sink without MIL animation records.
- The animated source-level overloads forward base values and count non-null animation clocks as unsupported animation state until ProGPU animation resource support is designed.
- Added GPU-free coverage for animated source-level draw and opacity overload degradation.
- Added source-level `WpfCompositionDrawingContext.DrawDrawing`, reusing the transition WPF-shaped drawing replay helper for `GeometryDrawing`, `DrawingGroup`, `ImageDrawing`, and `GlyphRunDrawing` instead of forcing `MilDrawDrawing` byte records.
- Added GPU-free coverage for source-level `DrawDrawing` applied, skipped, unsupported, and image-adapter replay status.
- Added `IWpfCompositionCommandSink.PushNoOpScope` and routed null source-level clip, transform, and opacity-mask pushes through no-op scopes so generated-resource nulls preserve matching pop behavior.
- Added source-level dynamic guideline-set handling for WPF's generated `PushGuidelineY1` and `PushGuidelineY2` fast paths, routing one- and two-Y-guideline cases into the ProGPU command sink.
- Added GPU-free coverage for null generated push resources, no-op close-time auto-pop, and dynamic guideline fast paths.
- Updated `WpfMilRenderDataDecoder` so token `0` clip, opacity-mask, and transform push records replay as applied no-op scopes while nonzero unresolved resource pushes remain skipped.
- Added GPU-free decoder and ProGPU sink integration coverage for null MIL resource push scopes.
- Updated `WpfMilRenderDataDecoder` so base-value replayed animated line, rectangle, rounded rectangle, ellipse, image, and opacity records count nonzero animation handles as unsupported state instead of silently dropping them.
- Added GPU-free decoder coverage for animated MIL base-value replay with nonzero and zero animation handles.
- Aligned `WpfCompositionDrawingContext` draw methods with generated `RenderDataDrawingContext` no-op guards so null pens, null image/glyph resources, null geometry, brush-plus-pen-null shape draws, and null video players do not emit commands or count unsupported animation state.
- Added GPU-free source-level coverage for generated no-op draw guards and animated no-op guard behavior.
- Added `IWpfGeneratedRenderDataDrawingContext` as the source-level generated-method contract that `WpfCompositionDrawingContext` implements for future render-data generator redirection.
- Added `WpfRenderDataInstructionRedirectionCatalog` to track WPF render-data instruction names, advanced overloads, generated no-op checks, generated internal methods, scope operations, unsupported operation categories, and ProGPU null-resource scope preservation decisions.
- Added XML-backed generator-redirection tests that parse WPF's `src/Microsoft.DotNet.Wpf/src/WpfGfx/codegen/mcg/xml/Resource.xml` and verify the ProGPU contract/catalog stay aligned with the source render-data instruction metadata.
- Added WPF in-tree `IRenderDataDrawingContextSink`, using real `PresentationCore` media types as the generated-method sink surface.
- Added a sink-aware internal `RenderDataDrawingContext` constructor, sink close on dispose, and close-time auto-pop behavior that no longer depends on `_renderData` being allocated.
- Updated WPF `renderdata.cs` codegen so generated constant and animated drawing methods branch to `_renderDataSink` before MIL serialization, while existing generated no-op checks and push/pop stack accounting remain in `RenderDataDrawingContext`.
- Updated checked-in WPF `System/Windows/Media/Generated/RenderDataDrawingContext.cs` so every generated draw/push/pop method redirects to `_renderDataSink` before `EnsureRenderData()` and MIL `WriteDataRecord(...)`.
- Added `RenderDataDrawingContextSinkProvider` as the WPF internal visual-owner-to-render-data-sink factory seam.
- Added scoped `RenderDataDrawingContextSinkProvider.PushSinkFactory(...)` registration so future ProGPU composition targets can install a sink while active and restore the previous MIL/default route on dispose.
- Hardened `RenderDataDrawingContextSinkProvider` registration as a nested scope stack, so out-of-order disposal marks stale scopes inactive and top-scope disposal restores the nearest still-active route.
- Added WPF in-tree `DrawingContextRenderDataSink`, forwarding every render-data sink instruction to a managed `DrawingContext`.
- Added scoped `RenderDataDrawingContextSinkProvider.PushDrawingContextFactory(...)` registration so a ProGPU-backed WPF `DrawingContext` can be installed without exposing the lower-level sink implementation to the eventual target.
- Added `ProGpuWpfDrawingFrame`, which clears/sizes the ProGPU root visual once per frame and exposes drawing-context factories that append multiple WPF-shaped drawing contexts into the same command buffer.
- Added `ProGpuWpfDrawingFrame` diagnostics for created drawing-context counts and last owner visual, and exposed the active frame through `ProGpuWpfFrameEventArgs.DrawingFrame`.
- Added `WpfRenderDataSinkProviderBridge`, a reflection bridge that installs an active frame's drawing-context factory into WPF's internal `RenderDataDrawingContextSinkProvider.PushDrawingContextFactory(...)` when the loaded `PresentationCore` identity exposes that provider.
- Updated `ProGpuWpfCompositionTarget` and `ProGpuWpfWindowHost` to use the frame object for per-frame drawing setup, keeping reflected visual replay, source-level WPF drawing, raw drawing, and future provider-created contexts on one active command buffer.
- Updated direct `ProGpuWpfCompositionTarget.ReplayVisualSubtree(..., pixelWidth, pixelHeight, ...)` to scope `ProGpuWpfDrawingFrame.TryRegisterRenderDataSinkProvider(...)` around composition-target-only replay.
- Updated `ProGpuWpfWindowHost` to scope `WpfRenderDataSinkProviderBridge` registration around each active render frame, so provider-backed `RenderOpen()` calls are frame-local and the previous WPF/MIL route is restored afterward.
- Added `VisualDrawingContext.Create`, including a sink-backed constructor path and default MIL-backed fallback.
- Routed `DrawingVisual.RenderOpen()` and `UIElement.RenderOpen()` through `VisualDrawingContext.Create(this)` so managed WPF visual rendering can opt into the ProGPU sink path.
- Included `IRenderDataDrawingContextSink.cs` in `PresentationCore.csproj`.
- Included `RenderDataDrawingContextSinkProvider.cs` in `PresentationCore.csproj`.
- Added static generator-redirection coverage for the WPF sink interface, project inclusion, `RenderDataDrawingContext` lifetime hook, scoped sink-provider registration, `DrawingContextRenderDataSink` forwarding, generator branch placement, checked-in generated-file branch placement, sink push/pop stack accounting, and visual render-open factory routing.
- Added static coverage for nested provider-scope restoration semantics in the WPF source.
- Added GPU-free `ProGpuWpfDrawingFrame` coverage for clear-once frame setup, clamped pixel dimensions, drawing-context factories, and composition-context factories appending to the same ProGPU command buffer.
- Extended frame/host coverage for owner tracking, context creation counters, and active-frame event args.
- Added GPU-free `WpfRenderDataSinkProviderBridge` coverage for typed provider-delegate adaptation, frame-factory registration, direct composition-target replay wiring, missing provider behavior, and incompatible delegate rejection.
- Added host coverage for default missing-provider registration and injected frame-registration scope disposal.
- Extended `WpfBitmapSourceImageAdapter` with managed Pbgra32 conversion for reflected `Pbgra32`, `Bgra32`, `Bgr32`, `Bgr101010`, `Bgr24`, `Rgb24`, `BlackWhite`, `Gray2`, `Gray4`, `Gray8`, `Gray16`, `Bgr555`, `Bgr565`, `Rgb48`, `Rgba64`, `Prgba64`, `Gray32Float`, `Rgb128Float`, `Rgba128Float`, `Prgba128Float`, `Cmyk32`, and indexed 1/2/4/8 bpp bitmap sources.
- Added GPU-free bitmap-adapter coverage for Pbgra32 copy, Bgra32 premultiplication, Bgr32 opaque alpha normalization, Bgr101010 expansion, 24-bit row conversion, packed grayscale expansion, Gray8/Gray16 expansion, Bgr555/Bgr565 expansion, Rgb48/Rgba64/Prgba64 down-conversion, non-profiled scRGB float down-conversion, device-CMYK conversion, indexed palette lookup with alpha premultiplication, unnamed 32-bit format fallback, and unsupported format rejection.
- Extended reflected `GeometryDrawing` replay with the first bounded `ImageBrush` transition path: simple non-tiled relative/absolute-viewport image brushes are replayed as `PushClip` plus `DrawImage` over the adapted geometry bounds or absolute viewport, with brush opacity preserved through a ProGPU opacity scope.
- Added GPU-free coverage for reflected `ImageBrush` geometry fills and for unsupported image-brush tile cases that still replay a supported geometry stroke while reporting partial unsupported content.
- Extended reflected `GeometryDrawing` replay with a bounded `DrawingBrush` transition path: simple non-tiled relative/absolute-viewport drawing brushes clip to the filled geometry, map inferred drawing content bounds into the brush viewport, and recursively replay the nested drawing through the same ProGPU command sink.
- Added GPU-free coverage for reflected `DrawingBrush` geometry fills and for partially replayed drawing-brush content being surfaced as applied-plus-unsupported decoder output.
- Extended reflected `GeometryDrawing` replay with a bounded `VisualBrush` transition path: simple non-tiled relative/absolute-viewport visual brushes clip to the filled geometry, map reflected visual bounds into the brush viewport, and replay the visual subtree through `WpfVisualTreeReflectionRenderer`.
- Added GPU-free coverage for reflected `VisualBrush` geometry fills and for partially replayed visual-brush content being surfaced as applied-plus-unsupported decoder output.
- Added GPU-free coverage for absolute `ViewportUnits` on reflected non-tiled `ImageBrush`, `DrawingBrush`, and `VisualBrush` geometry fills.
- Added simple absolute `Transform` support for reflected non-tiled `ImageBrush`, `DrawingBrush`, and `VisualBrush` geometry fills, with bounded `RelativeTransform` support deferred to a later slice.
- Added a source-rectangle image draw path to `IWpfCompositionCommandSink` and `ProGpuCompositionCommandSink`, mapping cropped bitmap draws to ProGPU `DrawTexture.SrcRect`.
- Extended reflected non-tiled `ImageBrush` geometry fills with relative and absolute `Viewbox` cropping when the adapted image exposes pixel dimensions.
- Extended reflected non-tiled `DrawingBrush` and `VisualBrush` geometry fills with relative and absolute `Viewbox` cropping by mapping the selected source bounds into the brush viewport and pushing a transformed source-space clip before replaying nested content.
- Extended reflected `ImageBrush`, `DrawingBrush`, and `VisualBrush` geometry fills with bounded `TileMode=Tile`, `FlipX`, `FlipY`, and `FlipXY` replay for finite fill geometry, including simple absolute brush `Transform` scopes.
- Added bounded `RelativeTransform` support for reflected `ImageBrush`, `DrawingBrush`, and `VisualBrush` geometry fills by converting adaptable affine relative transforms through the filled geometry bounds before replay.
- Added bounded `Stretch=None`, `Stretch=Uniform`, and `Stretch=UniformToFill` support for reflected `ImageBrush`, `DrawingBrush`, and `VisualBrush` geometry fills with `AlignmentX`/`AlignmentY` placement; stretched content that overflows a tile is clipped to that tile before replay.
- Extended `SilkNetWpfMonitorService` so `WpfMonitorInfo.DpiScale` is derived from reflected backend scale/DPI properties when available, or from Silk.NET video-mode resolution versus monitor bounds as a portable fallback.
- Added GPU-free monitor coverage for video-mode/bounds DPI inference, reflected scalar DPI scale, reflected DpiX/DpiY, and invalid reflected scale fallback.
- Added `WpfDrawingReplayStatus.PartiallyApplied` so reflected `DrawingGroup` replay can count supported children as applied while still reporting unsupported sibling content.
- Added GPU-free source-level and MIL-decoder coverage for partially replayed drawing groups.
- Replaced the sink-level endpoint-line fallback for complex transformed WPF arcs with SVG-style cubic Bezier approximation, keeping exact ProGPU arc segments for transforms ProGPU can represent natively.
- Added GPU-free coverage for complex transformed arc geometry and clip conversion through the native ProGPU command sink.
- Factored transformed arc cubic decomposition into a shared helper and routed geometry-group child-transform flattening through it, so transformed child arcs are copied as Bezier segments instead of endpoint lines.
- Added GPU-free coverage for transformed arcs inside reflected WPF-shaped geometry groups.
- Added `IWpfDragDropService`, `WpfDragDropData`, `WpfDragDropEventArgs`, and `SilkNetWpfDragDropService` as the first portable drag/drop payload boundary, mapping Silk.NET file drops to WPF-shaped drop events.
- Wired `CrossPlatformWpfPlatformServices`, `UnsupportedWpfPlatformServices`, and `ProGpuWpfWindowHost.DragDropReceived` through the new drag/drop service.
- Added GPU-free drag/drop coverage for file-drop payload mapping, null file lists, invalid attach arguments, and default host/service composition.
- Extended reflected drawing-resource opacity-mask replay so `DrawingGroup` masks can infer finite bounds from child geometry, image, glyph-run, and nested drawing-group content when `DrawingGroup.Bounds` is unavailable.
- Intersected inferred drawing-resource opacity-mask bounds with adaptable `DrawingGroup.ClipGeometry` bounds before pushing the native ProGPU opacity-mask scope.
- Added GPU-free coverage for inferred drawing-group opacity-mask bounds and clipped inferred opacity-mask bounds.
- Extended reflected visual-level opacity-mask replay so visuals without exposed bounds can infer finite mask bounds from replayable self or child `RenderData` content, including transformed content records.
- Added GPU-free visual-tree coverage for self, transformed, and child render-data-inferred visual opacity-mask bounds.
- Extended `WpfVisualInvalidationTracker` graph traversal to follow additional WPF media/resource references including drawing collections, foreground brushes, combined-geometry operands, path figures/segments/points, gradient stops, clips, guideline sets, and Viewport3D camera/model/material/mesh collections.
- Added GPU-free invalidation coverage for nested drawing foreground-brush changes and path-geometry figure/segment collection refreshes.
- Extended reflected `GeometryGroup` adaptation so mixed child sets containing `CombinedGeometry` preserve every adaptable child by folding them into ProGPU union combined-path trees instead of dropping non-path children.
- Added GPU-free decoder coverage for mixed `GeometryGroup`/`CombinedGeometry` replay, preserving the nested boolean operation inside the group union.
- Extended `WpfReflectionResourceResolver` transform adaptation beyond precomputed `Value` matrices so WPF-shaped `TranslateTransform`, `ScaleTransform`, `RotateTransform`, `SkewTransform`, and `TransformGroup` resources can replay even when the reflected object does not expose a readable `Value` property.
- Added GPU-free decoder coverage for WPF-shaped translate/scale transform groups, centered rotate transforms, and centered skew transforms without `Value` matrices.
- Extended the ProGPU vector radial-gradient brush, GPU brush packing, and WGSL gradient evaluation to carry separate X/Y radii plus gradient-origin/focal-point coordinates, then changed the WPF-shaped radial-gradient adapter to preserve `RadiusX`, `RadiusY`, and `GradientOrigin` instead of collapsing them to a larger circular radius centered at `Center`.
- Added ProGPU gradient spread-method support for `Pad`, `Reflect`, and `Repeat`, packed it into the GPU brush layout, applied it in the vector and hatch WGSL gradient paths, and mapped WPF-shaped gradient `SpreadMethod` values in the reflection resolver.
- Added WPF gradient `MappingMode` preservation for reflected gradient brushes and command-sink materialization of `RelativeToBoundingBox` linear/radial gradients against finite rectangle, rounded-rectangle, ellipse, geometry, line, pen, and opacity-mask bounds before recording ProGPU commands. Explicit `MappingMode=Absolute` gradients keep their source coordinates.
- Added WPF gradient `Transform` and `RelativeTransform` preservation for reflected gradient brushes. Linear and radial gradients now map finite-bounds coordinates first, then carry adaptable invertible 2D affine brush transforms as inverse ProGPU brush-coordinate transforms so the shader samples transformed brush space, including non-axis-aligned radial-gradient transforms.
- Added optional command-sink diagnostics sampled by `WpfMilRenderDataDecoder`, and wired `ProGpuCompositionCommandSink` to count non-invertible reflected gradient transforms, unrecognized reflected gradient `ColorInterpolationMode` values, and reflected gradient stop-table overflow above 65,536 stops as unsupported state when replay must fall back.
- Changed WPF-shaped gradient adaptation to preserve reflected valid gradient stops in the ProGPU vector brush up to the current 65,536-stop table capacity instead of trimming at the old fixed brush ABI boundary.
- Added ProGPU gradient color-interpolation support for WPF `SRgbLinearInterpolation` and `ScRgbLinearInterpolation`, carrying the mode through vector brushes, `GpuBrush`, vector WGSL, and hatch WGSL. The scRGB path converts sRGB stop colors to linear light for interpolation and converts the interpolated RGB result back to sRGB for output.
- Expanded ProGPU's `GpuBrush` ABI, vector brushes, vector WGSL, and hatch WGSL with a two-row affine brush-coordinate transform so transformed WPF linear and radial gradients are evaluated in shader space instead of CPU-flattening endpoints or radii.
- Moved ProGPU gradient stop sampling out of the fixed brush color slots and into a `GpuGradientStop` storage table shared by dynamic vector rendering, static DXF buffers, and hatch rendering. `GpuBrush` now stores a stop-table offset/count, vector group 0 binds the stop table, and the hatch extension binds the same table at its group 0 binding 4.
- Added basic ProGPU sink Y-guideline snapping for generated `PushGuidelineY1` and `PushGuidelineY2` scopes on axis-aligned primitive line, rectangle, rounded-rectangle, and ellipse coordinates; the `PushGuidelineY2` path snaps the leading coordinate and preserves the driven-edge offset.
- Added GPU-free coverage for Y-guideline snapping through decoded MIL records and for preserving unsnapped coordinates under rotated transforms.
- Added `IWpfCompositionCommandSink.PushGuidelineSet(object?)`, shared dynamic guideline-set reflection, and optional `IWpfGuidelineSetResourceResolver` support so dynamic frozen `GuidelineSet` X/Y arrays can reach the ProGPU sink from source-level drawing and MIL dependent-resource replay.
- Extended ProGPU primitive guideline snapping to both X and Y axes for dynamic `GuidelineSet` scopes on axis-aligned line, rectangle, rounded-rectangle, and ellipse commands.
- Added GPU-free coverage for dynamic `GuidelineSet` pass-through and decoded MIL `PushGuidelineSet` X/Y primitive snapping.
- Routed reflected `DrawingGroup.GuidelineSet` objects and readable reflected visual X/Y snapping guideline collections through `PushGuidelineSet(object?)`, preserving old no-op fallback behavior when visual collections cannot be read.
- Updated GPU-free drawing-resource and visual replay coverage to assert guideline object propagation through the sink overload.
- Added `ProGpuWpfPen` as a transition pen wrapper for reflected nonnegative WPF `DashStyle` arrays, offsets, line-cap metadata, `LineJoin`, and miter limits.
- Added `PenLineJoin`, `MiterLimit`, and `PenLineCap` metadata to the ProGPU vector `Pen` and WPF shim `Pen`, then copied reflected WPF join/miter/cap intent onto native ProGPU command pens.
- Added `PathSegment.IsSmoothJoin` metadata to the ProGPU vector path model and WPF shim path model, preserving it through reflected WPF-shaped path geometry, shim `StreamGeometryContext`, transformed path copies, and native `DrawPath` command emission.
- Added `StrokeJoinGeometry` to ProGPU vector code and wired `ProGPU.Scene.Compositor` path stroking to emit filled native triangle geometry for straight line-line `Miter`, `Bevel`, and `Round` joins, with miter-limit fallback and `IsSmoothJoin` suppression.
- Added `StrokeCapGeometry` to ProGPU vector code and wired `ProGPU.Scene.Compositor` path stroking to emit filled native triangle geometry for open line, quadratic Bezier, and cubic Bezier path start/end `Square`, `Round`, and `Triangle` caps using segment endpoint tangents.
- Added `ArcSegmentGeometry` to ProGPU vector code, moved native arc center/point math onto that helper, and initially wired `ProGPU.Scene.Compositor` path stroking to emit native arc strokes as bounded line-strip approximations with arc endpoint directions for open-path caps. This intermediate stroke path is superseded later in this worklog by shader-native arc stroke strips.
- Extended `StrokeJoinGeometry` and `ProGPU.Scene.Compositor` path stroking so adjacent line, quadratic Bezier, cubic Bezier, and arc segments emit native miter/bevel/round join fillers from endpoint tangents instead of only joining straight line-line corners.
- Added straight-line dashed pen replay in `ProGpuCompositionCommandSink` by splitting supported dashed `DrawLine` calls into native ProGPU line segment commands while preserving the active transform and original brush bounds.
- Added dashed rectangle-outline replay in `ProGpuCompositionCommandSink`, emitting any fill before segmented outline commands and carrying dash phase continuously across rectangle corners.
- Added dashed rounded-rectangle and ellipse outline replay by approximating supported primitive curves as local line segments before dash emission.
- Added dashed path-compatible geometry outline replay by flattening line, quadratic, cubic, and arc path figures to local polylines before dash emission. Filled dashed geometries emit the native fill path before the segmented outline.
- Added dashed combined-geometry transition replay: native combined fill remains a `DrawPath`, while dashed outlines are emitted from each combined operand's flattened boundary and counted as unsupported fidelity until exact boolean-result dash boundaries are implemented.
- Added transition dot-style dash replay for WPF zero-length drawn dash entries by emitting one-thickness local dot segments while leaving fuller vector-stroking fidelity as later work.
- Added transition `Square`, `Round`, and `Triangle` cap replay for emitted line segments: square caps extend segment endpoints by half the pen thickness, round caps emit filled circular cap geometry, and triangle caps emit filled triangle geometry with the apex one pen radius outward.
- Extended WPF-shaped glyph-run font adaptation to prefer reflected local `GlyphTypeface.FontUri` or font file paths before falling back to family-name lookup or platform fallback, with loaded ProGPU `TtfFont` instances cached by path.
- Preserved reflected `GlyphTypeface.StyleSimulations` by carrying bold/italic transition flags through the ProGPU shim `GlyphRun`, `DrawingContext.DrawGlyphRun(...)`, `ProGPU.Scene.DrawingContext.DrawGlyphRun(...)`, and `ProGpuCompositionCommandSink` into native `DrawGlyphRun` render commands.
- Added scoped `BitmapScalingMode` replay for WPF-shaped visuals: `NearestNeighbor` maps to ProGPU nearest-neighbor texture sampling, `LowQuality`, `Linear`, and default modes keep linear texture sampling, and `HighQuality`/`Fant` map to shader-side cubic texture sampling.
- Extended ProGPU texture draw commands, compositor draw calls, and texture bind-group cache keys with `TextureSamplingMode` so nearest, linear, and cubic sampler paths for the same texture do not alias.
- Extended reflected `DrawingGroup.BitmapScalingMode` replay through the same scoped sampling path, including `HighQuality`/`Fant` as cubic texture sampling while unknown values still report partial replay when supported children draw.
- Centralized reflected bitmap-scaling alias handling in `WpfBitmapScalingModeReflection` so visual replay, drawing-resource replay, and the ProGPU sink share the same WPF enum-name/numeric-value mapping.
- Updated ProGPU's Skia-compatible bitmap/image shims so installed bitmap pixels preserve source row stride, copied/converted rows skip padding, BGRA bitmap uploads normalize to RGBA textures, negative `SKImage.ReadPixels(...)` source origins clip into the destination instead of underflowing, and `SKSurface.Snapshot()`/bitmap-created images own their generated textures while borrowed texture images remain non-owning.
- Added focused ProGPU coverage for row-strided `SKBitmap.InstallPixels(...)`, BGRA `SKImage.FromBitmap(...)` upload, negative-source readback clipping, and owned versus borrowed `SKImage` texture disposal, addressing the ProGPU PR #17 bitmap/image review cluster in the backend used by WPF image rendering.
- Updated ProGPU retained-scene invalidation so `Visual.ClipBounds` changes invalidate cached/effected visuals, zero-opacity scopes restore the previous compositor opacity after `PopOpacity`, and effect caches compare render fingerprints that include effect change versions plus mutable WPF shader constants/sampler metadata.
- Added ProGPU coverage for `ClipBounds` change-version propagation, effect property change versions, zero-opacity pop rendering recovery, and WPF shader-effect cache-key changes for mutable constants and sampler metadata, addressing the latest ProGPU PR #17 invalidation review cluster used by WPF cached visual/effect rendering.
- Added scoped reflected `EdgeMode=Aliased` replay for WPF-shaped visuals and drawing groups; `ProGpuCompositionCommandSink` stamps native vector commands with `RenderCommand.IsEdgeAliased`, ProGPU encodes that state into the existing vector `ShapeType` channel, and the vector shader thresholds primitive, stroke, curve, and path-atlas coverage instead of smoothing it. `ClearTypeHint` remains reported as unsupported partial render-option state.
- Added native ProGPU `TextRenderingMode` command state for `Grayscale`, `Aliased`, and `ClearType`; `ProGpuCompositionCommandSink` now maps reflected WPF visual and drawing-group `TextRenderingMode` values onto that enum, ProGPU keeps `IsTextAliased` as a compatibility property, static DXF text records preserve the mode, and the text shader renders ClearType with RGB subpixel atlas sampling instead of requiring a managed WPF-side workaround. `ClearTypeHint` and `TextHintingMode` remain unsupported.
- Validated the native ClearType slice with focused ProGPU `TextRenderingModeRenderTests`, clean `ProGPU.Scene` and `ProGPU.Wpf` builds, a clean `ProGPU.Wpf.Tests` build, and the focused WPF replay/reflection filter covering `WpfReplayToProGpuCommandTests`, `WpfVisualTreeReflectionRendererTests`, and `WpfReflectionResourceResolverTests`.
- Addressed the ProGPU PR #17 `Bitmap.LockBits` rectangle-validation review cluster in the backend: `System.Drawing.Bitmap.LockBits(...)` now rejects non-positive, negative-origin, and overhanging lock rectangles before GPU readback or unsafe row-pointer math, with focused `GdiShimTests` coverage for rejected and accepted rectangles.
- Added ProGPU submodule commit `cad4257`: Skia `SKCanvas.DrawImage(...)` now honors `SKPaint.Color.A` by wrapping native texture draws in `PushOpacity`/`PopOpacity`, and System.Drawing `Graphics.DrawImage(...)` now records the untransformed local destination rectangle plus the full current affine transform instead of baking only two transformed rectangle corners.
- Addressed the matching ProGPU PR #17 image-draw review cluster with replies on the active Skia paint-opacity and GDI transform threads, backed by focused `SkCanvasStateTests`, `GdiShimTests`, and `git diff --check` validation.
- Added ProGPU submodule commit `87518e9`: System.Drawing `Bitmap.UnlockBits(...)` now respects `ImageLockMode.ReadOnly` by skipping texture writeback while preserving read-write lock behavior, and bitmap finalization releases resources without flushing recorded GPU drawing work from the finalizer thread.
- Addressed the matching ProGPU PR #17 read-only `LockBits` and bitmap-finalizer review threads, backed by focused `GdiShimTests` and `git diff --check` validation.
- Added ProGPU submodule commit `5031ceb`: System.Drawing `Bitmap.Flush()` now preserves existing texture pixels when replaying recorded GDI draws over a bitmap that already has defined contents from file load, `SetPixel`, earlier flush, or writeable `UnlockBits`, while first flush for a new bitmap still clears the target deterministically.
- Addressed the matching ProGPU PR #17 bitmap-flush preservation thread, backed by focused pixel-level `GdiShimTests`, a clean `System.Drawing.Common` build, and `git diff --check`.
- Added ProGPU submodule commit `e08fb87`: Skia `SKBitmap.Decode(SKCodec, SKImageInfo)` now decodes actual encoded image bytes through StbImageSharp and copies pixels into the requested RGBA or BGRA bitmap layout instead of returning a blank allocation.
- Addressed the matching ProGPU PR #17 `SKCodec` decode review thread, backed by focused `SkImageBitmapTests`, a clean `SkiaSharp` build, and `git diff --check`.
- Added GPU-free coverage for reflected pen dash metadata preservation, line-cap metadata preservation, line-join/miter metadata preservation, path-segment smooth-join metadata preservation, native ProGPU pen line-join/miter/cap metadata emission, native `DrawPath` smooth-join metadata emission, native vector bevel/miter/round line-line join geometry, tangent-driven curve join geometry, miter-limit bevel fallback, smooth-join hard-corner suppression, native vector square/round/triangle line-cap geometry including tangent-driven curve endpoint caps, native arc center/flattening geometry, native arc path compiler records, zero-radius arc line fallback, zero-length dot dash metadata preservation, decoded MIL dashed-line segment emission, decoded MIL dotted-line segment emission, square-capped line endpoint extension, round dash-cap geometry emission, triangle-capped line geometry emission, triangle dash-cap geometry emission, filled dashed-rectangle outline segment emission, filled dashed rounded-rectangle/ellipse segment emission, filled dashed geometry segment emission, and filled dashed combined-geometry operand-boundary segment emission with unsupported-fidelity accounting.
- Documented the current assembly-boundary decision: WPF `PresentationCore` is strong-named, `ProGPU.Wpf` is currently unsigned and points at the shim `PresentationCore`, so direct external sink installation is deferred until signing/type identity is resolved.
- Installed the repo-pinned .NET SDK `11.0.100-preview.4.26210.111` into the ignored `.dotnet/` directory through `./eng/common/dotnet.sh --info`, unblocking root WPF SDK selection on this machine.
- Updated `eng/WpfArcadeSdk/tools/Pbt.props` so Core MSBuild builds local `PresentationBuildTasks` for `$(BundledNETCoreAppTargetFramework)` instead of stale `net6.0`; `PresentationBuildTasks` now restores/builds for `net11.0` in the root lane.
- Updated WPF build tooling for the macOS/Linux bring-up lane: `DirectWriteForwarder.vcxproj` project references are conditioned to Windows, ApiCompat now resolves the `Microsoft.DotNet.ApiCompat` `tools/net9.0` layout used by the pinned package, and Perl-backed source generation uses `/usr/bin/perl` outside Windows instead of Strawberry Perl's `portableshell.bat`.
- Extended the same Windows-only `DirectWriteForwarder.vcxproj` guard to `ReachFramework.csproj`, which is pulled into the `PresentationFramework` graph and otherwise imports Visual C++ targets on macOS.
- Added a non-Windows managed `MS.Internal.Text.TextInterface` compile boundary in `PortableTextInterface.cs`, preserving WPF's expected DirectWriteForwarder-managed type surface while marking unimplemented runtime shaping and subsetting paths as portable text work.
- Conditioned WPF's DirectWrite-specific `Factory.cs` and `FontCollectionLoader.cs` to Windows and guarded `ModuleInitializer` so non-Windows loads do not call `user32.dll`, `dwrite.dll`, or the native WPF DWrite loader.
- Restored the legacy non-generic `MS.Internal.Span` payload struct and updated mutable `FrugalStructList<Span>` writes for modern C# value-type semantics.
- Added non-Windows compile shims for `TrueTypeSubsetter`, `NativeWPFDLLLoader`, and `DWriteTypeConverter` so remaining DirectWriteForwarder references are isolated behind explicit portable replacement points.
- Replaced the throw-only portable font collection/font family/font/font-face path with a managed SFNT font boundary for `.ttf`, `.otf`, and `.ttc` files.
- Added portable TTC face-index parsing, SFNT table directory validation, localized `name` string extraction, `head`/`hhea`/`maxp`/`hmtx`/`OS/2`/`post` metrics, cmap format 4 and format 12 Unicode mapping, `glyf`/`loca` basic glyph bounds, embedding-right reads, and GSUB/GPOS/GDEF table byte access for WPF's existing OpenType layout cache.
- Added cross-platform system font folder scanning for the portable text factory on macOS, Linux, and Windows, while continuing to use WPF's existing `IFontSourceCollection` enumeration for custom font folders and packaged font sources.
- Documented the text assembly-boundary decision: direct `ProGPU.Text` references from root `PresentationCore` are deferred because WPF remains on Arcade strong-name signing and ProGPU assemblies are currently unsigned, so this slice keeps the SFNT reader self-contained while mirroring ProGPU's `TtfFont` responsibilities.
- Added a basic portable `TextAnalyzer` glyph fallback that maps one Unicode scalar to one glyph, preserves cluster maps including surrogate pairs, treats formatting controls conservatively, and computes glyph advances from portable font-face design metrics for `GetGlyphs`, `GetGlyphPlacements`, and `GetGlyphsAndTheirPlacements`.
- Replaced the non-Windows `TrueTypeSubsetter.ComputeSubset` hard throw with a conservative full-font-copy fallback so `GlyphTypeface.ComputeSubset` and XPS font embedding paths can proceed cross-platform until an exact managed subsetter is available.
- Added source-level coverage that guards the portable text boundary against regressing to throw-only font wrappers/analyzers/subsetter and verifies real OpenType table tags, source enumeration, cmap mapping, glyph metrics, table lookup, embedding-right hooks, simple glyph mapping, scalar reading, placement hooks, and the subsetter full-copy fallback are present.
- Added XML-backed project-graph coverage that keeps the managed WPF `PresentationCore`, `PresentationFramework`, and `ReachFramework` references to `DirectWriteForwarder.vcxproj` Windows-only.
- Verified with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore`.
- Verified with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` from `src/ProGPU.Wpf` after bounded tile-brush relative/absolute viewport, absolute and relative brush transforms, cropped viewbox, bounded tile/flip-mode, transformed tiled-brush, tile-brush stretch/alignment, render-data-inferred visual opacity-mask bounds, WPF-shaped affine transform fallback, radial-gradient radius/origin preservation, gradient spread-method coverage, bounded gradient mapping-mode coverage, gradient coordinate-transform coverage including non-axis-aligned radial transforms, supported scRGB gradient color-interpolation coverage, unrecognized reflected gradient color-interpolation diagnostics, non-invertible gradient-transform diagnostics, many-stop gradient preservation, all-stop bridge preservation, the ProGPU gradient stop-table ABI, Y-guideline snapping, dynamic `GuidelineSet` X/Y snapping, straight dashed-line replay, dotted-line replay, square/round/triangle line-cap replay, WPF line-join/miter/cap metadata replay, WPF path-segment smooth-join metadata replay, native line-line and tangent-driven curve join geometry, native open line/Bezier path cap geometry, native arc flattening geometry, native arc path compiler records, dashed rectangle-outline replay, dashed rounded-rectangle/ellipse outline replay, dashed path-compatible geometry outline replay, dashed combined-geometry operand-boundary replay, glyph-run local font URI replay, glyph-run style-simulation flag replay, scoped visual/drawing-group nearest/low-quality/linear/high-quality/fant bitmap-scaling replay, scoped visual/drawing-group aliased edge-mode replay, scoped visual/drawing-group aliased/grayscale/ClearType text-rendering-mode replay, the portable text boundary source contract, and managed WPF project-graph DirectWrite guards; 341 tests passed.
- Verified with `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`; the real WPF `PresentationCore` now builds on macOS with 0 warnings and 0 errors.
- Verified with `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationFramework/PresentationFramework.csproj --no-restore --verbosity minimal`; the real WPF `PresentationFramework` graph now builds on macOS after restore and after guarding the transitive `ReachFramework` native DirectWrite reference.
- Fast-forwarded ProGPU submodule branch `fix/render-invalidation-and-leaks` to latest origin commits `61fce4c`, `805e8e8`, and `f126b6e`, then reapplied the WPF-port rendering work on top.
- Committed and pushed ProGPU branch commit `d84212b` with WPF-oriented vector rendering fidelity improvements, including reusable ProGPU geometry helpers and compositor/shader changes needed by WPF stroke, dash, gradient, sampling, and text-rendering semantics.
- Committed and pushed ProGPU branch commit `8d725c1`, adding repo-level strong-name signing for ProGPU assemblies, key `eng/ProGPU.snk`, SDK pin `global.json`, and a signing regression test. Built ProGPU assemblies report public key token `c29c9752855ee183`.
- Committed and pushed ProGPU branch commit `12acfc1`, adding reusable `ProGPU.Text.SfntFontFace` metadata/table parsing, TTC face enumeration, localized `name` string reads, `FontApi` system-font discovery updates, and deterministic SFNT/TTC tests. Verified with `dotnet build src/ProGPU.Text/ProGPU.Text.csproj --no-restore --verbosity minimal` and focused `SfntFontFaceTests` plus `StrongNameSigningTests`.
- Committed and pushed ProGPU branch commit `24a26df`, making `SfntFontFace` public for `ProGPU.Text` builds but internal when source-shared into WPF, so `PresentationCore` can reuse the helper without exposing ProGPU namespaces in the WPF public contract.
- Linked `external/ProGPU/src/ProGPU.Text/SfntFontFace.cs` into non-Windows `PresentationCore`, and routed portable text face-offset enumeration plus OpenType table-byte reads through the ProGPU-owned helper while keeping WPF-local metrics, cmap, shaping, and subsetting fallback code in place.
- Verified the source-sharing slice with `dotnet clean src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --verbosity minimal` followed by `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`; the real WPF `PresentationCore` builds with 0 warnings and 0 errors.
- Verified source-level contracts with focused `WpfManagedProjectGraphTests` and `WpfPortableTextInterfaceTests`; 5 tests passed.
- Committed and pushed ProGPU branch commit `b451f6a`, adding reusable SFNT APIs for glyph count, cmap format 4/12 lookup, symbol-cmap detection, horizontal glyph metrics, `glyf`/`loca` bounds, and OS/2 embedding rights, with deterministic synthetic-font coverage.
- Routed WPF `PortableTextInterface` glyph count, glyph index lookup, glyph advances/bearings, glyph bounds, symbol-cmap detection, and embedding-right reads through the source-shared ProGPU helper, removing the duplicate WPF-local cmap/hmtx/loca/glyf parsing block while retaining WPF-specific wrapper and fallback policy.
- Added `reports/progpu-wpf-progpu-branch-plan.md` to track backend features that should be implemented directly in ProGPU to accelerate the WPF port instead of accumulating temporary bridge code.
- Added WPF `ObjectRenderDataDrawingContextSink` plus `RenderDataDrawingContextSinkProvider.PushObjectSinkFactory(...)`, allowing generated real-WPF render-data calls to forward to object-shaped sinks without requiring ProGPU.Wpf to reference real WPF media types.
- Added `WpfObjectRenderDataDrawingContext` in ProGPU.Wpf and updated `WpfRenderDataSinkProviderBridge` to prefer object-sink provider activation, with explicit provider `Type` and `Assembly` overloads for hosts that can see the real WPF `PresentationCore` assembly while ProGPU.Wpf still references the shim.
- Threaded the active `IWpfImageSourceAdapter` through composition-target and Silk.NET host render-data provider registration so object-sink `DrawImage` calls can continue using the existing reflected image-source adaptation path.
- Verified object-sink activation and conversion with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` from `src/ProGPU.Wpf`; 347 tests passed.
- Added `src/ProGPU.Wpf.RealPresentationCoreHarness`, a `net11.0` integration harness that loads the real built `PresentationCore.dll`, registers `WpfRenderDataSinkProviderBridge.TryRegisterRenderDataSinkProvider(Assembly, ...)` against the real `RenderDataDrawingContextSinkProvider`, and proves the real `ObjectRenderDataDrawingContextSink` forwards `PushOpacity`/`Pop` into a ProGPU frame.
- The harness creates uninitialized real `DrawingVisual` owner instances for the provider call so the debug-only `CreateSink(Visual)` non-null contract is exercised without running WPF's dispatcher-bound constructor on macOS.
- Verified the real-provider bridge with `dotnet run --project src/ProGPU.Wpf.RealPresentationCoreHarness/ProGPU.Wpf.RealPresentationCoreHarness.csproj --no-restore --verbosity minimal`; the harness completed with `Real PresentationCore render-data provider registration succeeded.`
- Verified WPF builds after the object-sink hook with `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal` and `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationFramework/PresentationFramework.csproj --no-restore --verbosity minimal`; both completed with 0 warnings and 0 errors.
- Attempted the documented WPF MCG path with `dotnet build src/Microsoft.DotNet.Wpf/src/WpfGfx/codegen/mcg/mcg.proj --verbosity minimal`. Restore completed for `mcg.proj` and `csp.csproj`, then build stopped before generation because `csp.csproj` still targets `net48`/x86 and the macOS lane has no .NET Framework 4.8 reference assemblies (`MSB3644`). No generated files changed.
- Added a portable `net10.0` target to the legacy CSP tool, with a Roslyn-backed dynamic compile path for generated C# Prime projects, path normalization for response/source/reference arguments, and copy-local Roslyn runtime dependencies.
- Normalized MCG `FileCodeSink` output paths so XML destination strings containing Windows backslashes create real platform directories on macOS/Linux instead of literal backslash-named directories.
- Updated `mcg.proj` to keep the existing Windows `.cmd` flow on Windows but run the `Resources.rsp` and `Elements.rsp` generators directly through `dotnet csp.dll` on non-Windows. The non-Windows path relies on the checked-in `ResourceModel/Generated/Elements.cs` XML serialization model.
- Verified portable MCG with `dotnet build src/Microsoft.DotNet.Wpf/src/WpfGfx/tools/csp/csp.csproj -f net10.0 --no-restore --verbosity minimal` and `dotnet build src/Microsoft.DotNet.Wpf/src/WpfGfx/codegen/mcg/mcg.proj -p:McgOutputDirectory=/tmp/wpf-mcg-proj-output --verbosity minimal`; the MCG build completed with expected legacy generator warnings and 0 errors.
- Added ProGPU.Wpf test coverage that source-checks the portable MCG project wiring, the CSP `net10.0` Roslyn compile path, and generated output path normalization. Verified with focused `WpfRenderDataGeneratorRedirectionTests` and the full `ProGPU.Wpf.Tests` project; 350 tests passed.
- Removed the last Windows-only `xsd.exe` dependency from the MCG path: `GenerateElements.cmd` now uses the checked-in Elements XML serialization model directly, and `ResourceModel/Generated/Elements.cs` is source-owned instead of claiming to be regenerated by the Windows SDK tool.
- Added ProGPU.Wpf coverage that parses `xml/Elements.xsd` and verifies the source-owned Elements model exposes the matching XML serializer properties and boolean `Specified` flags. Verified with focused `WpfRenderDataGeneratorRedirectionTests`; 21 tests passed.
- Verified the xsd-free MCG path with `dotnet build src/Microsoft.DotNet.Wpf/src/WpfGfx/codegen/mcg/mcg.proj -p:McgOutputDirectory=/tmp/wpf-mcg-source-model-output --verbosity minimal`; the build completed with expected legacy generator warnings and 0 errors.
- Replaced the WPF-bridge cubic fallback for complex transformed arcs with a ProGPU-owned affine arc transform helper in `ProGPU.Vector.ArcSegmentGeometry`. The helper keeps WPF path arcs as native ProGPU arc segments through translation, scale, rotation, shear, and orientation-reversing transforms, flips sweep for reflected transforms, and falls back only for degenerate arcs.
- Updated `ProGpuCompositionCommandSink` and `WpfReflectionResourceResolver` to call the ProGPU arc transform helper when baking WPF geometry transforms, so transformed geometry draws, clips, combined-geometry operands, and flattened geometry groups preserve native arc metadata instead of generating managed cubic Bezier approximations.
- Verified ProGPU signing remained active through the existing ProGPU branch signing setup (`Directory.Build.props`, `eng/ProGPU.snk`) and focused `StrongNameSigningTests`; the ProGPU assemblies remain strong-name signed for WPF strong-name integration planning.
- Verified native transformed arc coverage with focused `ArcSegmentGeometryTests`, `WpfReplayToProGpuCommandTests`, and `WpfReflectionResourceResolverTests`; 151 tests passed.
- Verified the full WPF bridge suite with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal`; 354 tests passed.
- Verified the ProGPU arc/signing slice with `dotnet build src/ProGPU.Vector/ProGPU.Vector.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~StrongNameSigningTests|FullyQualifiedName~Arc"`; the focused ProGPU test run passed 4 tests with existing project warnings outside this slice.
- Added shader-native ProGPU path arc stroking: `CompilePathCommand` now emits one parametric arc stroke strip per valid arc using transformed ellipse center/axis vectors, the vector WGSL evaluates arc vertices/tangents directly, and degenerate arcs are the only path that falls back to the older flattened line-strip emission.
- Added a headless ProGPU compositor regression test proving stroked path arcs compile to arc shape type `11` instead of line shape type `3`, and verified it with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~ArcStrokeShaderTests`; 1 test passed.
- Re-ran the full WPF bridge suite against the shader-native arc submodule changes with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` from `src/ProGPU.Wpf`; 354 tests passed.
- Added exact ProGPU arc bounds in `ProGPU.Vector.ArcSegmentGeometry` and routed `PathAtlas.CompilePath(...)` plus `PathOpGeometrySolver.CompilePath(...)` through that helper, replacing the previous eight-sample arc bounds approximation so native path-fill/path-op arcs are not clipped in atlas bounds for WPF arc extrema.
- Made `PathAtlas.CompilePath(...)` static so native path compilation can be covered without a live WebGPU context.
- Added ProGPU arc path compiler tests proving partial circle arcs keep GPU path segment type `3` and exact extrema in both the atlas compiler and path-op compiler. Verified with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~ArcPathCompilerTests`; 3 tests passed.
- Re-ran ProGPU arc/signing coverage with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~StrongNameSigningTests|FullyQualifiedName~Arc"`; 8 tests passed.
- Re-ran the full WPF bridge suite against the exact arc-bounds submodule changes with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` from `src/ProGPU.Wpf`; 354 tests passed.
- Added ProGPU-owned `ArcSegmentGeometry.TryCreateSubArcSegment(...)` so callers can split a WPF/SVG arc span while preserving the same ellipse, sweep direction, scaled radii, and large-arc flag for the subspan.
- Replaced the WPF bridge's dashed path-arc cubic/polyline workaround with native dashed arc emission: `ProGpuCompositionCommandSink` now advances dash state over an arc-length table, records visible dash intervals as `DrawPath` commands containing ProGPU `ArcSegment` subspans, and uses the existing shader-native arc stroke path for rendering. Degenerate arcs still fall back to dashed line emission.
- Added ProGPU and WPF bridge regression coverage proving subarc construction and decoded dashed WPF arc geometry produce native arc dash commands instead of line fallback commands.
- Moved dashed arc span construction fully into `ProGPU.Vector.ArcSegmentGeometry.TryCreateDashedArcSegments(...)` with a reusable `ArcDashSegment` result type. The WPF bridge now delegates WPF dash cursor state and receives native arc subspans from ProGPU instead of carrying arc-specific length sampling and subarc parameter lookup locally.
- Verified the native dashed arc API with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ArcPathCompilerTests|FullyQualifiedName~ArcStrokeShaderTests"`; 8 tests passed with existing warnings outside this slice. Verified the WPF bridge consumer with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 tests passed.
- Added `ProGPU.Vector.BezierSegmentGeometry` with de Casteljau quadratic/cubic subdivision and native dashed Bezier span construction. `ProGpuCompositionCommandSink` now delegates WPF dash cursor state for quadratic and cubic path segments to ProGPU and records returned spans as one-segment native `DrawPath` commands instead of flattening those curves to line commands in the WPF bridge.
- Verified native Bezier dash helpers with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~BezierSegmentGeometryTests"`; 5 tests passed. Verified the WPF bridge consumer with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests"` from `/tmp`; 67 tests passed.
- Added `ProGPU.Vector.PrimitivePathGeometry` for reusable rectangle, rounded-rectangle, and ellipse path construction. Rounded rectangle corners and ellipses are represented as native `ArcSegment` values, and WPF dashed primitive replay now routes those shapes through the existing ProGPU arc dash/shader path instead of bridge-local polyline approximation.
- Verified native primitive arc paths with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~PrimitivePathGeometryTests|FullyQualifiedName~ArcPathCompilerTests|FullyQualifiedName~ArcStrokeShaderTests"`; 12 focused ProGPU tests passed. Verified the WPF bridge consumer with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests"` from `/tmp`; 67 focused tests passed.
- Added `ProGPU.Vector.DashPattern` for WPF-compatible dash-array normalization, odd-array duplication, dash offset state, zero-length drawn dash promotion, and straight-line dash segment generation. Arc and Bezier dash helpers now share the same validation/advance logic, and the WPF bridge delegates dash state instead of carrying a local copy.
- Preserved native arcs in the ProGPU WPF/WinUI path API shims. `PathGeometry.Parse(...)` now keeps SVG/WPF arc records, transformed `ArcSegment` values go through `ArcSegmentGeometry.TryTransformArcSegment(...)`, and only degenerate arcs fall back to endpoint lines; headless WinUI rendering verifies those API-created arcs reach the GPU arc shader.
- Verified the reusable dash and native shim arc work with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~PathArcShimTests|FullyQualifiedName~DashPatternTests|FullyQualifiedName~ArcPathCompilerTests|FullyQualifiedName~ArcStrokeShaderTests"`; 20 focused ProGPU tests passed.
- Verified the WPF bridge consumer with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests"` from `/tmp`; 67 focused tests passed.
- Added ProGPU submodule commit `b5a35bf`: valid stroked path arcs now use a fixed four-vertex `ArcSdf` vector primitive instead of the older parametric arc strip. The compositor passes transformed center/axis vectors plus WPF/SVG sweep metadata to WGSL, and the fragment shader solves the nearest ellipse parameter and flat-cap clipping directly for the arc stroke. Degenerate arcs still fall back inside ProGPU rather than in WPF bridge code.
- Verified the fixed-quad arc shader path with `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`; the build completed with 0 warnings and 0 errors. Verified focused ProGPU arc coverage with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ArcStrokeShaderTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~ArcPathCompilerTests"`; 15 tests passed, including a headless pixel smoke check for visible arc stroke output and shape type `12` instead of arc strip type `11` or line type `3`.
- Verified the WPF bridge arc consumer against the fixed-quad ProGPU submodule with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`; the build succeeded with existing ProGPU warning noise and 0 errors. Verified focused WPF bridge arc coverage with `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 tests passed.
- Added `ProGPU.Text.SfntSimpleGlyphShaper`, a source-shareable helper for the current portable one-scalar-to-one-glyph text fallback. It owns surrogate/code-point reading, WPF-compatible cluster-map filling, soft-hyphen mapping, formatting-control blanking, and metric-derived advance calculation.
- Linked `SfntSimpleGlyphShaper.cs` into non-Windows real WPF `PresentationCore` beside `SfntFontFace.cs`, and routed `PortableTextInterface.TextAnalyzer` glyph-run creation and glyph advance filling through the ProGPU helper instead of WPF-local `SimpleGlyphRun`, `ReadCodePoint`, `GetSimpleGlyphIndex`, and control-glyph helpers.
- Added ProGPU text tests for surrogate clusters, soft hyphen/control behavior, and sideways advance calculation, plus WPF source/project graph tests that require the new source-shared helper and reject the removed local simple-shaper code.
- Verified the source-shared text slice with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SfntSimpleGlyphShaperTests|FullyQualifiedName~StrongNameSigningTests"`; 7 tests passed.
- Verified real WPF source-sharing with `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`; `PresentationCore` built with 0 warnings and 0 errors.
- Re-ran the full WPF bridge suite with `dotnet test ../ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` from `src/ProGPU.Wpf`; 356 tests passed.
- Added `ProGPU.Text.SfntFontSubsetter`, a source-shareable glyph-ID-preserving TrueType subset writer. It parses a selected SFNT face, keeps glyph 0 and requested glyph IDs, recursively includes composite glyph component dependencies, rewrites `glyf`, long `loca`, and `head.indexToLocFormat`, recalculates SFNT checksums, and drops stale `DSIG` signatures.
- Linked `SfntFontSubsetter.cs` into non-Windows real WPF `PresentationCore`, and routed `TrueTypeSubsetter.ComputeSubset(...)` through the ProGPU helper before falling back to the full-font copy for unsupported or invalid font shapes.
- Added ProGPU subsetter tests covering composite dependency inclusion, omitted glyph stripping, stale `DSIG` removal, and invalid-font fail-closed behavior, plus WPF source/project graph tests requiring the new source-shared subsetter and call site.
- Added ProGPU compact glyph-remapping TrueType subsetting through `SfntGlyphRemap`, `SfntFontSubsetter.CreateCompactSubset(...)`, and `TryCreateCompactSubset(...)`. The compact path returns an explicit source-to-subset glyph map, recursively includes composite dependencies, rewrites composite component glyph IDs, rebuilds compact long `loca`/`glyf`, updates `maxp`, `hhea`, and `hmtx`, recalculates SFNT checksums, and drops stale glyph-indexed tables such as `cmap`, layout tables, color tables, `post`, and `DSIG`.
- Kept WPF's source-shared `TrueTypeSubsetter.ComputeSubset(...)` on the glyph-ID-preserving path for now because the current WPF API returns only subset bytes. The compact ProGPU path is ready for the later WPF embedding change that can also propagate the returned glyph map.
- Verified compact subset coverage with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SfntFontSubsetterTests"`; 4 tests passed. Verified WPF source-sharing with `dotnet build src/Microsoft.DotNet.Wpf/src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`; `PresentationCore` built with 0 warnings and 0 errors.
- Added ProGPU.Backend `PixelDataConverter`, a reusable WPF/WIC-shaped pixel-row converter to Pbgra32 for 32-bit, 24-bit, packed grayscale, indexed palette, 16-bit grayscale/BGR, high-bit-depth integer RGB/RGBA, scRGB float, and CMYK rows.
- Routed `WpfBitmapSourceImageAdapter` through `PixelDataConverter`, leaving WPF reflection, palette extraction, and shim `WriteableBitmap` construction in the bridge while moving reusable pixel conversion into ProGPU.
- Added a ProGPU-native WPF-style shader-effect path: `WpfShaderEffectParams`, `DrawingContext.DrawWpfShaderEffect(...)`, and `WpfShaderEffectExtensionPipeline` compile WGSL into cached WebGPU render pipelines with explicit compositor-compatible pipeline layouts, source-texture sampling, WPF-style 32-register constant uniforms, bounds/texture-size metadata, and sampler-aware texture bind groups.
- Documented that WPF effects should lower to ProGPU-native shader descriptors rather than managed CPU pixel loops; the remaining bridge work is WPF effect-resource extraction and Direct3D pixel-shader bytecode translation or registry replacement.
- Verified with focused ProGPU scene build and tests: `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectRenderTests|FullyQualifiedName~WpfShaderEffectParamsTests|FullyQualifiedName~PixelDataConverterTests|FullyQualifiedName~StrongNameSigningTests"`.
- Verified the WPF bridge against the ProGPU-owned pixel converter and unchanged replay paths with `dotnet test ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~WpfBitmapSourceImageAdapterTests` and `dotnet test ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfCompositionDrawingContextTests|FullyQualifiedName~WpfReplayToProGpuCommandTests"` from `src/ProGPU.Wpf.Tests`.
- Added WPF visual-effect descriptor extraction for reflected `BlurEffect` and `DropShadowEffect`, mapping radius/depth/direction/color/opacity into ProGPU-native `BlurEffect` and `DropShadowEffect` descriptors.
- Added `IWpfVisualEffectCommandSink` as the capability seam for retained-visual sinks that can push a real native effect scope. The current flat `ProGpuCompositionCommandSink` deliberately does not implement it, so visual effects continue to be reported as unsupported until replay allocates a retained ProGPU visual boundary for the source visual subtree.
- Verified the visual-effect extraction seam with `dotnet test ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~WpfVisualTreeReflectionRendererTests` from `src/ProGPU.Wpf.Tests`; 18 tests passed.
- Extended the ProGPU-native WPF shader-effect path in submodule commit `919799f`: `WpfShaderEffectParams` now supports a fixed 16-slot WPF sampler-register bank, `DrawWpfShaderEffect(...)` accepts sampler-only descriptors, and `WpfShaderEffectExtensionPipeline` binds every sampler slot through native WebGPU bind groups with transparent fallback textures for unbound registers.
- Added WGSL helper `wpf_sample_register(registerIndex, uv)` beside `wpf_sample_source(uv)` so translated WPF `ShaderEffect` code can sample registered inputs without managed CPU shader emulation.
- Verified the ProGPU shader-register slice with `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectParamsTests|FullyQualifiedName~WpfShaderEffectRenderTests"`; 7 focused tests passed, including a headless render sampling sampler register 1.
- Verified the WPF bridge against the updated submodule with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal` and `dotnet test ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfCompositionDrawingContextTests"` from `src/ProGPU.Wpf.Tests`; 39 focused tests passed.
- Added a retained ProGPU scene root for the WPF host path: `ProGpuWpfCompositionTarget` now renders a scene container with retained WPF replay below the existing flat drawing buffer, and `ProGpuWpfDrawingFrame` resets both roots per frame.
- Added `ProGpuRetainedCompositionCommandSink`, which delegates normal WPF replay commands to `ProGpuCompositionCommandSink` but creates bounded retained ProGPU drawing visuals for supported `BlurEffect`/`DropShadowEffect` scopes. Effect content is translated into inferred visual bounds so ProGPU's native `Visual.Effect` offscreen path can apply the effect without managed pixel loops.
- Wired `ProGpuWpfWindowHost.WpfRootVisual` replay through the retained sink while preserving direct `ProGpuWpfCompositionTarget.ReplayVisualSubtree(..., pixelWidth, pixelHeight, ...)` flat replay for existing provider-registration/source-contract callers.
- Verified retained WPF effect replay with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal` and focused `dotnet test ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ProGpuWpfDrawingFrameTests|FullyQualifiedName~ProGpuWpfWindowHostTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfRenderDataSinkProviderBridgeTests|FullyQualifiedName~WpfCompositionDrawingContextTests"` from `src/ProGPU.Wpf.Tests`; 62 focused tests passed.
- Added visual-level native WPF shader effects in ProGPU submodule commit `64516a9`: `ProGPU.Scene.WpfShaderEffect` now renders the affected visual into an offscreen source texture, forces that visual source to WPF sampler register 0, and emits the existing `CompositorBuiltInExtensions.WpfShaderEffect` WebGPU/WGSL draw call on the main pass. This keeps WPF shader execution in native ProGPU instead of adding managed CPU shader emulation.
- Added per-visual cached shader-effect draw parameters in the ProGPU compositor, keeping direct `DrawWpfShaderEffect(...)` sampler semantics unchanged while giving retained `Visual.Effect` the WPF-compatible visual-source sampler behavior.
- Added headless ProGPU coverage proving a visual `WpfShaderEffect` samples the visual's rendered source through the native GPU pipeline even when the effect descriptor also contains a conflicting sampler register 0 texture.
- Verified the ProGPU shader-effect slice with `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectRenderTests|FullyQualifiedName~WpfShaderEffectParamsTests"`; 8 focused tests passed.
- Added ProGPU submodule commit `df6faaf`: retained visual `WpfShaderEffect` now carries `SourceTextureRegisterIndex` into the native draw parameters, so WPF implicit input can bind to the reflected sampler register instead of always overriding sampler register 0.
- Added a WPF native shader-effect replacement registry. `WpfShaderEffectRegistry` registers WGSL replacements by effect type key, pixel-shader URI, or pixel-shader bytecode SHA-256 key; the reflection bridge maps registered WPF-shaped `ShaderEffect` instances to `ProGPU.Scene.WpfShaderEffect` descriptors with float constant registers, implicit input sampler register, nearest/linear sampling, and shader padding preserved.
- Missing shader replacements remain explicit unsupported visual state, and unsupported sampler brush inputs still fail closed. The bridge does not add a managed CPU shader fallback; raw Direct3D pixel-shader bytecode still needs a ProGPU-native translator/import path before existing WPF HLSL effects can run automatically.
- Verified the ProGPU shader source-register slice with `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectRenderTests|FullyQualifiedName~WpfShaderEffectParamsTests"`; 9 focused tests passed.
- Verified the WPF bridge with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~WpfVisualTreeReflectionRendererTests` from `/tmp`; 21 focused tests passed. The root `dotnet test` command is currently blocked by the WPF repo's `global.json` Microsoft.Testing.Platform policy while this xUnit test project still uses VSTest.
- Added ProGPU submodule commit `e8245f7`: the native `WpfShaderEffect` uniform ABI now includes source-register metadata, and the WGSL helper `wpf_sample_source(uv)` samples the reflected WPF implicit-input register instead of hard-coding sampler register 0. This keeps registered shader replacements WPF-API-friendly without a managed shader workaround.
- Verified the ProGPU shader-helper slice with `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectRenderTests|FullyQualifiedName~WpfShaderEffectParamsTests"`; 10 focused tests passed.
- Added source/object generated `PushEffect` lowering for supported context-bound legacy `BitmapEffect` objects that WPF can emulate with modern effect descriptors. The bridge uses WPF's emulation hook only to obtain a descriptor, then records a native ProGPU retained effect scope; it does not run a managed pixel loop.
- Updated unsupported `PushEffect` handling so unresolved effects push a balanced no-op scope and report partially applied unsupported state, preserving generated `Pop`/close-time stack behavior while making missing native coverage visible.
- Extended reflected visual replay so supported context-bound legacy `BitmapEffect` scopes lower to native retained ProGPU effects. Non-context `BitmapEffectInput`, non-emulatable bitmap effects, and unsupported sampler resources remain explicit unsupported state.
- Verified this native effect-scope slice with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfCompositionDrawingContextTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests"` from `/tmp`; 46 focused tests passed.
- Extended reflected `DrawingGroup` replay so supported `Effect` descriptors and context-bound emulatable legacy `BitmapEffect` descriptors push native retained ProGPU effect scopes around the group's child drawing content. The bridge still fails closed for cache state, non-adaptable transform/clip state, non-context bitmap-effect inputs, non-emulatable bitmap effects, and unsupported shader resources rather than replaying children with missing visual state.
- Verified the drawing-resource effect slice with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~WpfReflectionResourceResolverTests` from `/tmp`; 80 focused tests passed.
- Added `ProGpuWpfFrameState` and a conservative `ProGpuWpfWindowHost` frame-coalescing gate. The host records the last presented pixel size plus native scene, retained-WPF, and flat-drawing change-version counters, then skips unchanged Silk.NET render callbacks when there is no explicit `Draw`/`WpfDraw`/`Render` callback and no pending scheduler request.
- Verified the frame-coalescing slice with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~ProGpuWpfWindowHostTests|FullyQualifiedName~CoalescingWpfRenderSchedulerTests"` from `/tmp`; 20 focused tests passed. Direct root `dotnet test` remains blocked by the WPF repo's `global.json` Microsoft.Testing.Platform policy while this xUnit test project still uses VSTest.
- Added reflected source version polling to `WpfVisualInvalidationTracker`, capturing numeric `ChangeVersion`, `InternalVersion`, `Version`, `_changeVersion`, `_internalVersion`, and `_version` snapshots across the tracked WPF-shaped visual/resource graph.
- Wired `ProGpuWpfCompositionTarget.DetectWpfSourceChanges()` into `ProGpuWpfWindowHost` before frame-state capture, so silent source-version mutations dirty the retained ProGPU roots and set a pending render request before unchanged-frame coalescing can skip the Silk.NET callback.
- Verified the version-polling slice with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfVisualInvalidationTrackerTests|FullyQualifiedName~ProGpuWpfWindowHostTests|FullyQualifiedName~WpfRenderDataSinkProviderBridgeTests"` from `/tmp`; 37 focused tests passed.
- Added `DispatcherWpfRenderScheduler`, which keeps the existing `IWpfRenderScheduler` contract but schedules coalesced render requests through a one-shot `IWpfTimer` and then posts `RenderRequested` at WPF-shaped `Render` dispatcher priority.
- Updated `ProGpuWpfWindowHost` to use `DispatcherWpfRenderScheduler` by default when platform dispatcher/timer services are available, fall back to `CoalescingWpfRenderScheduler` when they are not, dispose host-owned schedulers, and preserve caller-supplied custom schedulers across platform-service changes.
- Verified the dispatcher/timer scheduling slice with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~DispatcherWpfRenderSchedulerTests|FullyQualifiedName~ProGpuWpfWindowHostTests|FullyQualifiedName~CoalescingWpfRenderSchedulerTests"` from `/tmp`; 27 focused tests passed. A parallel library/test build attempt hit a transient MSBuild reference-assembly file lock, so validation was rerun sequentially.
- Wired `ProGpuWpfWindowHost` to observe `IWpfRenderScheduler.RenderRequested`, detach from replaced/disposed schedulers, and issue a dispatcher-thread, non-reentrant Silk.NET `DoRender()` wakeup when a native window is available.
- Added host regression tests covering scheduler wakeup delivery, scheduler replacement disconnecting the previous wakeup source, and disposal detaching scheduler wakeups.
- Verified the scheduler wakeup bridge with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~ProGpuWpfWindowHostTests|FullyQualifiedName~DispatcherWpfRenderSchedulerTests|FullyQualifiedName~CoalescingWpfRenderSchedulerTests"` from `/tmp`; 30 focused tests passed.
- Added ProGPU submodule commit `fa97682`: removed the segmented `ArcStrip` stroke fallback from `ProGPU.Scene.Compositor` and `ProGPU.Backend.Shaders`, leaving valid stroked path arcs on the fixed-quad `ArcSdf` shader primitive and reserving line fallback for arcs that cannot produce valid shader parameters.
- Added ProGPU headless coverage proving transformed/sheared stroked arcs still emit exactly four `ArcSdf` vertices and no line or removed arc-strip shape vertices.
- Verified the ProGPU arc cleanup with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~ArcStrokeShaderTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~ArcPathCompilerTests"` from the ProGPU submodule; 16 focused tests passed. Existing warning noise remains outside this slice.
- Verified the WPF bridge against the updated submodule with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 focused tests passed.
- Added ProGPU.Backend `PixelDataBuffer` and `Pbgra32PixelBuffer` as the reusable image-upload contract on top of `PixelDataConverter`, including public source stride and byte-length helpers plus indexed-palette requirement validation.
- Routed `WpfBitmapSourceImageAdapter` through `PixelDataBuffer`, leaving WPF-specific reflection, palette extraction, `CopyPixels(...)`, and shim `WriteableBitmap` allocation in the WPF bridge while moving upload-shape validation into ProGPU.Backend.
- Added ProGPU backend coverage for `PixelDataBuffer` conversion, missing indexed-palette rejection, and packed-row source byte-length calculation.
- Verified the ProGPU image-upload contract with `dotnet build src/ProGPU.Backend/ProGPU.Backend.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~PixelDataConverterTests`; 8 focused tests passed. The full ProGPU test-project build still carries existing warning noise outside this slice.
- Verified the WPF bitmap adapter against the updated submodule with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~WpfBitmapSourceImageAdapterTests` from `/tmp`; 16 focused tests passed. A no-build test invocation with the repo-local `/Users/wieslawsoltes/GitHub/wpf/.dotnet/dotnet` is not usable for this net10 testhost because that local runtime currently contains only .NET 11 preview; the installed `/Users/wieslawsoltes/.dotnet/dotnet` runtime has `Microsoft.NETCore.App 10.0.5`.
- Extended the ProGPU image-upload contract so `Pbgra32PixelBuffer` owns compact row packing, `GpuTexture` owns `WritePbgra32(...)`/`WritePbgra32SubRect(...)` with BGRA texture-format and sub-rectangle validation, and the ProGPU shim `WriteableBitmap` forwards PBgra32 uploads through those backend APIs instead of carrying local padded-row copy logic.
- Updated `WpfBitmapSourceImageAdapter` to return a `Pbgra32PixelBuffer` for the upload path and call shim `WriteableBitmap.WritePbgra32Pixels(...)`, removing the bridge-local `GCHandle` pinning path while preserving the existing byte-array helper for tests and compatibility.
- Added backend coverage for compact PBgra32 row reuse and padded-row stripping, and WPF bridge coverage for the typed upload-buffer path.
- Verified the ProGPU texture-upload seam with `git diff --check`, `dotnet build src/ProGPU.Backend/ProGPU.Backend.csproj --no-restore --verbosity minimal`, `dotnet build src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~PixelDataConverterTests`; 10 focused tests passed. A parallel build attempt hit the known transient MSBuild reference-assembly file lock, and the backend build was rerun sequentially successfully.
- Verified the WPF typed bitmap upload path with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter FullyQualifiedName~WpfBitmapSourceImageAdapterTests` from `/tmp`; 17 focused tests passed.
- Added ProGPU submodule commit `66817b7`: the WPF `PresentationCore` path shim now preserves original WPF path figures and arc segments, then records `Geometry.Transform` on the native `DrawPath` command instead of baking transformed points in managed shim code.
- This keeps WPF API arcs on ProGPU's native path-atlas and fixed-quad arc-SDF shader paths for transformed filled and stroked geometry, with degenerate fallback decisions kept inside the ProGPU vector layer rather than in WPF compatibility code.
- Verified the native WPF arc handoff with `git diff --check`, `dotnet build src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~PathArcShimTests|FullyQualifiedName~ArcStrokeShaderTests|FullyQualifiedName~ArcPathCompilerTests"` from the ProGPU submodule; 17 focused tests passed. A parallel test-project build attempt hit the known transient MSBuild deps-file lock and was rerun sequentially successfully.
- Added ProGPU submodule commit `1e92cd2`: the ProGPU WPF `DrawingContext.PushClip(Geometry)` shim now emits native `PushGeometryClip`/`PopGeometryClip` commands for path-compatible clips, with `Geometry.Transform` composed with the active drawing transform, instead of using axis-aligned bounds rectangles for all geometry clips.
- Added ProGPU submodule commit `b932568`: the `Geometry.TryGetPathGeometry(...)` hook is opt-in with a default `false` fallback, so WPF bridge geometry subclasses such as combined geometry keep bounds fallback behavior until they can expose a native path/mask representation.
- Added `Geometry.TryGetPathGeometry(...)` and shared `PathGeometry.ToProGpuPathGeometry()` conversion so `PathGeometry` and `StreamGeometry` clips reuse the same native path records as `DrawGeometry`, preserving line, quadratic/cubic Bezier, arc, close/fill, and smooth-join metadata for the ProGPU mask path.
- Improved the remaining rectangular fallback to transform all four geometry-bounds corners before computing the fallback clip rectangle, so rotated or sheared unsupported geometry still gets a conservative bounds clip while native geometry-mask support is expanded.
- Verified the native geometry-clip handoff with `git diff --check`, `dotnet build src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfDrawingContextClipTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~ArcStrokeShaderTests"` from the ProGPU submodule; 10 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `b932568` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 focused tests passed. The first WPF bridge build against `1e92cd2` caught an abstract geometry-hook compatibility issue for bridge-owned geometry subclasses, which was fixed in ProGPU commit `b932568`.
- Added ProGPU submodule commit `650945f`: `ProGPU.Scene.Compositor.PushClipRect(...)` now transforms all four local clip corners before computing the native scissor rectangle, fixing rotated and sheared WPF visual/scrollable rectangular clips in the backend instead of adding managed WPF-side compensating bounds logic.
- Added headless ProGPU coverage that renders a skewed rectangular clip and samples pixels inside the corrected four-corner scissor area but outside the legacy two-corner bounds, proving the native draw-call scissor preserves the visible content.
- Verified the transformed clip slice with `git diff --check`, `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~CompositorClipTests|FullyQualifiedName~WpfDrawingContextClipTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~ArcStrokeShaderTests"` from the ProGPU submodule; 11 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `650945f` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 97 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this clip slice.
- Added ProGPU submodule commit `f6ba74e`: `ProGPU.Vector.PathGeometry.CreateTransformed(...)` now provides reusable affine path transforms that preserve native arc segments, and the ProGPU WPF shim now includes `CombinedGeometry`/`GeometryCombineMode` mapping WPF `Union`, `Intersect`, `Xor`, and `Exclude` to native ProGPU path-op geometry for drawing and clipping.
- Updated `ProGpuCompositionCommandSink` to use `PathGeometry.CreateTransformed(...)` for recorded native paths and combined path operands, removing the bridge-local duplicate path-transform implementation so transformed arc preservation stays owned by ProGPU.Vector.
- Verified the native combined-geometry clip slice with `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal -m:1` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfDrawingContextClipTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~CompositorClipTests|FullyQualifiedName~ArcStrokeShaderTests"` from the ProGPU submodule; 14 focused tests passed. Verified the WPF bridge with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 focused tests passed.
- Added ProGPU submodule commit `9ecf516`: the WPF `PresentationCore` shim now includes native-path `LineGeometry`, `RectangleGeometry`, `EllipseGeometry`, and `GeometryGroup` implementations, plus a shared `GeometryPathHelper` for draw/bounds forwarding.
- Added `ProGPU.Vector.PathGeometry.TryGetBounds(...)` for ordinary and combined paths, so shim path, primitive, group, and combined geometry bounds reuse ProGPU arc-aware vector code instead of duplicating managed bounds logic.
- Addressed the ProGPU PR #17 `PathGeometry.IsFilled=false` review item: WPF shim `PathGeometry.Draw(...)` now excludes unfilled figures from fill commands while preserving the full path for stroke commands, and keeps the previous single native draw command when all figures are fillable.
- Verified the ProGPU geometry-shim slice with `dotnet build src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal -m:1` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfDrawingContextClipTests|FullyQualifiedName~WpfPathGeometryDrawTests|FullyQualifiedName~PathArcShimTests|FullyQualifiedName~PrimitivePathGeometryTests"` from the ProGPU submodule; 19 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `9ecf516` with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 focused tests passed.
- Added ProGPU submodule commit `f942988`: native `ProGPU.Vector.PathGeometry` now carries `FillRule`, path compiler records write that rule into the GPU path-record ABI, and the path rasterizer/path-op shaders evaluate either WPF `EvenOdd` or `Nonzero` fill natively instead of relying on WPF-side contour rewriting.
- Added per-segment `IsStroked` to ProGPU vector path segments and the WPF `PresentationCore` shim, with `StreamGeometryContext` preserving every `isStroked` parameter. The compositor skips non-stroked segments while still letting those segments participate in fill paths, and treats skipped stroke spans as contour breaks for native cap/join generation.
- Updated WPF/WinUI path and WPF `GeometryGroup` shims to forward `FillRule` into native ProGPU path commands, and preserved the new segment stroke metadata through affine path transforms, Bezier subdivision, arc transforms, dashed span helpers, and path-op figure copies.
- Addressed the remaining ProGPU PR #17 WPF path review items for `FillRule` and `StreamGeometryContext` `isStroked` preservation in backend code rather than managed bridge workarounds.
- Verified the native fill/stroke slice with `git diff --check` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --filter "FullyQualifiedName~ArcPathCompilerTests|FullyQualifiedName~WpfPathGeometryDrawTests|FullyQualifiedName~ArcStrokeShaderTests"` from the ProGPU submodule; 19 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `f942988` with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 75 focused tests passed.
- Added ProGPU submodule commit `ba59fdf`: `PathOpGeometrySolver` now sizes the path-op output buffer to the WGSL split capacity of 15 kept spans per input segment, passes that capacity through the path-op uniform, guards `destSegments.segments[destIdx]` writes after the atomic append, and clamps the finalized record count to the same bound. This addresses the ProGPU PR #17 path-op output-bounds review comments and keeps WPF combined geometry on the native GPU boolean path instead of forcing managed-side safety workarounds.
- Updated `ProGpuCompositionCommandSink` so filled dashed combined geometries first resolve their native ProGPU path-op result when a WebGPU context is available, then dash that boolean-result boundary. The context-free transition/test fallback still emits operand-boundary dash segments and reports unsupported fidelity.
- Preserved WPF `PathGeometry.FillRule` and `PathSegment.IsStroked` metadata when the WPF bridge converts real/shim path geometry into `ProGPU.Vector.PathGeometry`, aligning the bridge with the ProGPU ABI added in `f942988`.
- Verified the ProGPU path-op bound with `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~ArcPathCompilerTests`; 13 focused tests passed with existing warning noise outside this slice.
- Verified the WPF bridge against ProGPU submodule head `ba59fdf` with `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1` and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~ArcSegmentGeometryTests"` from `/tmp`; 77 focused tests passed. A direct root `dotnet test` remains blocked by the WPF repo's `global.json` Microsoft.Testing.Platform policy while this xUnit test project still uses VSTest.
- Added ProGPU submodule commit `759191f`: `WgpuContext.CleanupPendingResources()` now releases queued bind-group layouts, pipeline layouts, render pipelines, compute pipelines, and shader modules instead of clearing those queues without native release. This addresses the ProGPU PR #17 queued pipeline/shader resource leak review comments.
- Verified the ProGPU backend resource-release fix with `dotnet build src/ProGPU.Backend/ProGPU.Backend.csproj --no-restore --verbosity minimal`; 0 warnings and 0 errors.
- Added ProGPU submodule commit `d697304`: WPF shim `WriteableBitmap` now preserves the requested `PixelFormat`, exposes `Format`, validates `WritePixels(...)` byte length through backend stride helpers, keeps padded PBgra32 rows on the `Pbgra32PixelBuffer` upload path, and converts `Bgr32` writes to opaque PBgra32 before `GpuTexture.WritePbgra32SubRect(...)`.
- Added `WriteableBitmapTests` coverage for distinct shim pixel formats, padded PBgra32 row handling, and Bgr32-to-opaque-PBgra32 upload conversion.
- Verified the ProGPU WriteableBitmap upload fix with `dotnet build src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal` and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~WriteableBitmapTests`; 3 focused tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Added ProGPU submodule commit `ae133cf`: `ProGPU.Scene.Compositor` now compiles geometry clip masks and opacity mask brush passes with `_activeOpacity` temporarily reset to `1.0`, then restores the caller's opacity stack before content drawing resumes. This keeps WPF-style `PushOpacity(...); PushGeometryClip(...)` from applying opacity once to the mask texture and a second time to the clipped content.
- Added headless compositor coverage comparing clipped and unclipped rectangles under the same `PushOpacity(0.5)` scope, proving geometry mask compilation no longer dims the clipped draw.
- Verified the native mask-opacity fix with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~CompositorClipTests`; 2 focused tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Added ProGPU submodule commit `265f31d`: `ImageEffectExtensionPipeline` now uses explicit compositor-compatible pipeline layouts, reuses the compositor vector uniform bind groups, releases its cached bind groups/layouts on disposal, and binds `ImageEffectParams.MaskTexture` ahead of the active compositor mask when rendering native image effects.
- Exposed compositor mask bind-group layouts internally so native effect extensions can share the same mask bind groups instead of relying on auto-layout compatibility or managed-side mask workarounds.
- Added headless image-effect coverage proving an explicit black mask suppresses a red image-effect quad while the same effect renders unmasked when no explicit mask is supplied.
- Verified the native image-effect mask fix with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~ImageEffectRenderTests`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~WpfShaderEffectRenderTests`; 1 image-effect test and 4 WPF shader-effect tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Verified the WPF bridge against ProGPU submodule head `265f31d` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. A parallel bridge/test build emitted a transient MSBuild copy retry warning but completed successfully.
- Added ProGPU submodule commit `33abd3b`: Skia `SKPath.Bounds` now uses `ProGPU.Vector.PathGeometry.TryGetBounds(...)`, so native arc extrema contribute to Skia path/region bounds instead of only endpoint coordinates.
- Mapped Skia `SKPath.FillType` and GDI `GraphicsPath.FillMode` onto ProGPU `PathGeometry.FillRule`, preserving even-odd/alternate fill semantics when `SKCanvas.DrawPath(...)` and `System.Drawing.Graphics.FillPath(...)` hand paths to the native renderer.
- Split full-circle `System.Drawing.Drawing2D.GraphicsPath.AddArc(...)` sweeps into two native semicircle arcs, avoiding coincident-start/end degenerate arcs while keeping full ellipses on the ProGPU arc path.
- Verified the path shim fixes with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~PathShimCompatibilityTests`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`, and `dotnet build src/System.Drawing.Common/System.Drawing.Common.csproj --no-restore --verbosity minimal`; 4 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `33abd3b` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. A parallel bridge/test build hit the known transient deps-file lock and was rerun sequentially.
- Added ProGPU submodule commit `919f844`: `WpfShaderEffectExtensionPipeline` now applies active compositor masks to native WPF shader-effect draws, binding the shared mask texture/sampler path used by other native draw pipelines.
- Specialized WPF shader-effect WGSL and source bind-group layouts per active sampler-register set instead of declaring all 16 WPF sampler registers for every effect. This keeps common effects under WebGPU's 16 sampled-texture/sampler fragment-stage limit even when an active mask is bound, while preserving WPF register identity in `wpf_sample_register(...)` and `wpf_sample_source(...)`.
- Added a clear backend failure for the rare case where an effect uses all 16 WPF sampler registers under an active mask on a device that cannot bind an additional mask sampler; the follow-up is a ProGPU device-limit/descriptor strategy rather than a managed WPF-side shader workaround.
- Added headless coverage proving a red native WPF shader-effect quad is suppressed by a black `PushOpacityMask(...)` scope while an unmasked quad still renders.
- Verified the native WPF shader-effect mask/layout slice with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~WpfShaderEffectRenderTests`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectParamsTests|FullyQualifiedName~ImageEffectRenderTests"` from the ProGPU submodule; 5 WPF shader-effect tests and 7 adjacent focused tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Verified the WPF bridge against ProGPU submodule head `919f844` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise and one transient copy retry that completed successfully.
- Added ProGPU submodule commit `fd16a92`: static DXF cached/replay draw calls now preserve active compositor opacity-mask and blend state for both the public `DrawingContext.DrawStaticDxf(...)` extension route and direct `RenderCommandType.DrawStaticDxf` route.
- Added default compositor-managed screen-space uniforms for `DxfStaticBuffer` draws that have not supplied an explicit pan/zoom viewport, keeping WPF-style cached drawing replay usable without DXF-viewer-specific `UpdateViewport(...)` calls while preserving the existing explicit viewport path.
- Added `DrawGlyphRun` handling to both static DXF compile overloads so cached drawing buffers keep WPF glyph-run commands on the native ProGPU text path.
- Verified the static DXF slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~StaticDxfRenderTests`, and `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`; 2 focused tests passed and the scene build reported 0 warnings and 0 errors.
- Verified the WPF bridge against ProGPU submodule head `fd16a92` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this static DXF slice.
- Added ProGPU submodule commit `667db31`: cached/effected visual layers now render the root subtree into the offscreen texture in local padded space, then composite the texture through `Visual.GetLocalTransform() * parentTransform`, so WPF-style `Effect` and `CacheAsLayer` visuals preserve scale, rotation, transform, and transform-origin semantics instead of drawing cached textures as untransformed offset rectangles.
- Verified the cached layer transform slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~LayerRenderTests|FullyQualifiedName~WpfShaderEffectRenderTests"`, and `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`; 6 focused render tests passed and the scene build reported 0 warnings and 0 errors.
- Verified the WPF bridge against ProGPU submodule head `667db31` with `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. An earlier concurrent WPF/test build attempt hit the known transient `GenerateDepsFile` deps-file lock and was rerun sequentially successfully.
- Added ProGPU submodule commit `3bb9952`: Skia `SKPath.Transform(...)` now preserves native `ArcSegment` records by delegating arc affine transforms to `ProGPU.Vector.ArcSegmentGeometry.TryTransformArcSegment(...)`, including non-uniform scale and orientation-aware sweep metadata, and only falls back to a line for degenerate transforms the vector layer rejects.
- Fixed Skia `SKPath.AddPath(...)` to deep-copy mutable native path segments instead of sharing segment instances across paths. The offset overload now uses the same clone helper and preserves smooth-join/stroked metadata, preventing copied paths from mutating the source path during later transforms.
- Verified the native Skia arc-transform slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~PathShimCompatibilityTests`, and `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`; 6 focused tests passed and the affected shim build reported 0 warnings and 0 errors.
- Verified the WPF bridge against ProGPU submodule head `3bb9952` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this path shim slice.
- Added ProGPU submodule commit `a4f9bd1`: Skia `SKPath.AddRoundRect(..., CounterClockwise)` now starts at the top-left left tangent and walks the rounded rectangle perimeter with four line spans and four counter-clockwise native arc spans, eliminating the previous doubled-back diagonal edges through the shape interior.
- Verified the counter-clockwise rounded-rectangle fix with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter FullyQualifiedName~PathShimCompatibilityTests`, and `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`; 7 focused tests passed and the affected shim build reported 0 warnings and 0 errors.
- Verified the WPF bridge against ProGPU submodule head `a4f9bd1` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this path shim slice.
- Added ProGPU submodule commit `f0a86ff`: Skia `SKCanvas.SaveLayer(...)` now returns the pre-save restore count and pushes only the current layer's opacity to the compositor, keeping nested translucent layers from multiplying parent opacity twice while preserving cumulative opacity as canvas-local state.
- Skia `SKCanvas.Translate(...)` now applies translations through the full current 2x2 linear matrix, and `ClipRect`/`ClipPath` now record the current canvas matrix on native clip commands. `ProGPU.Scene.Compositor` now uses the command-active transform when replaying `PushClip`, so transformed Skia rectangle clips reach the native scissor path instead of clipping in untransformed device space.
- Verified the canvas-state slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SkCanvasStateTests|FullyQualifiedName~CompositorClipTests"`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`, and `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`; 6 focused tests passed and both affected builds reported 0 warnings and 0 errors.
- Verified the WPF bridge against ProGPU submodule head `f0a86ff` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 149 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this Skia state slice.
- Added ProGPU submodule commit `84cb2c2`: Skia `SKCanvas.Clear(...)` and System.Drawing `Graphics.Clear(...)` now scope their full-target clear rectangles with native `GpuBlendMode.Src`, so transparent and partial-alpha clears replace destination pixels instead of blending through SrcOver. This addresses the ProGPU PR #17 clear-source review threads in the backend/shims rather than adding WPF-side pixel workarounds.
- Added focused ProGPU coverage for `SKCanvas.Clear(...)` and `Graphics.Clear(...)` command emission, plus a headless-test project reference fix that excludes the transitive Microsoft `System.Drawing.Common` package assets so signed ProGPU drawing-shim tests bind to the local shim assembly consistently.
- Verified the ProGPU clear-source slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SkCanvasStateTests"`, `dotnet test src/ProGPU.Tests.Headless/ProGPU.Tests.Headless.csproj --verbosity minimal --filter "FullyQualifiedName~GdiShimTests"`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`, and `dotnet build src/System.Drawing.Common/System.Drawing.Common.csproj --no-restore --verbosity minimal`; 6 Skia canvas tests and 14 GDI shim tests passed.
- Verified the WPF bridge against ProGPU submodule head `84cb2c2` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this clear-source slice.
- Added ProGPU submodule commit `ef287df`: ShaderToy headless test debug and screenshot artifacts now write below the test output directory's `artifacts` folder instead of `/Users/wieslawsoltes/GitHub/ProGPU`, addressing the ProGPU PR #17 portable-path review threads without changing WPF runtime code.
- Verified the ProGPU artifact-path fix with `git diff --check`, `dotnet build src/ProGPU.Tests.Headless/ProGPU.Tests.Headless.csproj --no-restore --verbosity minimal -m:1`, and a focused ShaderToy test filter covering presets 2-5 plus terrain and seascape; 6 focused tests passed and produced artifacts under `src/ProGPU.Tests.Headless/bin/Debug/net10.0/artifacts`.
- Added ProGPU submodule commit `9322988`: `ShaderToyExtensionPipeline` now binds the compositor active-mask texture/sampler as a native WGSL bind group for onscreen and offscreen ShaderToy draws, multiplies ShaderToy output by active opacity and mask alpha, keeps WebGPU pipeline error handling active through `WaitIdle()`, and removes hard-coded renderer debug file writes.
- Added focused ProGPU render coverage proving a black `PushOpacityMask(...)` suppresses a red ShaderToy quad while an unmasked ShaderToy quad still renders through the native shader pipeline.
- Verified the native ShaderToy mask/error-handler slice with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ShaderToyRenderTests"`, and the existing headless ShaderToy preset/transpiler test filter; 1 ShaderToy render test and 6 headless ShaderToy tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Added ProGPU submodule commit `c428b4e`: `ImageEffectExtensionPipeline` now clips axis-aligned image-effect quads by intersecting the geometry before vertex emission and interpolating UVs from the original source quad, so clipped effect content samples the correct texture region instead of stretching the full source over the clipped area.
- Added focused ProGPU render coverage with a red/green source texture proving a clipped image-effect quad samples the right-half green texels after clipping.
- Verified the native image-effect clipped-UV slice with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ImageEffectRenderTests"`; 2 focused image-effect tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Added ProGPU submodule commit `c64d2bf`: WPF shim `DrawingContext.DrawRoundedRectangle(...)` now carries both `radiusX` and `radiusY` into native `RenderCommand` records, and the compositor routes elliptical-radius rounded rectangles through `PrimitivePathGeometry.CreateRoundedRectangle(...)` so ProGPU native arc/path rendering preserves WPF elliptical corners without a WPF bridge workaround.
- Updated the Skia shim so uniform `SKRoundRect` values record both X/Y radii and per-corner or otherwise non-uniform radii lower to native `SKPath.AddRoundRect(...)` path geometry, preserving asymmetric Skia rounded rectangles through ProGPU arc segments.
- Addressed the ProGPU PR #17 rounded-rectangle review comments for WPF and Skia with inline replies referencing `c64d2bf`.
- Verified the rounded-rectangle slice with `git diff --check`, `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~DrawingContextRoundedRectangleTests|FullyQualifiedName~SkCanvasStateTests"`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`, and `dotnet build src/PresentationCore/PresentationCore.csproj --no-restore --verbosity minimal`; 10 focused ProGPU tests passed.
- Verified the WPF bridge against ProGPU submodule head `c64d2bf` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this rounded-rectangle slice.
- Added ProGPU submodule commit `cbcd62d`: native shader/effect quad compilation now shares `QuadClipper.TryClipAxisAlignedQuad(...)`, preserving original UV and ShaderToy `fragCoord` mappings when axis-aligned parent clips trim WPF shader effects, ShaderToy draws, and image effects.
- Changed WPF shader-effect and ShaderToy clipped rendering so non-axis-aligned quads are not vertex-clamped; they remain geometrically intact and rely on the compositor scissor, avoiding shader-coordinate compression for transformed draws.
- Addressed the ProGPU PR #17 WPF shader UV and ShaderToy coordinate review comments with inline replies referencing `cbcd62d`.
- Verified the native shader-coordinate slice with `git diff --check`, `dotnet build src/ProGPU.Scene/ProGPU.Scene.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~WpfShaderEffectRenderTests|FullyQualifiedName~ShaderToyRenderTests|FullyQualifiedName~ImageEffectRenderTests"`; 10 focused ProGPU render tests passed. Existing warning noise remains in unrelated ProGPU projects.
- Verified the WPF bridge against ProGPU submodule head `cbcd62d` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this shader-coordinate slice.
- Added ProGPU submodule commit `17aa1d9`: `ProGPU.Text.TtfFont` now accepts a TTC face index, validates collection bounds, and exposes the selected `FaceIndex`, so collection members no longer implicitly load face 0.
- Updated the WPF `PresentationCore`, System.Drawing, and Skia font lookup/cache paths to carry `FontInfo.FaceIndex` with `FilePath` and cache `TtfFont` instances by the combined path/face key.
- Addressed the ProGPU PR #17 TTC face-index review comments for WPF `Typeface` and GDI `FontFamily` with inline replies referencing `17aa1d9`.
- Verified the TTC face-index slice with `git diff --check`, clean sequential builds for `ProGPU.Text`, `SkiaSharp`, `PresentationCore`, and `System.Drawing.Common`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SfntFontFaceTests"`; 5 focused text tests passed.
- Verified the WPF bridge against ProGPU submodule head `17aa1d9` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The `ProGPU.Wpf` build still reports existing ProGPU submodule warning noise outside this text slice.
- Added ProGPU submodule commit `870dc92`: Skia `SKCanvas.ClipRect(..., Difference)` and non-inverse `ClipPath(..., Difference)` now record native combined geometry masks for canvas bounds minus the transformed clip operand, while inverse path clips map to the corresponding native difference/intersection behavior.
- Updated Skia inverse path filling so `InverseWinding` and `InverseEvenOdd` draw as native canvas-bounds-minus-path combined geometry, with strokes still emitted against the original path for `StrokeAndFill`.
- Updated `ProGPU.Vector.PathAtlas.CompilePath(...)` so combined path geometry compiles through the existing WebGPU path-op solver before atlas rasterization. This keeps Skia difference clips, inverse fills, and WPF combined-geometry masks on the native path-op/shader path instead of requiring WPF bridge-side clipping workarounds.
- Addressed the ProGPU PR #17 Skia difference-clip and `SKPath.FillType` review comments with inline replies referencing `870dc92`.
- Verified the native Skia difference/inverse-fill slice with `git diff --check`, `dotnet build src/ProGPU.Vector/ProGPU.Vector.csproj --no-restore --verbosity minimal`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~CompositorClipTests|FullyQualifiedName~SkCanvasStateTests|FullyQualifiedName~PathShimCompatibilityTests|FullyQualifiedName~ArcPathCompilerTests"`; 36 focused tests passed, including a headless readback test proving a native combined difference clip cuts a mask hole.
- Verified the WPF bridge against ProGPU submodule head `870dc92` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The `ProGPU.Wpf.Tests` build emitted a transient SkiaSharp copy retry during parallel project output copying but completed successfully.
- Added ProGPU submodule commit `de9a7c5`: System.Drawing `Graphics.DrawString(...)` and `MeasureString(...)` now convert `Font.Unit` into the pixel size consumed by ProGPU text layout, so default point-sized GDI fonts render and measure at DPI-correct pixel sizes instead of being treated as raw pixels.
- Point, inch, document, and millimeter font units now scale through `DpiY`; pixel-like units keep their previous raw-size behavior until a broader page-unit/page-scale model is introduced.
- Addressed the ProGPU PR #17 GDI font-unit review comment with an inline reply referencing `de9a7c5`.
- Verified the ProGPU text-unit slice with `git diff --check`, `dotnet build src/System.Drawing.Common/System.Drawing.Common.csproj --no-restore --verbosity minimal`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~GdiTextUnitTests"`; 7 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `de9a7c5` with `git diff --check`, `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. The first `--no-build` attempt with the WPF-local preview runtime failed before execution because that local runtime only contains .NET 11, so the net10 testhost was rerun with the user-level net10 runtime.
- Added ProGPU submodule commit `444f97b`: `GpuTexture` now carries explicit straight versus premultiplied alpha metadata, and ProGPU texture pipeline keys include that mode instead of inferring premultiplied blending from names containing `Texture`.
- Updated native `SrcOver` blend creation so straight-alpha CPU/image uploads use `SrcAlpha` for color blending, while premultiplied render targets, WPF PBgra uploads, cached layers, and effect textures keep the existing `One` source factor.
- Marked Skia/GDI/WinUI decoded RGBA uploads as straight alpha and compositor-rendered offscreen targets as premultiplied, keeping WPF image/effect/layer composition native without WPF bridge-side shader or pixel rewrites.
- Addressed the ProGPU PR #17 straight-alpha texture blending review comment with an inline reply referencing `444f97b`.
- Verified the ProGPU texture-alpha slice with `git diff --check`, sequential clean builds for `ProGPU.Backend`, `SkiaSharp`, and `System.Drawing.Common`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~RenderPipelineCacheTests|FullyQualifiedName~SkImageBitmapTests|FullyQualifiedName~TextureBlendRenderTests"`; 10 focused tests passed, including a headless pixel readback proving straight-alpha texture RGB is scaled by source alpha.
- Verified the WPF bridge against ProGPU submodule head `444f97b` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this texture-alpha slice.
- Added ProGPU submodule commit `d1ccbef`: Skia `SKTextBlobBuilder` now accumulates all positioned runs, snapshots each run into `SKTextBlobRun`, and clears the builder after `Build()` so earlier mixed-font or fallback glyph runs are not overwritten by the final run.
- Updated `SKCanvas.DrawTextBlob(...)` to emit every stored text-blob run through ProGPU's native glyph-run path with that run's own font and positioned glyph data, keeping WPF/Skia multi-run text replay native instead of adding WPF bridge-side splitting workarounds.
- Addressed the ProGPU PR #17 multi-run text blob review comment with an inline reply referencing `d1ccbef`.
- Verified the ProGPU text-blob slice with `git diff --check`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SkTextBlobTests|FullyQualifiedName~SkCanvasStateTests"`; 14 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `d1ccbef` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this text-blob slice.
- Added ProGPU submodule commit `73d99ab`: Skia `GRBackendRenderTarget` now supports borrowed ProGPU `GpuTexture` targets, and `SKSurface.Create(GRContext, GRBackendRenderTarget, ...)` wraps the supplied ProGPU texture instead of allocating a private render target that the caller cannot observe.
- Unsupported GL/Vulkan/Metal backend handles now fail explicitly in the WebGPU-backed Skia shim, addressing the ProGPU PR #17 render-target review thread without hiding interop failure behind a wrong-resource draw.
- Verified the ProGPU backend-render-target slice with `git diff --check`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SkSurfaceBackendRenderTargetTests"`; 2 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `73d99ab` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this Skia surface slice.
- Added ProGPU submodule commit `1b6d866`: the ShaderToy transpiler now rewrites bare early `return;` statements inside generated value-returning `mainImage(...)` functions to `return <fragColor>;`, using the resolved output parameter name.
- Addressed the ProGPU PR #17 ShaderToy early-return review comment in the transpiler/native shader path rather than adding managed shader fallback logic.
- Verified the ProGPU ShaderToy transpiler slice with `git diff --check`, `dotnet build src/ProGPU.Transpiler/ProGPU.Transpiler.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests.Headless/ProGPU.Tests.Headless.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~ShaderToyTranspilerTests"`; 1 focused test passed.
- Verified the WPF bridge against ProGPU submodule head `1b6d866` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this ShaderToy transpiler slice.
- Added ProGPU submodule commit `d4f2a3a`: Avalonia `ProGpuHostControl` now acquires shared `WgpuContext` leases with per-context reference counts, and host-created contexts are disposed only after the last borrower releases its lease.
- Moved context/compositor ownership out of `ProGpuCustomVisualHandler` disposal so closing the original host cannot destroy a shared WebGPU device while another host control still renders through it.
- Addressed the duplicate ProGPU PR #17 shared-context review comments with inline replies referencing `d4f2a3a`.
- Verified the ProGPU Avalonia host lifetime slice with `git diff --check` and `dotnet build src/ProGPU.Avalonia/ProGPU.Avalonia.csproj --no-restore --verbosity minimal -m:1`; the build succeeded with existing warning noise outside this lifetime change.
- Verified the WPF bridge against ProGPU submodule head `d4f2a3a` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this host-lifetime slice.
- Added ProGPU submodule commit `e6c17f6`: Avalonia `ProGpuHostControl` now builds host rounded clips as per-corner `StreamGeometry` paths and scales over-constrained radii proportionally, preserving asymmetric `CornerRadius` semantics instead of applying the maximum radius to every corner.
- Addressed the ProGPU PR #17 per-corner host clip review comment with an inline reply referencing `e6c17f6`.
- Verified the ProGPU host-clip slice with `git diff --check` and `dotnet build src/ProGPU.Avalonia/ProGPU.Avalonia.csproj --no-restore --verbosity minimal -m:1`; the build succeeded with existing warning noise outside this clip change.
- Verified the WPF bridge against ProGPU submodule head `e6c17f6` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this host-clip slice.
- Added ProGPU submodule commit `168e74a`: Skia `SKSurface` now tracks whether the backing texture already contains valid pixels and asks `RenderOffscreen(...)` to load existing contents after the first GPU flush, or immediately for borrowed/CPU-backed textures, so repeated `Flush()` calls preserve earlier draw batches.
- Added focused ProGPU readback coverage proving a red draw flushed before a later blue draw remains visible after the second `SKSurface.Flush()`.
- Addressed the ProGPU PR #17 SKSurface repeated-flush review comment with an inline reply referencing `168e74a`, and replied to the remaining no-reply duplicate review threads for already-fixed arc bounds/transforms, WPF geometry clips, installed row stride, path deep-copy, full-circle `AddArc`, and `SaveLayer` restore-count behavior.
- Verified the ProGPU surface-flush slice with `git diff --check`, `dotnet build src/SkiaSharp/SkiaSharp.csproj --no-restore --verbosity minimal -m:1`, and `dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj --no-restore --verbosity minimal --filter "FullyQualifiedName~SkSurfaceBackendRenderTargetTests"`; 3 focused tests passed.
- Verified the WPF bridge against ProGPU submodule head `168e74a` with `git diff --check`, sequential `dotnet build src/ProGPU.Wpf/ProGPU.Wpf.csproj --no-restore --verbosity minimal -m:1`, sequential `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReplayToProGpuCommandTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests|FullyQualifiedName~WpfReflectionResourceResolverTests"` from `/tmp`; 172 focused tests passed. Existing ProGPU warning noise remains outside this surface-flush slice.
- Lowered reflected WPF visual and drawing-group `ClearTypeHint=Enabled` to scoped native ProGPU `TextRenderingMode.ClearType` when no explicit `TextRenderingMode` is already present, preserving explicit text-rendering-mode precedence and removing `ClearTypeHint` from unsupported render-option accounting.
- Added focused WPF replay coverage for drawing-group `ClearTypeHint` and visual `ClearTypeHint` lowering to `PushTextRenderingMode(ClearType)`, while the mixed render-options test now proves explicit `TextRenderingMode=Aliased` still wins over the hint.
- Verified the WPF ClearTypeHint slice with `git diff --check`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~WpfReflectionResourceResolverTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests"` from `/tmp`; 103 focused tests passed, and the standard bridge replay/reflection filter passed 173 focused tests.
- Added `IWpfVisualCacheCommandSink` and implemented it in `ProGpuRetainedCompositionCommandSink` so reflected WPF visual `CacheMode` can create a bounded retained `ProGpuRetainedDrawingVisual` with `CacheAsLayer=true`, drawing replay shifted into local cache bounds, and native ProGPU layer caching handling the actual offscreen texture path.
- Updated `WpfVisualTreeReflectionRenderer` to lower non-null visual `CacheMode` through that native cache capability when available, while non-retained/flat sinks still count the state as unsupported. Added retained-frame and visual-replay tests proving native cache visual creation, cache bounds propagation, and unchanged unsupported accounting for sinks that cannot cache.
- Verified the WPF visual cache-mode slice with `git diff --check`, `dotnet build src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-restore --verbosity minimal -m:1`, and `/Users/wieslawsoltes/.dotnet/dotnet test /Users/wieslawsoltes/GitHub/wpf/src/ProGPU.Wpf.Tests/ProGPU.Wpf.Tests.csproj --no-build --verbosity minimal --filter "FullyQualifiedName~ProGpuWpfDrawingFrameTests|FullyQualifiedName~WpfVisualTreeReflectionRendererTests"` from `/tmp`; 31 focused tests passed. Launching the same `dotnet test` command from the WPF repo root remains blocked by the repo `global.json` Microsoft.Testing.Platform policy for this VSTest-based xUnit project.

## Open Porting Items

- Replace the source-shared portable `MS.Internal.Text.TextInterface` one-scalar-to-one-glyph fallback, remaining compact/CFF subsetting gaps, and LineServices dependencies with a full ProGPU-backed or cross-platform shaping/subsetting abstraction.
- Wire `WpfRenderDataReflectionBridge` into real WPF visual rendering paths or replace it with an in-assembly bridge once the port lane compiles managed WPF directly.
- Replace the reflection visual subtree renderer with in-assembly visual traversal when managed WPF compiles in the port lane.
- Expand adapters for cross-assembly real WPF visual resources: remaining tile-brush cases including non-affine or unreadable transform types and advanced visual-brush state, platform codecs, color management and color-profile fidelity, formatted text, complex glyph/typeface identity beyond local font-file URI and bold/italic style-simulation replay, animation resources, and remaining non-emulatable drawing effects mapped to ProGPU-native shader descriptors.
- Add visual state support for visual opacity masks whose finite bounds are not exposed or inferable from replayable content, remaining generated `PushEffect` resource mapping for non-emulatable effects and non-context `BitmapEffectInput`, WPF `ShaderEffect` bytecode translation/import plus non-implicit sampler brush/texture inputs, non-axis-aligned/exact WPF guideline snapping fidelity, exact curve-join fidelity beyond the current endpoint tangent approximation, exact smooth-join rasterization fidelity, exact cap clipping at complex joins, backend-owned dashed/stroked path commands for combined-geometry boolean results so the WPF bridge no longer emits transition dash spans, drawing-group/non-visual cache mode scopes, text hinting, advanced 3D interop (`Viewport2DVisual3D`, hit testing, animations, textures/material fidelity), and retained invalidation coalescing beyond the first host-level frame-state gate and source-version polling, including dirty-region policy and unchanged native branch skips.
- Add custom `IWpfMilResourceResolver` adapters for real WPF resource instances until type identity is unified with the ProGPU shim.
- Port WPF `CompositionTarget`/`HwndTarget` to a Silk.NET-backed target.
- Replace `MilCoreApi` geometry helpers with managed/ProGPU vector implementations.
- Replace WIC imaging paths with ProGPU texture-backed bitmap sources and platform codecs; pixel-row conversion, compact PBgra32 row packing, WriteableBitmap Bgr32/PBgra32 upload conversion, and upload buffer/texture validation are now in ProGPU.Backend/shims, but codecs, color management, caching, download state, and lifetime remain open.
- Replace DWrite, DirectWriteForwarder subsetting, and LineServices dependencies with the ProGPU text path or a cross-platform shaping stack.
- Harden process-backed platform services or replace them with native per-OS implementations behind the same interfaces, and implement WPF input routing, IME/stylus handling, full routed drag/drop behavior over the drag/drop payload boundary, native event-loop wakeups, animation ticks, dirty-region scheduling, and full WPF Dispatcher/MediaContext integration.
