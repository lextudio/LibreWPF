# ProGPU Branch Plan for WPF Port Acceleration

## Reuse Policy

The ProGPU branch should add reusable native primitives and shim behavior only where they let the WPF port keep real managed WPF code on the active path. Rendering, text rasterization, shader effects, vector geometry, image upload, cache/lifetime, and backend resource fixes belong in ProGPU when the alternative would be a WPF-bridge workaround or managed-side pixel/vector emulation. XAML, controls, resources, styles/templates, routed events/commands, layout metadata, application lifetime, and theme dictionaries should stay in the WPF superproject and continue to use upstream managed WPF code.

## SDK Delivery Requirement

The deliverable shape is a custom `ProGPU.Wpf.Sdk` that existing WPF applications can select by changing only the project `Sdk`. The SDK must hide the portable WPF transport, ProGPU/Silk.NET package graph, runtime assets, and bootstrap registration behind MSBuild imports while preserving normal WPF `ApplicationDefinition`, `Page`, `Resource`, BAML, theme, code-behind, dependency-property, binding, command, layout, and control-manager behavior. Any required source or XAML edit in a normal app is a compatibility bug unless it is an explicitly documented Windows-only interop exception. ProGPU branch work should therefore favor native primitives and platform services that let this SDK contract keep upstream managed WPF code active, not managed bridge workarounds that customers would need to opt into. This is now a hard delivery criterion for the port: repo-local smoke apps, external package-mode harnesses, and CI/package validation should prove that app projects need no ProGPU-specific properties or source edits beyond the SDK selection.

## Current Branch State

The WPF superproject tracks ProGPU submodule branch `fix/render-invalidation-and-leaks`. The branch has been fast-forwarded to the latest origin commits and now includes:

- vector rendering fidelity work for WPF-style pens, caps, joins, arcs, dashed geometry, gradient brush packing, brush transforms, bitmap sampling modes, aliased vector rendering flags, native text rendering modes including ClearType, and related shader/storage-buffer changes.
- repo-level strong-name signing for ProGPU assemblies with public key token `c29c9752855ee183`.
- ProGPU-owned affine arc transformation in `ProGPU.Vector.ArcSegmentGeometry`, so WPF transformed path arcs can remain native ProGPU arc segments through translation, scale, rotation, shear, and orientation-reversing transforms.
- shader-native fixed-quad ProGPU arc stroke primitives in `ProGPU.Scene.Compositor`/`ProGPU.Backend.Shaders`, using reusable `ProGPU.Vector.ArcShaderParameters` created by `ArcSegmentGeometry.TryCreateShaderParameters(...)` to pass transformed ellipse axes and WPF/SVG sweep metadata into WGSL. The fragment shader now evaluates the arc stroke SDF directly, and the older segmented `ArcStrip` shader/vertex path has been removed, so normal WPF API arcs cannot regress to managed flattening or per-arc strip tessellation.
- exact ProGPU arc bounds for `PathAtlas` and path-op compilation, so WPF path fills and combined-geometry operations can keep native GPU arc segment records without atlas clipping from coarse sampled bounds.
- ProGPU-owned subarc and dashed-arc span construction for WPF/SVG arc spans, allowing the WPF bridge to request visible dash intervals as native `ArcSegment` commands that still render through the ProGPU arc shader instead of carrying bridge-local cubic/polyline fallback geometry.
- ProGPU-owned quadratic/cubic Bezier subdivision and dashed-span construction, allowing the WPF bridge to request visible dash intervals as native `QuadraticBezierSegment`/`CubicBezierSegment` commands that render through ProGPU's vector Bezier stroke shaders instead of bridge-local line approximation.
- ProGPU-owned primitive path construction for rectangles, rounded rectangles, and ellipses, with rounded corners and ellipse outlines represented as native arc segments so WPF dashed primitive outlines can reuse the arc dash/shader path instead of bridge-local polyline approximations.
- WPF and Skia rounded-rectangle commands now preserve separate X/Y radii in ProGPU command records, and asymmetric or per-corner cases route through native `PathGeometry` rounded-rectangle arcs. This keeps WPF elliptical rounded corners and Skia `SKRoundRect.SetRectRadii(...)` on the ProGPU vector/arc path instead of forcing bridge-side flattening.
- Unequal-radius rounded-rectangle path fallback now clears per-command transform metadata after visual playback has folded it into the active compositor transform, so transformed elliptical rounded rectangles are not double-transformed while still using the native path/arc fallback.
- reusable `ProGPU.Vector.DashPattern` for WPF-compatible dash-array normalization, odd-array duplication, dash-offset state, dot-dash promotion, and straight-line dash span construction shared by line, Bezier, and arc helpers.
- native arc preservation in the ProGPU WPF/WinUI path API shims, so parsed SVG/WPF `ArcSegment` records and transformed path arcs remain `ProGPU.Vector.ArcSegment` values that feed the existing vector arc shader instead of being replaced by endpoint line placeholders. The WPF `PresentationCore` path shim now preserves original path figures and carries `Geometry.Transform` as a native `DrawPath` command transform, letting ProGPU's path atlas and fixed-quad arc shader own transformed-arc evaluation instead of baking transformed points in managed shim code.
- native geometry-clip preservation in the ProGPU WPF `DrawingContext.PushClip(Geometry)` shim, so path-compatible WPF clips record `PushGeometryClip`/`PopGeometryClip` commands with composed geometry and drawing transforms instead of degrading non-rectangular clips to axis-aligned bounds rectangles.
- opacity-neutral native mask compilation in `ProGPU.Scene.Compositor`, so geometry clip masks and opacity mask brushes are rendered into mask textures without inheriting the current draw opacity. The draw content still receives active opacity once through the normal draw path, avoiding double opacity application for WPF `PushOpacity(...); PushClip(...)` and related mask scopes.
- four-corner transformed rectangular clip bounds in `ProGPU.Scene.Compositor`, so WPF visual clips, scrollable-area clips, and unsupported-geometry rectangular fallbacks use conservative native scissor bounds under rotation or shear instead of relying on a two-corner managed workaround.
- reusable `ProGPU.Vector.PathGeometry.CreateTransformed(...)` and shim-level WPF `CombinedGeometry`, so WPF combined path draws/clips can stay native ProGPU path-op geometry while child transforms preserve arc metadata in the vector layer instead of duplicating transform code in the WPF bridge.
- reusable `ProGPU.Vector.PathGeometry.TryGetBounds(...)` plus shim-level WPF `LineGeometry`, `RectangleGeometry`, `EllipseGeometry`, and `GeometryGroup`, so primitive WPF geometry clips/draws use native ProGPU path and arc records. WPF `PathFigure.IsFilled=false` figures are now excluded from fill commands while still participating in stroke commands, addressing the ProGPU PR #17 review feedback without a managed bridge-side workaround.
- native WPF-compatible path fill/stroke semantics in ProGPU: `ProGPU.Vector.PathGeometry.FillRule` is written into the GPU path-record ABI and evaluated by the path rasterizer and path-op shaders as `EvenOdd` or `Nonzero`; WPF and WinUI path shims plus WPF `GeometryGroup` forward their fill rules; and per-segment `IsStroked` is preserved from WPF `StreamGeometryContext` into ProGPU segments so the compositor can skip non-stroked stroke spans while keeping them in fill geometry.
- Skia and GDI-compatible path shims now reuse those native path semantics: `SKPath.FillType` and `GraphicsPath.FillMode` map onto `PathGeometry.FillRule`, `SKPath.Bounds` uses arc-aware `PathGeometry.TryGetBounds(...)`, full-circle `GraphicsPath.AddArc(...)` sweeps split into native semicircle arcs instead of creating degenerate coincident-endpoint arcs, and `SKPath.Transform(...)` preserves native `ArcSegment` records through ProGPU's affine arc-transform helper instead of only moving arc endpoints. `SKPath.AddPath(...)` now deep-copies native segments so later transforms do not mutate source paths, counter-clockwise `SKPath.AddRoundRect(...)` walks the perimeter with native corner arcs instead of crossing through the rounded rectangle interior, `SKRoundRect.SetRect(...)` clears stale per-corner radii, and `SKCanvas` now preserves Skia-compatible save-layer restore/opacity semantics plus transformed rectangle/path clip commands that replay through ProGPU's native clip pipeline. Skia `Difference` clips and inverse path fill types now lower to native combined path geometry, and `PathAtlas` compiles those combined geometries through the WebGPU path-op solver before rasterization so WPF/Skia hole clips stay on the backend path-op path. Combined atlas entries now allocate from true logical combined-geometry bounds and apply atlas padding exactly once, avoiding the double-padding offset that could clip or shift WPF `CombinedGeometry` and Skia path-op masks.
- Skia-compatible bitmap/image shims now preserve installed bitmap row stride, reject CPU-backed `SKSurface` row strides that cannot hold one full pixel row, copy only active pixels instead of padding, normalize BGRA bitmap rows before RGBA texture upload, clip negative or partially out-of-bounds `SKImage.ReadPixels(...)` source rectangles into the destination, convert `ReadPixels(...)` between premultiplied and unpremultiplied alpha when the requested destination alpha type requires it, populate `SKImage.ScalePixels(...)` destinations with stride-aware scaled pixels, distinguish owned images created from bitmaps/surface snapshots from borrowed texture wrappers for deterministic texture disposal, decode `SKCodec` encoded image data into requested RGBA/BGRA bitmap layouts, carry straight-alpha upload metadata for unpremultiplied decoded RGBA sources, and honor `SKPaint.Color.A` on `SKCanvas.DrawImage(...)` through native ProGPU opacity scopes instead of CPU-side image rewriting.
- Skia `GRBackendRenderTarget` can now carry a borrowed ProGPU `GpuTexture`, and `SKSurface.Create(GRContext, GRBackendRenderTarget, ...)` wraps that texture instead of allocating a private target. Unsupported GL/Vulkan/Metal backend handles fail explicitly in the WebGPU shim, preventing WPF/Skia interop from rendering into the wrong resource.
- Skia `SKSurface` now preserves backing texture contents across repeated `Flush()` calls by loading existing contents after the first GPU flush and immediately for borrowed or CPU-backed textures. This keeps Skia/WPF interop surfaces persistent between incremental draw batches instead of requiring WPF-side full-surface replay workarounds.
- Skia paint/image compatibility now stays in the ProGPU shim/backend path: `SKCanvas` wraps paint-based draw calls and save-layer restore in supported native `GpuBlendMode` scopes, `SaveLayer(...)` records into a child drawing context and composites the flattened layer texture with paint opacity/blend state only when the layer is restored, and `SKImage.Encode(...)` unpremultiplies premultiplied GPU readback before PNG/JPEG encoding. This keeps WPF/Skia interop from adding managed-side blend or pixel-convention workarounds.
- Skia save-layer bounds and region semantics now stay in the ProGPU shim/backend path: `SKCanvas.SaveLayer(bounds, paint)` clips both offscreen layer rendering and restored parent-layer composition to the requested layer bounds, while `SKRegion.SetPath(...)` accepts single rectangular paths and fails closed for unsupported non-rectangular paths instead of converting them to misleading bounding-box regions.
- Skia context-keyed compositor caches are now lifetime-bound to `WgpuContext.Disposing` in both `SKSurface` and `SKCanvas`, so WPF/Skia interop surfaces do not retain disposed contexts through static shim caches.
- System.Drawing-compatible bitmap shims now validate `Bitmap.LockBits(...)` rectangles before GPU readback and unsafe row-pointer copies, reject non-positive, negative-origin, and overhanging regions instead of reading outside the bitmap buffer, preserve `ImageLockMode.ReadOnly` by skipping unlock writeback, avoid flushing recorded GPU draws from bitmap finalizers, preserve existing texture pixels when flushing recorded draws over already-defined bitmap contents, unpremultiply premultiplied readback for `GetPixel(...)` and PNG save, and preserve/premultiply the existing texture alpha mode in `SetPixel(...)`. This addresses the ProGPU PR #17 bitmap lock/finalizer/flush/alpha review clusters in the backend.
- System.Drawing-compatible `Bitmap.LockBits(...)` now materializes requested 32bpp, 24bpp RGB, and RGB565 layouts with GDI-style aligned strides and converts them back on unlock, so WPF/GDI interop can depend on the shim contract rather than translating lock buffers in the WPF bridge.
- System.Drawing-compatible image draws now preserve the full active `Graphics` affine transform by recording texture commands with local destination rectangles plus command transforms, so WPF/GDI-shaped rotated and sheared image content can stay on the native texture path instead of relying on managed quad pre-transforms.
- System.Drawing-compatible `Graphics.DrawImage(...)` now snapshots source bitmap textures into command-owned GPU textures with `GpuTexture.CopyFrom(...)` before recording deferred bitmap-backed draw commands. `DrawingContext.Clear()` disposes those retained resources after `Bitmap.Flush()` consumes the command buffer, so WPF/GDI-style code can dispose source images before destination flushes without invalidating native texture draws.
- System.Drawing-compatible bitmap saves now convert premultiplied GPU readback from rendered GDI content back to straight RGBA before PNG encoding, so WPF/GDI interop does not need bridge-side alpha correction for semi-transparent rendered bitmaps.
- Native texture blending now uses explicit `GpuTextureAlphaMode` metadata. Straight-alpha CPU/image uploads use `SrcAlpha` color blending, while premultiplied render targets, WPF PBgra uploads, cached visual layers, and effect textures keep `One` source color blending. Generic `GpuTexture` construction defaults to straight-alpha upload semantics for existing `WritePixels(...)` callers, and render-target/backing allocations opt into premultiplied semantics explicitly. The native texture shader also carries source alpha-mode metadata so straight-alpha opacity masks scale coverage once while premultiplied texture masks scale RGB and alpha together. This keeps WPF image/effect composition in the backend without bridge-side premultiplication or mask-compensation guesses.
- Native `Screen`, `Multiply`, and `DstOver` blend modes now select premultiplied fragment entry points for vector, text, and straight-alpha texture draws before fixed-function blending runs. Already-premultiplied textures stay on the normal fragment entry point. This keeps semi-transparent WPF/Skia advanced blend behavior in the WebGPU shader/pipeline layer instead of adding managed WPF bridge premultiplication passes.
- Native image-effect color operations now run in straight color space for premultiplied sources, then premultiply output again in the ProGPU image-effect shader path. WPF/Skia effect callers therefore do not need managed-side unpremultiply/premultiply passes to match expected color behavior.
- Skia and System.Drawing-compatible clear operations now use native `GpuBlendMode.Src` around full-target clear rectangles, preserving source clear semantics for transparent and partial-alpha clears without managed WPF-side pixel rewriting. The headless test graph excludes transitive Microsoft `System.Drawing.Common` package assets so signed ProGPU drawing-shim tests consistently bind to the local shim assembly.
- Shim GPU texture allocation now prefers the thread's active `WgpuContext.Current` before falling back to the active-context list, so GDI+ and WPF shim images created during a render pass allocate on the current compositor device in multi-window or multi-surface scenarios. `SKCanvas.DrawImage(...)` now retains source images before pushing blend state and unwinds opacity in `finally`, keeping failed cross-context image retention from leaking recorded blend or opacity state.
- Retained `ProGPU.Scene.Visual.OpacityMask` and `OpacityMaskBounds` state is now part of the native scene model. The compositor applies that state through the same visual composite scope as clip and opacity for direct visuals, cached layers, and effect composites, so WPF visual masks can stay on a retained scene path without bridge-side inline descendant replay.
- bounded native path-op output writes in `PathOpGeometrySolver`: the output segment buffer is sized to the shader split capacity, the WGSL append path guards writes after the atomic reservation, and the finalizer clamps the record count. The solver also releases all temporary GPU readback resources from a `finally` path, including timeout and non-success `BufferMapAsync` exits. This addresses the ProGPU PR #17 path-op overflow and readback-cleanup review items and keeps WPF combined geometry reliable on the GPU boolean path without managed bridge lifetime workarounds.
- deferred native resource cleanup now releases queued bind-group layouts, pipeline layouts, render pipelines, compute pipelines, and shader modules, addressing the ProGPU PR #17 queued pipeline/shader leak review item in the backend rather than relying on compositor-side workarounds.
- reusable `ProGPU.Text.SfntFontFace` metadata/table/cmap/glyph-metric APIs plus `FontApi` updates for TTC face enumeration, localized name strings, and cross-platform system font directory discovery.
- `ProGPU.Text.TtfFont` now loads a requested TTC face index, and WPF/System.Drawing/Skia font caches carry `FontInfo.FaceIndex` with the font file path. This keeps macOS/Linux collection fonts and WPF fallback faces selectable without bridge-side font alias hacks.
- System.Drawing-compatible text draws and measurement now normalize `Font.Unit` values to ProGPU's pixel text layout size before issuing native text commands. Point, inch, document, and millimeter units scale by graphics DPI, so GDI/WPF-shaped default point fonts do not require WPF bridge-side size compensation.
- Skia `SKTextBlobBuilder` now preserves every positioned run in a text blob and `SKCanvas.DrawTextBlob(...)` emits each run with its own font and glyph positions. This keeps mixed-font and fallback text blobs on ProGPU's native glyph-run path instead of forcing WPF bridge-side text splitting.
- Native color-layer text batching appends actual glyph instances only and flushes text/vector batches before COLR/CPAL layer path output, so mixed color and non-color WPF/Skia glyph runs keep full native text/vector rendering without reserved-slot draw artifacts or managed bridge-side splitting.
- reusable `ProGPU.Text.SfntSimpleGlyphShaper` APIs for the current one-scalar-to-one-glyph portable fallback, including surrogate cluster maps, soft-hyphen/control handling, and metric-derived advance calculation.
- reusable `ProGPU.Text.SfntFontSubsetter` APIs for glyph-ID-preserving and compact glyph-remapping TrueType subset writing, including composite glyph dependency closure, composite component glyph-id rewriting, compact `glyf`/`loca`/`maxp`/`hhea`/`hmtx` rebuilding, `head` rewriting, checksum recalculation, explicit source-to-subset glyph maps, and stale glyph-indexed table removal.
- reusable `ProGPU.Backend.PixelDataConverter`, `PixelDataBuffer`, `Pbgra32PixelBuffer`, and `GpuTexture.WritePbgra32SubRect(...)` APIs for WPF/WIC-shaped bitmap row conversion, source-stride/byte-length validation, palette requirement validation, compact Pbgra32 row packing, and BGRA texture upload normalization, so image upload shape does not live only in the WPF bridge.
- reusable `ProGPU.Backend.DisplayScaleResolver` for ProGPU/Silk.NET host integrations. The WPF host now delegates display-scale resolution to ProGPU.Backend, preserving authoritative monitor scale values and using macOS Cocoa `backingScaleFactor` only as a fallback when Silk.NET/platform monitor discovery reports `1.0`. The resolver handles both `NSWindow` and `NSView` native handle shapes by probing direct `backingScaleFactor`, `screen`, `window.screen`, and then `NSScreen.mainScreen`, keeping Retina/HiDPI swapchain sizing in the backend/platform boundary and avoiding per-host AppKit P/Invoke code in WPF bridge logic.
- WPF shim `WriteableBitmap` format preservation and native upload conversion: `PixelFormats.Bgr32` and `PixelFormats.Pbgra32` now remain distinguishable, the bitmap exposes its stored `Format`, padded `WritePixels(...)` rows are validated through the backend stride helpers, and `Bgr32` writes convert to opaque PBgra32 before the BGRA texture upload path. This addresses the ProGPU PR #17 WriteableBitmap review items in the ProGPU branch instead of adding WPF bridge-side channel/stride workarounds.
- WPF and Skia shim compatibility now preserves WPF transform push order, default matrix identity semantics, and opaque destination readback alpha. `DrawingContext.PushTransform(...)` appends nested transforms to the current stack entry, `new Matrix()`/`default(Matrix)` mutators start from an identity basis, and `SKImage.ReadPixels(...)` clamps alpha to 255 for `SKAlphaType.Opaque` RGBA/BGRA outputs. This keeps cross-platform WPF/Skia API semantics inside ProGPU shims rather than managed WPF bridge workarounds.
- Skia `SKPaint` dash path effects now map into ProGPU vector pen dash metadata and route through the compositor's native line, Bezier, and arc dash lowering. Skia gradient tile modes map to ProGPU spread modes, while unsupported `Decal` semantics fail explicitly rather than rendering a misleading fallback.
- native `ProGPU.Scene` WPF shader-effect execution primitives: `WpfShaderEffectParams`, `DrawWpfShaderEffect(...)`, `WpfShaderEffect`, and `WpfShaderEffectExtensionPipeline` for WGSL/WebGPU effects with WPF-style constant registers, a fixed 16-slot logical sampler-register bank, configurable visual-source sampler register mapping for retained `Visual.Effect` usage, `wpf_sample_source(uv)` routing through the reflected source register, explicit compositor-compatible pipeline layouts, cached native bind groups/render pipelines, transparent fallback behavior for unbound logical samplers, compact per-effect WebGPU source layouts for active sampler registers, active compositor mask binding, source-alpha-mode-specific opacity/mask handling for straight versus premultiplied WPF shader sources, and headless render-path coverage.
- native `ProGPU.Scene` ShaderToy/WGSL extension rendering now carries active compositor masks and opacity through the shader path, binds the same mask texture/sampler resources used by other native draw pipelines, and keeps WebGPU pipeline validation error collection active through device polling. This gives WPF-facing shader experiments a full native masked path instead of requiring managed-side shader-output rewriting.
- native `ProGPU.Scene` ShaderToy/WGSL extension pipelines now key by active blend mode and output alpha convention, pass `GpuBlendMode`/`GpuTextureAlphaMode` into `RenderPipelineCache`, keep straight-alpha mask output from scaling RGB twice, and switch to premultiplied shader output for `DstOver`, `Multiply`, and `Screen`. This keeps WPF-compatible custom shader blending on the native WebGPU path instead of adding managed WPF-side shader workarounds.
- ShaderToy GLSL-to-WGSL generation now rewrites bare early `return;` statements inside value-returning generated `mainImage(...)` functions to return the current fragment color, preserving native shader execution for common ShaderToy control-flow patterns.
- ShaderToy GLSL-to-WGSL generation now preserves positive integer literal array declarations for top-level and local variables, emits fixed-size WGSL array types, and resolves array element access through the element type. Non-literal or non-positive array sizes fail explicitly instead of degrading to invalid scalar shader variables, keeping WPF-facing shader experiments on ProGPU's native shader path rather than managed source rewriting.
- ShaderToy GLSL-to-WGSL generation now expands single-scalar `mat2`/`mat3`/`mat4` constructors into explicit diagonal WGSL matrix components, preserving GLSL constructor semantics without managed WPF-side shader source rewriting.
- ShaderToy GLSL-to-WGSL generation now keeps scalar/vector builtin compatibility in the native transpiler path for `pow(...)` and emits type-correct standalone increment/decrement literals for `int`, `uint`, and `float` operands. The WPF port should rely on these ProGPU shader-language fixes rather than rewriting shader source or emulating increment/builtin behavior in managed code.
- native `ProGPU.Scene` image-effect, WPF shader-effect, and ShaderToy rendering now use shared clipped-quad UV interpolation for axis-aligned parent clips and leave non-axis-aligned quads to compositor scissor clipping. This addresses the ProGPU PR #17 clipped-UV/coordinate review items in the backend and keeps WPF effect masking/clipping on the full native WebGPU path without managed-side shader-coordinate rewriting.
- native image-effect and WPF shader-effect resource binding now respect source texture/sampler registers and source alpha conventions: direct WPF source textures bind at the requested logical register when no explicit sampler occupies it, WPF shader-effect primary source resolution uses the declared `SourceTextureRegisterIndex` before sampler register 0, and image-effect/WPF shader-effect pipelines and shader-module keys are keyed by straight versus premultiplied source alpha mode. Straight WPF shader effects keep straight RGB and scale only alpha/coverage under opacity and masks, while straight-alpha image-effect/WPF shader-effect output is converted to premultiplied output when `DstOver`, `Multiply`, or `Screen` require premultiplied source RGB. This keeps advanced WPF effect blending in the native shader/pipeline path without bridge-side shader rewrites.
- native WPF shader-effect mask binding now follows WebGPU device limits captured by `WgpuContext`: ProGPU requests and records fragment sampled-texture, sampler, and bind-group limits, keeps active masks bound for all-16-register shader effects when devices expose enough resources for the additional mask texture/sampler, and otherwise reports a transient per-draw backend diagnostic without marking the reusable shader-effect parameters failed. Removing the active mask lets the same WPF effect render again, keeping the capacity limit in the native shader path instead of forcing a managed fallback.
- non-axis-aligned rectangular clips now lower to native geometry masks instead of widened AABB scissors, while axis-aligned clips keep the fast scissor path. The same clip-scope state is saved and restored through popups, diagnostics, offscreen effects/layers, and static compilation contexts.
- WPF shim `StreamGeometry.Bounds` now uses the same transform-aware ProGPU path-bounds helper as `PathGeometry` and `CombinedGeometry`, so transformed geometry bounds are correct before rendering and can feed layout, clips, hit-testing, and cache sizing without stale draw-time mutation.
- native static DXF cached drawing replay now carries active compositor mask and blend state through both the extension path and direct `DrawStaticDxf` command path, selects vector/text pipelines for the active blend mode during buffer replay, updates default screen-space static-buffer uniforms automatically when no explicit DXF pan/zoom viewport was supplied, and compiles glyph-run commands into static text buffers. This addresses the ProGPU PR #17 static DXF mask/blend/glyph-run review items in the backend so WPF retained drawing caches do not need managed replay workarounds.
- SKCanvas deferred image draws and System.Drawing `TextureBrush` rectangle fills now snapshot their source textures into command-retained GPU textures, and static DXF spline replay selects vector pipelines through the active blend mode. This addresses the latest ProGPU PR #17 deferred texture lifetime and static spline blend review items in the backend/shims, so WPF retained caches and image/brush interop do not need managed-side lifetime or blend workarounds.
- Headless WPF retained theme replay now depends on ProGPU cleanup owning native lifetime order for shared cached resources. `WgpuContext.CleanupPendingResources()` snapshots distinct nonzero pending handles before release so duplicate queued pointers cannot hang or double-release during retained scene disposal, and cached texture/mask bind groups removed by texture disposal or cache eviction are queued through the same cleanup path and released before their texture views/textures. This keeps WPF retained image/effect sampler lifetime in the backend instead of adding managed bridge-side disposal workarounds.
- Chart line/scatter `GpuSeriesBuffer` cleanup now queues cached onscreen/offscreen bind groups through `WgpuContext.QueueBindGroupDisposal(...)` before clearing handles, addressing the latest ProGPU PR #17 chart lifetime review item in the backend so WPF-facing chart and retained-scene hosts do not need managed-side render-thread disposal workarounds.
- cached/effected visual layer composition now applies the visual's local transform during main-target compositing while keeping offscreen layer rendering in local padded space, refreshes cached textures when physical size changes, and uses ceiled physical texture dimensions plus ceiled logical offscreen extents for fractional visual size or DPI scale. This addresses the ProGPU PR #17 transform, DPI-refresh, and fractional-size review items for WPF `Effect`/`CacheAsLayer` visuals without requiring WPF-side texture-quad or rounding workarounds.
- reflected WPF visual `CacheMode` now lowers through `IWpfVisualCacheCommandSink` into retained ProGPU drawing visuals with `Visual.CacheAsLayer=true`, finite visual bounds, and local-bounds drawing transforms. This reuses the native cached-layer compositor path instead of adding managed bitmap-cache replay in the WPF bridge.
- reflected WPF `DrawingGroup.CacheMode` now lowers through `IWpfDrawingCacheCommandSink` into the same bounded retained ProGPU `CacheAsLayer` visual path, so non-visual drawing caches use native offscreen layer caching when the retained sink is active and only degrade to partial replay on flat sinks.
- retained/effected visual invalidation now treats `Visual.ClipBounds` as render-affecting state, restores compositor opacity correctly after zero-opacity scopes, and refreshes effect caches when effect property versions or mutable WPF shader constants/samplers change, addressing the latest ProGPU PR #17 cached-visual/effect invalidation review items in the backend.
- explicit render-target viewport mask sampling now stays in the ProGPU compositor: mask render targets remain full framebuffer-sized for onscreen explicit viewports, mask pass viewport/scissor state matches the content pass, and offscreen mask targets continue to use the offscreen texture dimensions. This addresses the ProGPU PR #17 viewport-offset mask review item without moving WPF root visuals or adding bridge-side coordinate compensation.
- `GpuTexture.Generation` is now treated as native content-version metadata for full writes, sub-rectangle writes including `WritePbgra32SubRect(...)`, and texture copies. WPF shader-effect and retained sampler caches can therefore key on texture identity plus generation for `WriteableBitmap`/PBgra uploads rather than requiring managed WPF replay workarounds or app-side invalidation.
- mutable `ProGPU.Scene.EffectBase` instances now track weak owning visuals and invalidate those owners on property changes, so visual effects, WPF shader padding, and cached/effected retained layers propagate dirty state through native scene parents without WPF bridge-side polling.
- native `ProGPU.Scene.TextRenderingMode` command state for `Grayscale`, `Aliased`, and `ClearType`, with `IsTextAliased` retained as a source-compatible property, native `DrawingContext` overloads that accept the mode, static DXF text record preservation, a text WGSL branch that renders ClearType through RGB subpixel atlas sampling, and WPF visual/drawing-group `ClearTypeHint=Enabled` lowering to scoped native ClearType when no explicit `TextRenderingMode` is present. This removes WPF bridge-side ClearType unsupported accounting without introducing managed pixel workarounds.
- native `ProGPU.Scene.TextHintingMode` command state for `Auto`, `Fixed`, and `Animated`, with text/glyph drawing overloads and static DXF compilation preserving the mode. The native compositor keeps snapped/subpixel placement for `Auto`/`Fixed` and preserves fractional transformed text positions for `Animated`, while WPF visual/drawing-group `TextHintingMode` lowers to scoped ProGPU command state instead of bridge-side compatibility code.
- retained `ProGPU.Scene.Visual.ChangeVersion` counters that advance on property invalidation, child invalidation propagation, and child collection changes without advancing during render-time dirty clearing. `ProGpuWpfCompositionTarget` exposes scene, retained-WPF, and flat-drawing version snapshots so WPF invalidation can map to native retained dirty tracking.
- Silk.NET host frame coalescing in `ProGpuWpfWindowHost` using `ProGpuWpfFrameState` snapshots of pixel size plus ProGPU scene, retained-WPF, flat-drawing `ChangeVersion` counters, and retained-branch targetability diagnostics to skip unchanged frames when no explicit draw callbacks are registered and no render request is pending.
- transition WPF source version polling in `WpfVisualInvalidationTracker`, reading reflected numeric `Version`, `ChangeVersion`, and `_version` values before frame-state coalescing so mutable source resources that miss events still dirty the retained ProGPU roots.
- dispatcher/timer-backed render scheduling in `DispatcherWpfRenderScheduler`, plus a guarded `ProGpuWpfWindowHost` scheduler wakeup bridge into Silk.NET `DoRender()`, giving the WPF host a portable MediaContext-style render-priority scheduling boundary before full dirty-region scheduling and animation clocks are ported.
- a real WPF `PortableMediaContextRenderService` and PresentationFramework dispatcher-flush hook registered by `WpfPortableWindowActivation`, so `MediaContext.PostRender()` and render-operation promotion can wake the Silk.NET-owned ProGPU host loop and drain WPF `DispatcherPriority.Render` operations before the host render pass without adding a ProGPU dependency to WPF managed assemblies.
- delay-aware `IWpfDelayedRenderScheduler` support and disconnected non-Windows `MediaContext` managed ticks, so WPF animation timing can carry `ScheduleNextRenderOp(...)` delays through the portable service, preserve follow-up ticks scheduled during the current render wakeup, and run `TimeManager`/render callbacks without DUCE channels before the ProGPU host renders the visual tree.
- queued dispatcher work-availability notifications through `IWpfDispatcherService.WorkAvailable`, with `ProGpuWpfWindowHost` draining owner-thread platform callbacks immediately and preserving background-thread safety until a native Silk.NET event-loop wake primitive is selected.
- Avalonia `ProGpuHostControl` now leases shared WebGPU contexts with per-context reference counts, and only disposes host-created contexts after the last borrower detaches. This keeps multi-host/multi-window WPF-style embedding from invalidating a live borrower when the original owner closes first.
- Avalonia `ProGpuHostControl` host clips now preserve per-corner `CornerRadius` values with a `StreamGeometry` clip, including proportional radius scaling for over-constrained sizes, instead of replacing asymmetric radii with a uniform maximum radius.
- non-mutating offscreen visual compilation for native effect/layer rendering, so `RenderOffscreen(...)` can render at padded texture origins without assigning `Visual.Offset` or advancing `ChangeVersion` during the render pass.
- ProGPU.Text source-sharing visibility so focused helpers remain public when built by `ProGPU.Text` but compile as internal when WPF links them under `PresentationCore`.
- a ProGPU-local `global.json` pinned to SDK `10.0.201` so the submodule builds consistently even when nested below WPF's .NET 11 preview SDK checkout.
- a signing regression test in `ProGPU.Tests`.
- System.Drawing-compatible stroke scaling under world transforms for GDI shim lines, rectangles, ellipses, and paths, keeping transformed pen widths in ProGPU command data instead of asking WPF bridge code to pre-expand stroked geometry.
- Skia-compatible opaque bitmap upload normalization in `SKImage.FromBitmap(...)`, forcing alpha bytes to 255 for `SKAlphaType.Opaque` inputs before the ProGPU texture upload.
- native two-point conical/focal gradient support through `TwoPointConicalGradientBrush`, ProGPU brush packing, and WGSL moving-circle parameter solves for the vector and hatch shader paths, so Skia `CreateTwoPointConicalGradient(...)` no longer needs to reject valid conical parameters.
- native bottom-left backend render-target origin handling for ProGPU-backed `SKSurface` wrappers by carrying `GRSurfaceOrigin` into the surface and applying a GPU-side root y-flip during flush.
- native backend render-target format matching for ProGPU-backed `SKSurface` wrappers by caching offscreen compositors per context and texture format, so BGRA backend targets use BGRA render pipelines instead of reusing RGBA pipelines from earlier surfaces.
- Skia `SKSurface.Snapshot()` texture-format preservation for ProGPU-backed surfaces, so snapshots of BGRA backend render targets keep BGRA texture metadata and avoid channel swaps or invalid texture-to-texture copies.
- render-thread `WgpuContext.Current` scoping in `Compositor.RenderScene(...)` and `RenderOffscreen(...)`, plus owner-context tracking in `GpuSeriesBuffer`, so GPU line/scatter uploads and bind-group cleanup remain valid when Avalonia or WPF host rendering runs on a composition thread different from the context initialization thread.
- explicit logical-to-physical compositor render-target coverage for HiDPI WPF hosts: `Compositor.RenderScene(...)` now has a regression that renders a 10x10 logical scene into a 20x20 physical target at scale 2 and verifies the lower-right physical pixel is covered, so SDK smoke windows cannot regress to upper-left quadrant rendering through target-size inference.
- explicit physical canvas metadata for HiDPI WPF hosts: `CurrentCanvasPixelWidth` and `CurrentCanvasPixelHeight` now prefer the active explicit render-target width/height, and a native extension-frame regression records those values while rendering a non-integer logical-to-physical target. Effects, masks, WPF shader metadata, and cached layer helpers should continue to consume these compositor-owned physical dimensions instead of recomputing framebuffer pixels in managed WPF bridge code.
- Skia `SaveLayer` texture lifetime cleanup after `SKSurface.Flush()`, releasing layer textures once the parent draw commands have been consumed instead of retaining one full-canvas texture per saved layer until canvas disposal.
- Skia `SaveLayer` image-filter lowering through native ProGPU paths: blur filters draw the isolated layer via the `ImageEffect` extension, and drop-shadow filters render the layer through a native `DropShadowEffect` offscreen pass before compositing.
- Skia `SaveLayer` image-filter replay now snapshots active rect/path clips when a layer is opened and replays those clips around offscreen source composition before native blur/drop-shadow filtering. This keeps filtered layer inputs clipped in ProGPU's shim/backend path, avoiding WPF bridge-side clipping or pre-rasterization workarounds for clipped effects.
- Image-effect extension teardown now queues bind groups, bind-group layouts, and pipeline layouts through `WgpuContext` disposal queues instead of directly releasing WebGPU handles from `Dispose()`. This keeps cleanup ordering in the backend and avoids asking WPF or Skia callers to know render-thread/device lifetime details.
- ShaderToy extension teardown now follows the same deferred WebGPU lifetime rule: per-frame bind groups, the ShaderToy bind-group layout, and onscreen/offscreen pipeline layouts are queued through `WgpuContext` disposal queues, while uniform buffers use queued `GpuBuffer.Dispose()`. WPF/ShaderToy shader surfaces should keep native GPU cleanup in ProGPU instead of introducing WPF bridge-side render-thread disposal workarounds.
- Skia `SKImage.ReadPixels(...)` destination stride validation for clipped source rectangles and shifted destination writes, preventing unsafe row overruns when callers provide too-small aligned strides.
- GDI `Graphics.DrawString(...)` font-style forwarding into ProGPU text command metadata, preserving synthetic bold and italic rendering for WPF/GDI compatibility shims.
- WPF shim `FormattedText` now uses WPF-compatible `System.Windows` font weight/style/stretch value types and forwards `Typeface` bold/slanted intent into ProGPU text command metadata, preserving synthetic style rendering for reused WPF-shaped managed text calls.
- WPF shim `Pen.DashStyle` now preserves dash arrays and offsets through `PresentationCore.Pen.ToNative(...)`, scales dash lengths by stroke thickness, rejects invalid dash arrays before native command creation, and keeps dash/cap/join/miter metadata in `ProGPU.Vector.Pen`. This lets the WPF bridge and SDK-switched shim callers preserve WPF pen intent in one native command instead of expanding supported dashes in managed replay code.
- ProGPU PR #17 follow-up fixes now keep WPF/Skia effect and readback fidelity in the backend: `DropShadowEffect` composites with logical padded dimensions while retaining physical backing textures, image-effect and WPF shader-effect mask uniforms use physical compositor canvas dimensions on HiDPI targets, skipped Skia save-layer restores clear retained layer contexts before returning, BGRA backend snapshots normalize CPU readback to RGBA for `ReadPixels`/`ScalePixels`/`Encode`, and top-level `RenderOffscreen(...)` scopes extension `BeginFrame`/`EndFrame` cleanup without double-resetting nested effect/layer subpasses.
- ProGPU PR #17 review cleanup now also hardens the reusable bitmap/text shim boundary needed by WPF: `System.Drawing.Bitmap` rejects non-positive dimensions at construction, and `SfntFontFace` falls back from a missing format 12 cmap hit to format 4 for BMP glyphs. This keeps malformed bitmap creation and mixed cmap font behavior inside ProGPU instead of adding WPF bridge validation or font-name-specific fallbacks.
- ProGPU PR #17 bitmap/surface dimension cleanup now rejects non-positive WPF shim `WriteableBitmap` sizes and invalid Skia `SKImageInfo` surface sizes before any `GpuTexture` allocation. Invalid layout or raster dimensions therefore stay as managed shim API errors instead of becoming huge wrapped WebGPU sizes or clamped backing textures that WPF bridge code would need to guard.
- ProGPU PR #17 shader-effect review cleanup now keeps WebGPU mask/sampler exhaustion transient: `WpfShaderEffectExtensionPipeline` records the all-16-register-plus-mask capacity diagnostic for the skipped draw without setting `WpfShaderEffectParams.IsFailed`, clears that diagnostic on the next renderable draw, and the regression test verifies the same effect parameters recover when the mask is removed.
- ProGPU PR #17 buffer-lifetime cleanup now routes explicit `GpuBuffer.Dispose()` through `WgpuContext.QueueBufferDisposal(...)` before clearing the managed pointer, matching finalizer and cached-resource cleanup behavior. This keeps WebGPU buffer destroy/release ordering in the backend cleanup queue instead of requiring WPF or shim callers to know whether disposal is happening on the render thread.
- ProGPU PR #17 cached-layer context/lifetime cleanup now treats visual layer textures as valid only for their owning `WgpuContext`, disposes and recreates stale cross-context `Visual.LayerTexture` instances, tracks layer textures by owning `Visual`, and sweeps detached, hidden, disabled, mismatched, or disposed layer caches after each rendered frame. This keeps WPF `CacheMode` and effect layer semantics on the native ProGPU compositor path without managed WPF bridge code trying to own WebGPU texture lifetime.
- ProGPU PR #17 Avalonia host zero-copy cleanup now keeps `ProGpuHostControl.EnableZeroCopy` opt-in by default and falls back to the existing custom visual renderer when shared surface creation/import fails after opt-in. This keeps platform-specific shared-image capability probing and blank-control prevention in the ProGPU host layer instead of asking WPF or Avalonia callers to detect failed external-image imports.
- ProGPU PR #17 Avalonia host async zero-copy cleanup now rechecks the captured swapchain image, imported image, drawing surface, write index, and `WgpuContext` after asynchronous staging-buffer mapping and drawing-surface update awaits. This keeps detach/fallback races inside the ProGPU host layer instead of asking WPF or Avalonia callers to serialize host lifetime around shared-image readback.
- ProGPU PR #17 SKSurface failed-flush cleanup now clears recorded surface commands and releases canvas-owned `SaveLayer` textures from a failure-safe `SKSurface.Flush()` cleanup path, even when `RenderOffscreen(...)` or readback throws. This keeps WPF/Skia interop resource lifetime in the shim/backend instead of adding WPF bridge-side try/finally compensation for retained image or layer textures.
- ProGPU PR #17 Skia/WPF stroke-transform cleanup now records transformed stroke thickness in the ProGPU shims. `SKCanvas` scales `SKPaint.StrokeWidth` from the active canvas matrix for stroked primitives and paths, and the WPF shim `DrawingContext` scales `System.Windows.Media.Pen.Thickness` from pushed transforms before converting to native `ProGPU.Vector.Pen`. The reusable `ProGPU.Vector.TransformMetrics.GetStrokeScale(...)` helper keeps the 2D matrix metric in ProGPU instead of duplicating bridge-local managed stroke workarounds.

## Decisions

- Keep feature work that accelerates WPF rendering fidelity in the ProGPU branch when it improves the backend model directly, instead of compensating in the WPF bridge.
- Keep WPF arc semantics in ProGPU vector primitives. The WPF bridge may adapt WPF types, but affine arc math, sweep flipping, and shader-rasterized arc records belong in `ProGPU.Vector`/`ProGPU.Scene`.
- Preserve API-level rounded-rectangle radii before they reach the bridge. WPF `radiusX`/`radiusY` and Skia per-corner radii should remain native ProGPU command/path data, with only uniform scalar cases using the compositor's fast rounded-rect primitive path.
- Preserve host-level per-corner corner radii as geometry when the host platform API cannot express them as independent radii. Do not collapse asymmetric `CornerRadius` values to a uniform max-radius clip in host controls, because that clips square corners that WPF/Avalonia semantics leave uncut.
- Keep WPF/WinUI path API shims from erasing arcs before rendering. Parsed `ArcSegment` values should stay native ProGPU arcs, and WPF `Geometry.Transform` should be passed as command transform metadata so ProGPU owns affine arc evaluation. Degenerate arcs may fall back only inside the ProGPU vector layer where shader parameters cannot be produced.
- Prefer fixed-quad shader primitives for WPF-compatible arc strokes. CPU flattening should be limited to degenerate arcs that cannot produce valid shader parameters; parametric strip tessellation should not be reintroduced as a WPF bridge workaround.
- Preserve WPF clip geometry as ProGPU geometry masks whenever the source geometry can be represented as a ProGPU vector path. Rectangular fallback is acceptable only for unsupported geometry forms, and rotated/sheared fallback bounds must still include all transformed corners.
- Keep WPF geometry API shims non-mutating and query-correct. Geometry bounds must apply the current transform through ProGPU vector helpers before rendering, not by copying state during `Draw()`, because WPF layout, hit-testing, clipping, and retained cache sizing commonly query bounds before render replay.
- Treat subtractive and inverse clip/fill semantics as native path-op work. Skia `Difference` clips, inverse fills, and WPF `CombinedGeometry.Exclude` should feed ProGPU combined geometry and the WebGPU path-op/mask pipeline rather than managed bridge-side region emulation.
- Treat GDI/WPF text-size unit conversion as a shim/backend responsibility before native text layout. The WPF bridge should pass source font intent through; ProGPU should normalize point/document/inch/millimeter sizes against the active graphics DPI rather than asking bridge code to pre-scale text.
- Preserve text run boundaries in ProGPU shims. Mixed fonts, fallback runs, and positioned glyph spans should remain explicit `SKTextBlob`/glyph-run data that the backend can draw natively instead of being collapsed or re-split in the WPF bridge.
- Treat color-layer glyph interleaving as compositor batch ownership. COLR/CPAL vector layer output may switch to path rendering, but text buffers should expose only committed glyph instances and resume text batching afterward rather than asking WPF bridge code to split glyph runs.
- Treat texture alpha convention as backend texture metadata, not as a pipeline-name heuristic or WPF bridge concern. Upload paths that know they contain straight RGBA should mark that explicitly; render targets and PBgra uploads should remain premultiplied so layer/effect composition stays stable.
- Treat fixed-function advanced blend limitations as backend shader/pipeline specialization work. When WebGPU blend factors require premultiplied source RGB for WPF-compatible `Screen`, `Multiply`, or `DstOver`, ProGPU should choose native fragment variants or effect wrappers that produce the right source convention rather than asking WPF bridge code to rewrite colors or shaders.
- Treat WebGPU shader resource capacity limits as backend draw-time limits, not WPF managed-effect state. A WPF shader effect that uses every logical sampler register can skip a masked draw on devices that cannot also bind the mask texture/sampler, but that limitation must not poison cached/reused effect parameters once the mask scope is gone.
- Treat mutable effect invalidation as native scene ownership. Effects may be shared across visuals, so ProGPU effect descriptors should invalidate weakly tracked visual owners and let parent dirty propagation refresh cached layers instead of relying on WPF bridge polling or managed replay invalidation workarounds.
- Treat backend render-target ownership as explicit. A shim surface may borrow a ProGPU `GpuTexture` and render into it directly; backend handles from APIs this WebGPU context cannot mutate must fail rather than allocating a hidden replacement texture.
- Preserve backend texture metadata through shim snapshots. Surface snapshots should retain the source texture format and alpha convention instead of coercing into RGBA and expecting downstream WPF/Skia bridge code to repair channel interpretation.
- Preserve surface contents inside backend shims across incremental flushes. Skia/GDI-style surfaces should track whether a render target already has valid contents and request native load operations where the source API expects persistent pixels, rather than forcing WPF bridge code to replay previous draw batches.
- Treat SKPaint blend modes, save-layer paint application, and encoded-image alpha conversion as Skia shim/backend responsibilities. The WPF bridge should issue WPF/Skia drawing intent and let ProGPU map supported blend modes and alpha conventions into native command state and texture metadata.
- Treat Skia save-layer image filters as native layer operations. `SaveLayer` should route supported `SKPaint.ImageFilter` values through ProGPU image/effect pipelines and fail closed for future unsupported filters instead of silently compositing the unfiltered layer or asking WPF bridge code to emulate pixels.
- Treat Skia surface flush cleanup as shim/backend ownership. `SKSurface.Flush()` must release command-retained image resources and `SaveLayer` textures on both success and failure paths, because WPF/Skia callers should not need managed-side retry or disposal workarounds to avoid native texture leaks after a failed render/readback.
- Treat zero-copy shared-image lifetime as host/backend ownership. `ProGpuHostControl` must revalidate captured resources after async map/update awaits and abandon stale frames when detach, cleanup, or fallback replaces the current zero-copy state, because WPF and Avalonia callers should not need to guard shared WebGPU resources around host lifetime races.
- Treat Skia/WPF dash, gradient spread, and image-effect color-space semantics as backend responsibilities. Dash spans, tile/spread mode mapping, and straight/premultiplied image-effect color math should stay in ProGPU vector/shader code rather than in WPF bridge adapters.
- Treat GDI world-transform stroke thickness as shim/backend state. WPF and GDI callers should pass source geometry and pen intent through; ProGPU should convert that into native command thickness or transformed command state rather than bridge-side outline inflation.
- Treat Skia canvas transforms and WPF pushed transforms as shim/backend stroke state. When the backend consumes scalar pen thickness in device space, ProGPU shims should derive that thickness from the active transform and leave WPF managed rendering managers focused on source pen/geometry intent.
- Treat WPF `RenderTransform`, `LayoutTransform`, `TransformGroup`, and transform-origin metadata as managed WPF object graph and layout state until replay. The SDK path should compile and instantiate those normal WPF properties without app code changes, then pass the resolved visual transform state to ProGPU for native drawing, clipping, effects, and cache placement rather than adding managed transform workarounds.
- Treat WPF dash/cap/join/miter intent as native pen state. The bridge should preserve the source `Pen` metadata and issue one native command where possible; any remaining dash-span construction, cap clipping, curve flattening, or shader specialization needed for full WPF fidelity belongs in ProGPU vector/compositor code, not in managed WPF replay.
- Treat unsupported Skia gradient and backend-origin cases as backend capability gaps. Two-point conical gradients and bottom-left ProGPU backend render targets now have native support; future unsupported cases should either fail closed or gain native shader/render-target support in ProGPU, not bridge-side CPU compensation.
- Treat effect coordinate systems and extension frame cleanup as compositor responsibilities. HiDPI mask uniforms, logical versus physical effect quads, BGRA readback swizzling, skipped layer cleanup, and offscreen-only extension resource pruning should be fixed in ProGPU native render/effect/shim code rather than compensated in WPF managed replay.
- Treat explicit render-target viewport origins as compositor state. Mask texture allocation, mask-pass viewport/scissor state, and shader sampling coordinates must stay aligned in ProGPU for decorated or HiDPI native client subrects; WPF layout coordinates should remain logical client coordinates.
- Treat `GpuTexture.Generation` as the backend cache-invalidation contract for mutable texture contents. Any ProGPU write, PBgra/WriteableBitmap upload, or texture copy that changes pixels must advance generation so WPF shader effects, image effects, retained samplers, and future texture-backed bitmap sources invalidate from native metadata.
- Keep WPF-specific type adaptation in `src/ProGPU.Wpf` until the real WPF `PresentationCore` and ProGPU shim type identities are unified.
- Prefer adding reusable ProGPU primitives for WPF concepts that also benefit other frontends: exact path stroking, gradient tables, texture sampling modes, text flags, mesh extensions, image effects, and retained resource lifetime helpers.
- Treat render-thread WebGPU context availability as a backend/compositor invariant. Host integrations should not compensate for thread-static context state; compositor render entry points should publish their active context while backend resources should remember their owning context for cleanup and cache reuse.
- Use signed ProGPU assemblies for direct references from strong-named WPF assemblies when the TFM/project graph is compatible. Until then, source-share small ProGPU-owned helpers only when they remain internal to WPF and avoid exposing ProGPU namespaces as WPF public API.
- Treat XAML loading, writing, resource-key conversion, resource dictionaries, and theme object graphs as WPF-owned managed behavior. When a serializer gap appears, first repair the reused WPF serializer path, as with `SystemResourceKey`/`SystemThemeKey` emission through `SystemKeyConverter`; only move work to ProGPU when the missing piece is a native rendering, shader, vector, text, image, or platform capability.
- Treat visual opacity-mask execution as native ProGPU retained scene work. `Visual.OpacityMask` and `Visual.OpacityMaskBounds` now let the WPF bridge set supported bounded masks as native retained visual state, so masked WPF descendants can remain separate retained child visuals without bridge-side inline replay or managed mask emulation.

## ProGPU Features to Implement Next

- Stroke/dash rendering: use the new `Pen.DashStyle` native metadata path as the API boundary, then move any remaining bridge- or compositor-side dash-span expansion into backend-owned vector stroke commands and shader paths for lines, curves, arcs, primitive outlines, and combined path-op results. The WPF bridge should not need to know whether the dash target is a line, Bezier, arc, rounded rectangle, ellipse, or boolean path.
- Text: build on the native `TextRenderingMode`/ClearType shader path, scoped `ClearTypeHint=Enabled` replay, native `TextHintingMode` placement policy, mixed COLR/CPAL color-layer batching, multi-run `SKTextBlob` preservation, DPI-aware GDI font-unit normalization, and the reusable SFNT, simple shaper, and subsetter helpers by adding richer OpenType layout table APIs, full glyph shaping hooks, fallback chains, CFF subsetting, cmap/name pruning, page-unit/page-scale policy, and deeper font-level grid-fitting/hinting parity so WPF `PortableTextInterface` can delegate more of the remaining text stack instead of carrying compatibility code. The compact glyph-remapping TrueType subsetter now exists in ProGPU; WPF should adopt it only once its embedding path can consume the returned glyph map.
- Geometry: move remaining WPF fidelity gaps into ProGPU vector code, including backend-owned dashed/stroked path commands for boolean-result outlines, non-axis-aligned guideline snapping policy, robust cap clipping at complex joins, remaining Skia/GDI path conformance cases that can feed WPF-compatible tooling paths, direct atlas-side path-op rasterization without readback for high-volume combined paths, exact WPF-compatible geometry bounds for real `PathGeometry.GetPathBoundsAsRB(...)` callers including transformed arcs, and remaining non-path geometry clip forms beyond path-compatible, primitive, grouped, and combined geometry that should become native masks instead of bridge-side bounds fallbacks. The current managed non-Windows WPF bounds fallback is a temporary bring-up guard; full-performance arc-compatible bounds and rasterization should remain ProGPU vector/backend responsibilities.
- Brushes: build on the native two-point conical/focal gradient shader path and keep expanding ProGPU brush ABI/shaders for WPF brush semantics, including more tile-brush transform cases, color-profile policy, and remaining gradient interpolation/spread edge cases.
- Images: build on `PixelDataBuffer`/`Pbgra32PixelBuffer`, `GpuTexture.WritePbgra32SubRect(...)`, explicit `GpuTextureAlphaMode`, opaque-upload alpha normalization, and the row-aware Skia-compatible bitmap/image shims with ProGPU image codec, color-management, cache, source-rect upload, mip/sampling policy, alpha-convention propagation, backend-origin handling for wrapped render targets, wrapped texture usage validation, and texture-backed bitmap seams so WPF bitmap sources can move away from reflection-based `CopyPixels` transition upload and toward backend-owned image resources. `SKImage.FromTexture(...)` now declares the current deferred `SKCanvas.DrawImage(...)` copy requirement by requiring borrowed textures to include `TextureUsage.CopySrc`; if ProGPU later supports retaining/sampling borrowed textures directly, replace that validation with a native no-copy retain path rather than a WPF-side workaround. The WPF transport can keep basic GIF frame enumeration in managed code for bring-up, but animated GIF timing, per-frame upload/cache policy, public metadata, and texture-backed frame reuse should move behind ProGPU/platform image services instead of becoming WPF-side rendering workarounds.
- Effects: continue mapping WPF effect resources onto ProGPU-native shader/compute/render pipelines. Visual `BlurEffect` and `DropShadowEffect` descriptors can now flow through retained ProGPU host replay, registered WPF `ShaderEffect` replacements can now lower to visual-level `ProGPU.Scene.WpfShaderEffect` descriptors that run WGSL over the retained visual's offscreen source through native WebGPU with WPF sampler-register mapping, active mask binding, and source-alpha-mode-specific opacity/mask handling, ShaderToy/WGSL extension draws now have native active-mask and opacity propagation, simple image effects now have explicit native mask binding and straight-color operation for premultiplied sources, native texture draws now branch mask RGB scaling by source alpha mode, and native blend pipelines/effect wrappers now specialize straight vector/text/texture/image-effect/WPF-shader-effect outputs for WPF-compatible `DstOver`/`Screen`/`Multiply` blending. Source/object generated `PushEffect` can lower supported context-bound legacy bitmap effects to native retained effect scopes, supported `DrawingGroup` effects can now wrap child drawing replay in native retained effect scopes, registered shader effects can bind bitmap-backed `ImageBrush` sampler registers into ProGPU's native `WpfShaderEffectSampler` bank, finite replayable `DrawingBrush`/`VisualBrush` shader sampler inputs render into cached ProGPU textures through the existing brush replay path, and all-register effects keep native active-mask binding when the WebGPU device exposes enough sampled texture/sampler slots. Remaining WPF-facing work is non-emulatable legacy bitmap effects, non-context `BitmapEffectInput`, advanced/unbounded brush shader sampler inputs, other non-image sampler texture inputs, and legacy Direct3D pixel-shader bytecode import/translation so existing WPF HLSL effects can populate the native ProGPU shader descriptor automatically. Avoid managed CPU shader emulation.
- Composition: build on retained `ChangeVersion` counters, native `CacheAsLayer` lowering for WPF visual and drawing-group `CacheMode`, retained `Visual.OpacityMask`/`OpacityMaskBounds` lowering for bounded WPF visual opacity masks, effect render cache keys, reflected WPF source version polling with dirty-source identity, dispatcher/timer render scheduling, the first MediaContext render-priority wakeup/dispatcher-flush bridge, disconnected managed animation ticks, the first host-level frame-state gate, clean-source retained WPF replay skips, and `WpfRetainedVisualBranchMap` dirtying mapped native branches for reflected WPF source objects while falling back when any dirty source is still unmapped or shares a native branch with clean source owners. Exact branch replay now includes tracked replay resources, source/object drawing-context resources, provider-created owner-scoped `RenderOpen()` branches during retained-root rebuild frames, and shallow owner `Children` collection and visual content-object dependencies, so source drawing, add/remove/reorder, or `RenderData` content invalidations can target the owning retained branch without recursively making child visuals parent dependencies. Preserved-retained-root frames keep provider-created owner contexts on the flat fallback to avoid appending stale branches outside a skipped or partial replay target. Continue toward dirty-region policy, partial retained-branch replay, resource-to-visual ownership mapping beyond reflected source visual branches, unbounded/advanced mask resource handling, and full MediaContext integration so WPF `Freezable`/visual/effect/cache invalidation can skip unchanged retained branches without replaying the full reflected tree. Keep native offscreen effect and layer rendering version-stable by using render-only transform overrides rather than mutating retained visual properties during composition.
- Windowing: keep Silk.NET surface and input abstractions in ProGPU reusable enough for WPF's eventual `CompositionTarget` replacement, including resize, title/client-size updates, DPI, activation, file-drop, cursor, timer, dispatcher scheduling, and render wakeup hooks.
- Keep WPF SDK app rendering in logical DIPs while ProGPU owns high-DPI swapchain pixels. The WPF host should configure WebGPU surfaces from physical framebuffer sizes, keep WPF layout and retained replay roots sized from the managed client dimensions, and pass both logical dimensions and the explicit physical render-target size to ProGPU's compositor. When Silk.NET reports a logical-sized framebuffer on a scaled display, the host should derive the physical render target from the monitor DPI scale rather than moving WPF layout into physical pixels. `Compositor.RenderScene(...)` now has a logical/physical overload so projection, DPI uniforms, text hinting, effects, masks, and cached layer sizing stay consistent on Retina/HiDPI displays without managed WPF coordinate workarounds or assumptions about Silk.NET `IWindow.Size`.
- Treat Silk.NET `IWindow.Size` and resize callbacks as platform-reported client dimensions that must be normalized before they become WPF logical DIPs, and treat `IWindow.FramebufferSize` as the physical render-target source. Some macOS/Retina startup paths can report framebuffer-sized values through `IWindow.Size` or resize callbacks; the host should compare native size, framebuffer size, cached WPF client size, and monitor scale to keep WPF layout in DIPs, with a conservative inferred-scale fallback when monitor scale discovery reports `1.0` but native size equals the framebuffer, or when native size itself looks like cached WPF DIPs multiplied by a plausible scale while the framebuffer is scaled again. Once a portable `PresentationSource` is bound, its last synchronized client size is the preferred WPF logical-size cache; `Window.SetClientSize(...)`, native resize, and render-surface resolution should use that source cache before the host-native cache so a transient physical callback cannot remeasure WPF content into the upper-left quadrant. Resize can arrive before WPF target attachment, so the host must update its logical cache and request a render even during startup; otherwise a stale or physical-sized client cache can reproduce the SDK smoke upper-left-quadrant rendering while the swapchain itself is correctly configured at Retina size.
- Keep decorated-frame client placement as a native ProGPU render-target viewport, not a WPF layout offset. Silk.NET and platform windowing may expose a framebuffer larger than the WPF client content because of title-bar/decorated frame pixels. `ProGPU.Scene.Compositor.RenderScene(...)` now accepts `RenderTargetViewport`, and the WPF host resolves a client viewport inside the physical framebuffer before presenting. Future fixes for screenshot shapes where WPF content fills only the upper-left client-sized region, or is shifted under/away from native decorations, should preserve WPF logical coordinates and improve ProGPU/Silk.NET viewport, scissor, canvas-pixel helper, and swapchain geometry handling directly.
- Keep effect-mask sampling dimensions in the native ProGPU effect pipelines. When an active opacity/geometry mask is backed by a full physical render target but content is rendered through an offset `RenderTargetViewport`, image effects and WPF shader effects must sample masks using the active mask texture dimensions rather than the viewport dimensions. This keeps WPF layout/replay in logical coordinates while ProGPU shaders own framebuffer-space mask alignment for decorated and HiDPI targets.
- Keep startup client-size handoff in the WPF host rather than adding shader, compositor, or managed replay offsets. The current SDK smoke issue is fixed by caching WPF-declared `Window.Width`/`Height` as logical DIPs during portable activation attach and delaying `PortablePresentationSource.SetClientSize(...)` until WPF reaches `Show()`/`Run()`, while ProGPU continues to receive explicit logical and physical render sizes. Future ProGPU work should make display-scale/native-size reporting reusable across Silk.NET hosts, but should not compensate for this class of bug by shifting draw coordinates or resizing WPF visual roots in physical pixels.
- Keep the WPF-declared startup client size as an explicit requested logical-size hint until the portable `PresentationSource` can become authoritative. The SDK smoke offset can still reproduce if a native startup resize writes physical Retina dimensions into the host cache before WPF reaches `Show()`/`Run()`; `ProGpuWpfWindowHost` now keeps the requested logical `Window.Width`/`Height` as a fallback for render-surface resolution and native resize normalization, so WPF content remains measured at `420x840` DIPs while ProGPU renders to the physical `840x1680` target. This remains a host/windowing responsibility, not a shader or retained-scene coordinate workaround.
- Reconcile conflicting WPF-requested and portable `PresentationSource` client-size caches before trusting either one as logical size. If the two cached dimensions differ by a plausible DPI scale, the smaller DIP-sized value should win so a transient physical Retina report such as `840x1680` cannot become WPF layout size for a `420x840` window. This cache reconciliation belongs in the host/windowing DPI boundary and should eventually move into a reusable ProGPU/Silk.NET display-scale/native-size service, not into WPF control layout, managed replay offsets, or compositor coordinate shifts.
- Reconcile the final host-resolved logical client size against the cached WPF DIP size before applying it to either render-surface geometry or native resize state. A missing startup framebuffer report can otherwise let a physical Retina native size look like the logical client size even after the earlier cache reconciliation. The host now accepts the cached DIP dimension only when the resolved dimension differs by the monitor scale or a known common DPI scale, preserving ordinary non-DPI-multiple logical resizes. Future ProGPU branch work should expose a reusable display-size observation service that reports native client size, framebuffer size, monitor scale, and confidence/missing-data state for Silk.NET hosts so WPF does not carry this policy locally.
- Keep startup native-window creation out of WPF's startup dispatcher operation. WPF `Application` should still call `Window.Show()` for `StartupUri` roots so managed visibility, source initialization, window collections, and event semantics are reused, but the ProGPU bridge should defer Silk.NET initialization for `Application.Current.MainWindow` until `Run()` enters the native loop. Do not fix startup hangs by draining WPF normal/idle dispatcher work before `TryRun(...)`; native-loop render wakeups should drive those dispatcher operations after the host owns progress.
- Keep retained WPF frame roots in logical DIPs as well. The host-level logical/physical split is not sufficient if the retained composition sink creates its root owner visual from physical framebuffer dimensions; that branch then behaves like an oversized logical scene and can show the SDK smoke content in the upper-left physical quadrant. Retained roots and the retained layer root should both keep logical bounds with identity scale, leaving logical-to-physical mapping to the explicit `Compositor.RenderScene(...)` logical/physical render pass instead of compensating inside WPF replay. ProGPU backend resources, masks, effect textures, and cache textures should scale to physical pixels, but WPF visual tree sizing and owner branch bounds should remain in DIPs. The SDK switch and external package-mode harnesses should keep validating this from the actual output assemblies by refreshing mutable local fixed-version packages during restore, byte-matching output `ProGPU.Wpf.dll`/`ProGPU.Scene.dll` against the local packages, instantiating `ProGpuWpfDrawingFrame`, and checking logical root sizes, physical pixel dimensions, retained-layer identity scale, and transform origin, because mutable local `11.0.0-dev` packages can otherwise make stale output look like a rendering regression.
- Keep the repo-local SDK smoke startup guard in place for the custom SDK deliverable. Package-mode validation apps should verify the shipped ProGPU display-scale, WPF host render-surface, composition-target render, and scene compositor logical/physical APIs before opening a native window; stale fixed-version output should fail directly instead of encouraging managed layout shifts, compositor offsets, or shader-side compensation for a package/output freshness bug.
- Keep package-mode HiDPI pixel guards on real WPF retained visual replay until source-level shim type identity is unified. The repo-local smoke and generated harnesses should use a measured real WPF element, `ProGpuRetainedCompositionCommandSink`, and `ReplayVisualSubtree(...)` to prove a `420x840` DIP scene reaches the lower-right pixel of an `840x1680` target. Direct typed `WpfCompositionDrawingContext.DrawRectangle(Brush, Pen, Rect)` calls still expose the current shim-vs-real assembly split where ProGPU.Wpf expects some WPF primitives from the shim `PresentationCore` while the SDK transport provides them from `WindowsBase`; treat that as a bridge compatibility item, not as a reason to add managed replay offsets.
- Treat popup windows as a platform-host boundary, not a reason to fork WPF menu/tooltip managers. The current portable bring-up uses `PortablePresentationSource` for non-Windows popup roots so WPF `ToolTip`, `ContextMenu`, and popup manager state can run without Win32 `HwndSource`, `MonitorFromRect`, `GetCursorPos`, or MSAA/UIA dependencies. ProGPU/Silk.NET should eventually provide reusable native popup/window surfaces plus cursor/screen services for real z-order, screen constraint, capture, and accessibility integration, while WPF continues to own popup content, placement policy, routed events, styles, templates, and command state.
- Keep WPF `InputScope`, `InputScopeName`, and `InputScopePhrase` in the managed WPF object model and XAML parser. ProGPU/Silk.NET should later map that metadata to native IME and touch-keyboard hints behind a platform service, not require app code or controls to bypass WPF's attached `InputMethod.InputScope` API.
- Host lifetime: reuse the ProGPU shared-context lease model for WPF host surfaces so multiple composition targets/windows can borrow the same WebGPU device without stale owner disposal. Any per-platform host should release resources before releasing its context lease.
- Zero-copy host composition: keep shared-surface/import probing inside ProGPU host controls and leave a safe copy-backed/custom-visual path available by default until each platform can prove imported images are usable at runtime.
- WPF command conformance: add ProGPU-side fixtures for every generated WPF render-data instruction that reaches `IWpfCompositionCommandSink`, so unsupported command state is tracked in ProGPU tests before WPF bridge code grows new compatibility branches.

## WPF Integration Follow-Ups

- Build on the new `packaging/ProGPU.Wpf.Sdk` custom MSBuild SDK skeleton as a first-class deliverable. Existing WPF apps should adopt the port by changing only the project SDK, while the SDK redirects WPF framework references, imports the real WPF markup compiler/build targets, packages ProGPU/Silk.NET runtime assets, selects the portable platform backend, and preserves existing XAML/BAML/code-behind behavior. The SDK shell now packs as an `MSBuildSdk` nupkg with root `Sdk/` and `targets/` assets, package mode references the ported managed WPF transport bundle through configurable package id/version properties, aligns dependency versions to the current SDK package version, requires the WPF transport bundle to carry both ref and lib payloads, keeps `ProGPU.Wpf` from advertising package dependencies so restore uses the real WPF identities from the transport bundle, owns the explicit ProGPU/Silk.NET package closure, owns the portable `InternalMarkupCompilation=true` plus `AlwaysCompileMarkupFilesInSeparateDomain=false` defaults so application projects do not need those PresentationBuildTasks compatibility switches, and owns the current portable `RuntimeFrameworkVersion` default through `ProGpuWpfRuntimeFrameworkVersion` so application projects do not need runtime pins. `src/ProGPU.Wpf.SdkSwitchSmoke` restores/builds against the nupkg with a normal default-item WPF project file, default assembly name/root namespace, and now covers a compiled merged resource dictionary, dynamic/static resources, `x:Shared=false` resources, `Style.BasedOn`, `ObjectDataProvider` and `XmlDataProvider` resource providers, explicit styles, control templates, `VisualStateManager` groups, property/data/multi-condition triggers, data templates, `HierarchicalDataTemplate`/`TreeView` item hierarchy binding, collection binding, dynamic `ObservableCollection` change notifications, `INotifyPropertyChanged` binding notifications, binding validation rules and attached validation state, custom routed-event registration and BAML attached-event hookup, `IValueConverter` and `IMultiValueConverter` resources, binding `StringFormat`, `MultiBinding`, XPath binding, data-template selector resources, `ItemsPanelTemplate`, `ItemContainerStyle`, item string-format metadata, `Grid` row/column definitions, `DockPanel`, `Canvas`, and `UniformGrid` layout metadata plus attached layout properties, `CollectionViewSource` sorting/grouping, `GroupStyle` headers, selected-item binding, a second compiled `UserControl` XAML page with generated code-behind, custom dependency properties, `ElementName` and deterministic `RelativeSource` bindings, a compiled `Page` loaded through `Frame.Source`, a default-item `Themes/Generic.xaml` default style for a custom `Control`, generated events, compiled command/input bindings, routed command execution through `Button.Command`, `CheckBox` and `RadioButton` checked/unchecked routed events plus radio group state, `PasswordBox` password state, `Calendar`/`DatePicker` selected-date state, button tooltip and context-menu loading, context-menu command/checkable state, toolbar command and toggle state, status-bar item/content binding, group/expander/scroll content state, slider/progress range binding, `ListView`/`GridView` display-member column binding and selection state, explicit `DataGrid` text/checkbox column binding and selection state, text input binding, and rich document objects without app-specific ProGPU project properties. The SDK copies WPF/ProGPU/Silk.NET/support-package managed assets plus current-platform native WebGPU/GLFW assets into the app output, and the SDK injects an automatic non-Windows module-initializer bootstrap that registers the existing ProGPU/Silk.NET portable activation path without app code changes. `src/ProGPU.Wpf.SdkSwitchRuntimeHarness` validates the SDK-produced app's richer BAML resources, application and window resource-provider output, distinct unshared resources, generated `Window`, `UserControl`, and `Page` object graphs, `Frame` component-resource source normalization, namescope, binding, property-change binding refresh after dispatcher data-bind work drains, collection-change propagation to item views and an `Items.Count` binding, validation-rule failure and recovery through WPF's binding validation manager, custom routed-event bubbling over WPF's event manager, converter output, multi-binding converter output, XPath data-provider binding text, formatted binding text, data-template selector decisions, items-panel template loading, item-container style metadata, item string-format metadata, hierarchical-template metadata and child collection binding, dependency-property values, based-on style inheritance, default theme-style lookup through `ThemeInfo`/`Generic.xaml`, grid/dock/canvas/uniform-grid layout metadata, collection-view/group-style object graphs, generated event hookup, command/input binding metadata, routed command execution, check/radio click state, password/date-selection state, tooltip and context-menu item/control state, toolbar and status-bar item/control state, group/expander/scroll state, range value changes, range-to-progress binding, list-view grid-view column bindings, list-view selected-item state, list-view item collection propagation, data-grid column bindings, selected-row state, data-grid item collection propagation, templating, visual-state groups, triggered resource values, rich text, SDK auto-bootstrap, native asset copy, and real `Application.Run()` startup/shutdown path from that output. The SDK-produced apphost now reaches the native ProGPU/Silk.NET run loop in a bounded direct probe, including package-mode geometry replay that avoids shim-only `PathGeometry.Parse`, shim-owned primitive geometry value-type reads, the transition `ProGpuCombinedGeometry` subclass, and package-mode transform replay that converts reflected WPF transforms through neutral/native matrices instead of JITting shim-only `System.Windows.Media.Matrix` metadata when real WPF assemblies are loaded. This is a package-mode smoke milestone, not full arbitrary app compatibility; the next concrete SDK tasks are CI/package validation, additional full apphost coverage beyond the smoke app, app shutdown/loop edge hardening, and broader platform service certification. Treat any additional normal app source-code change as a port bug unless it is explicitly Windows-only interop.
- Gate the custom SDK deliverable with an external project smoke outside the repository build graph. `src/ProGPU.Wpf.SdkExternalSmokeHarness` now creates a temporary WPF application and WPF user-control library that use only `Sdk="ProGPU.Wpf.Sdk/11.0.0-dev"` plus ordinary WPF project metadata (`TargetFramework`, `UseWPF`, `OutputType`, and `ProjectReference`) and no generated `Directory.Build.props`/`Directory.Build.targets` or local artifact-root properties. The richer explicit-item application is validated through both `dotnet app.dll` and the SDK-produced native apphost, and the same native apphost is launched in a live ProGPU/Silk.NET geometry probe that verifies swapchain pixels cover the XAML-declared logical window. The harness also builds a second temporary external application that keeps default SDK item discovery and the default project assembly name enabled, contains no explicit `Compile`, `ApplicationDefinition`, or `Page` items, and validates compiled `App.xaml`, `MainWindow.xaml`, a default-discovered `UserControl`, resources, generated event hookup, runtime asset copy, bounded `Application.Run()` shutdown through `dotnet app.dll`, direct SDK-produced native apphost launch, and live apphost swapchain pixels covering its XAML-declared logical window. Keep this as the migration-contract check: package feed configuration can be environment/tooling setup, but ordinary app project files should not need ProGPU-specific properties or source changes when switching to the ProGPU/Silk.NET WPF SDK. The external gate now also covers referenced-library `ThemeInfo`/`Themes/Generic.xaml` default style lookup, `TemplateBinding`, and component-resource-key lookup from compiled output, merged app resource dictionaries, static and dynamic resources, dynamic-resource invalidation, `Style BasedOn`, style property triggers, property `Trigger.EnterActions`/`ExitActions`, `ControlTemplate` instantiation, control-template `Trigger.EnterActions`/`ExitActions`, template binding, `RelativeSource TemplatedParent` binding inside templates, `Menu`/`MenuItem` command and checkable state, `ContextMenu` item state, `ToolTip` placement/content, `CheckBox` checked state, grouped `RadioButton` mutual exclusion, `ToggleButton` checked state, `Grid`, `DockPanel`, `Canvas`, `UniformGrid`, `ItemsPanelTemplate`, `ItemContainerStyle`, `ListView`/`GridView` display-member column binding, explicit `DataGrid` text/checkbox column binding and selection state, `HierarchicalDataTemplate` child binding, data-bound `TreeView`, explicit `TreeViewItem` expansion/selection events, `ComboBox` selected-value binding/source updates, `TabControl` selected-tab state, generated `SelectionChanged` handlers, `GroupBox`/`Expander` content, expanded/collapsed routed events, `ScrollViewer` content/visibility metadata, `RichTextBox`/`FlowDocument` rich document trees, formatted inlines, `Hyperlink.RequestNavigate`, inline/block UI containers, lists, sections, tables, `RichTextBox.Selection`, `TextRange` traversal, `FlowDocumentScrollViewer`, `FlowDocumentPageViewer`, and `FlowDocumentReader` document hosts, compiled `DataTemplate` loading, `ContentControl.ContentTemplate`, `ObservableCollection` item binding, `ObjectDataProvider` method invocation, `XmlDataProvider` inline XML/XPath binding, compiled value converters, `MultiBinding`, `PriorityBinding`, custom `ValidationRule` state, compiled `Window.CommandBindings`, `InputBindings`, `RoutedUICommand`, `Button.Command`, generated `Click` hookup, `FocusManager` scopes, `KeyboardNavigation` metadata, compiled app `Page` BAML, `Frame.Source`, navigation events, second-page navigation, journal back navigation, and normal WPF `Application.Run()` with `StartupUri`, `Startup`/`Exit`, `Application.Current`, `MainWindow`, `Windows`, startup resource mutation, dynamic-resource resolution, secondary-window `Application.Windows` tracking, default `OnLastWindowClose` retention, owner/owned-window tracking and parent-close semantics, modal `Window.ShowDialog()`/`DialogResult`, canceled `Window.Closing`, `Window.Closed` sequencing, `ShutdownMode.OnMainWindowClose`, and clean shutdown. ProGPU/Silk.NET must render the resolved WPF resource/data/binding/style/template/layout/item-presentation/list-view/grid-view/data-grid/menu/popup/choice/tree/selector/content/rich-document/theme/command/focus/navigation/application trees and provide host-loop/input/text/rendering integration, not replace WPF's resource, binding, style, template, layout, item-control, list-view, grid-view, data-grid, menu, popup-adjacent, choice-control, tree, selector, content-control, document, document-viewer, command, focus, theme, page, journal, modal-dialog, or application-lifetime managers.
- Keep a tiny manually launchable SDK MVP app beside the broad stress sample. `samples/ProGPU.Wpf.HelloApp` uses the same switch-only project shape as a normal WPF app, default item discovery, compiled `App.xaml`/`MainWindow.xaml`, `StartupUri`, binding, collection binding, dynamic resources, and button code-behind, and `eng/run-progpu-wpf-hello.sh` launches the SDK-produced native apphost. This is the first stop for quick manual app bring-up; `PROGPU_WPF_HELLO_LIVE_VALIDATE=1` now validates the live apphost's ProGPU swapchain pixels against the declared logical window size and also verifies host-delivered mouse/keyboard/text input by focusing the `TextBox`, updating two-way binding, clicking the button, and observing bound UI updates. `samples/ProGPU.Wpf.MvpApp` remains the richer subsystem coverage sample.
- Keep the manually launchable MVP app on the SDK-produced apphost path. `eng/run-progpu-wpf-mvp.sh` builds `samples/ProGPU.Wpf.MvpApp` and executes the generated native apphost from the package-mode output directory, while `eng/progpu-wpf-sdk-ci.sh` validates `dotnet run`, direct apphost `Application.Run()` startup/shutdown, and a live ProGPU/Silk.NET apphost geometry probe whose pixels must cover the app's declared `760x560` WPF logical window. This keeps quick MVP testing aligned with how users run a built SDK-switched WPF app, without adding ProGPU-specific project properties or application APIs.
- Keep a short MVP readiness gate separate from the full package rebuild CI. `eng/progpu-wpf-mvp-quickcheck.sh` now runs the external no-source-change SDK smoke, the SDK-switch smoke apphost live geometry probe, and the Hello/MVP apphost self-tests plus live input/geometry probes, so day-to-day porting can quickly answer whether package-mode SDK apps still build, start through the produced apphost, reach a correctly sized ProGPU/Silk.NET swapchain, and deliver basic mouse/keyboard/text input before deeper framework or renderer changes. The full `eng/progpu-wpf-sdk-ci.sh` remains the source rebuild/package certification gate.
- Keep live MVP input coverage on WPF's focus, command, binding, button, toggle, and keyboard-navigation managers, not only text entry. The apphost probe now clicks the compiled Add button through the ProGPU/Silk.NET bridge and requires WPF `ButtonBase` plus routed-command/data-binding state to add and select a view-model item. It also clicks the compiled Actions `CheckBox` off and back on to validate two-way `ToggleButton.IsChecked` binding from native mouse input, then sends host `Tab` key events and requires WPF `KeyboardNavigation.TabNavigation="Cycle"` to move focus across the compiled Input tab controls. ProGPU/Silk.NET should deliver normalized native events; WPF should continue to own control activation, commands, binding updates, focus, and keyboard-navigation policy without app-side ProGPU focus/control code.
- Keep default-item `Page` navigation in the external migration-contract lane. The generated default-item app now loads a default-discovered compiled `DefaultItemsPage.xaml` through a normal `Frame Source` and validates the resulting `Page` instance during `Application.Run()`. Future ProGPU/Silk.NET work should render and host the resulting visual tree while leaving page BAML discovery, URI resolution, navigation scheduling, and `Frame` content ownership in managed WPF.
- Keep default-item resource dictionaries and component loading in the same managed WPF lane. The generated default-item app now merges a default-discovered `Resources/DefaultItemsAppResources.xaml` dictionary and validates `Application.LoadComponent(...)` for a compiled `UserControl`, so ProGPU/Silk.NET should consume the created visual/resource state rather than owning resource URI lookup, component BAML loading, or namescope/ElementName binding.
- Keep default-item dynamic-resource invalidation in that managed resource lane. The generated default-item app now replaces application and merged app-dictionary brush resources during `Application.Run()` and validates that existing `DynamicResource` expressions update, so ProGPU/Silk.NET should observe WPF's dependency-property/resource invalidation and redraw rather than owning resource-expression tracking.
- Keep default-item layout containers in the managed layout lane. The generated default-item app now validates `Grid`, `DockPanel`, `Canvas`, and `UniformGrid` metadata plus attached layout properties and a bound grid child during `Application.Run()`, so ProGPU/Silk.NET should consume WPF-arranged visual bounds and invalidation rather than owning a parallel WPF layout engine.
- Keep default-item item presentation in the managed item-control lane. The generated default-item app now validates `ItemsPanelTemplate`, generated `ListBoxItem` containers, `ItemContainerStyle` setters, and source item content during `Application.Run()`, so ProGPU/Silk.NET should render generated item visuals rather than owning item-container generation, panel selection, or item-container style application.
- Keep default-item selector-driven templates in the managed template lane. The generated default-item app now validates keyed `DataTemplate` lookup, a custom `DataTemplateSelector`, generated content-template visual trees, and template binding as selection changes during `Application.Run()`, so ProGPU/Silk.NET should render selected template visuals rather than owning WPF template-selection policy.
- Keep default-item style inheritance and event setters in the managed style/event lane. The generated default-item app now validates `Style.BasedOn` setter inheritance plus `EventSetter` routed handler hookup during `Application.Run()`, so ProGPU/Silk.NET should render the resulting dependency-property state and routed-event side effects rather than owning style inheritance or XAML event wiring.
- Keep default-item binding fallback selection in the managed binding lane. The generated default-item app now validates `PriorityBinding` fallback and refresh after source notifications/source updates during `Application.Run()`, so ProGPU/Silk.NET should consume the resulting dependency-property values rather than owning binding-expression priority or fallback policy.
- Keep default-item relative-source binding in that managed binding lane. The generated default-item app now validates `RelativeSource Self` and `RelativeSource AncestorType` lookup plus dependency-property refresh during `Application.Run()`, so ProGPU/Silk.NET should consume resolved values and invalidation rather than owning visual-tree ancestor binding lookup.
- Keep default-item binding fallback/null-value replacement in the managed binding lane. The generated default-item app now validates `FallbackValue` for missing paths and `TargetNullValue` for null source values plus refresh after source update during `Application.Run()`, so ProGPU/Silk.NET should render resolved binding target values rather than owning fallback/null replacement policy.
- Keep referenced-library component URI loading in that migration-contract lane as well. The generated default-item library now contributes a default-discovered `UserControl` and `Page`, and the app validates `Application.LoadComponent` against `/ExternalSdkDefaultItemsLibrary;component/...` URIs for both. ProGPU/Silk.NET should host and render the created cross-assembly visual trees while WPF keeps assembly resource lookup, BAML loading, namescope creation, and page/component instantiation.
- Keep implicit typed template lookup in the managed template/resource lane. The generated default-item app now validates an unkeyed `DataTemplate` for an app-defined type through a `ContentPresenter`, including generated visual tree and binding output. ProGPU/Silk.NET should render the selected template visuals and observe invalidation, not select templates or duplicate WPF's resource-key lookup policy.
- Keep default-item view-model binding and collection propagation in managed WPF as well. The generated switch-only app now validates `DataContext`, `INotifyPropertyChanged`, `ObservableCollection<T>`, `TextBlock.Text` binding, and `ListBox.ItemsSource` updates during `Application.Run()`. ProGPU/Silk.NET should render the resulting item visuals and consume WPF invalidation, not own a parallel binding engine, item view, or collection-change manager.
- Keep default-item two-way text input binding in that same WPF-owned binding lane. The generated switch-only app now validates `TextBox.Text` with `Mode=TwoWay`, `UpdateSourceTrigger=PropertyChanged`, explicit `UpdateSource()`, formatted sibling bindings, and dependent target refresh during `Application.Run()`. ProGPU/Silk.NET should deliver text input and render the updated values while WPF owns binding expressions, source updates, and notification fan-out.
- Keep default-item binding validation in WPF's validation manager. The generated switch-only app now validates a default-discovered `ValidationRule`, XAML `Binding.ValidationRules`, rejected source updates with `Validation.GetHasError(...)`, accepted source updates, and dependent target refresh during `Application.Run()`. ProGPU/Silk.NET should render validation-driven visual state and deliver input; it should not duplicate validation rules, validation error storage, or source-update policy.
- Keep default-item `DataTrigger` evaluation in WPF's style/trigger managers. The generated switch-only app now validates a keyed `Style`, default setter value, binding-driven `DataTrigger` activation, and trigger exit when the source changes during `Application.Run()`. ProGPU/Silk.NET should render resolved dependency-property values and observe invalidation, not own style trigger state machines.
- Keep default-item property `Trigger` evaluation in WPF's style/trigger managers as well. The generated switch-only app now validates a keyed `Style` with an `IsEnabled=false` trigger, initial setter state, trigger activation, and trigger exit during `Application.Run()`. ProGPU/Silk.NET should provide input/property invalidation and render the resolved values, not duplicate property trigger state.
- Keep default-item `ControlTemplate` loading in WPF's template manager. The generated switch-only app now validates a keyed `Button` style with a local `ControlTemplate`, named template parts, `TemplateBinding` content/background, and template trigger enter/exit behavior during `Application.Run()`. ProGPU/Silk.NET should render the realized template visuals and invalidated property values, not own template namescopes, template binding, or template trigger evaluation.
- Keep default-item selector binding in WPF's selector/item-control managers. The generated switch-only app now validates `ComboBox.ItemsSource`, `DisplayMemberPath`, `SelectedItem` two-way binding, collection updates, view-model-driven selection, and control-driven source updates during `Application.Run()`. ProGPU/Silk.NET should deliver pointer/keyboard/text input and render selected visual state, not own selected-item synchronization or item-view state.
- Keep default-item converter and `MultiBinding` behavior in WPF's binding/resource managers. The generated switch-only app now validates default-discovered `IValueConverter` and `IMultiValueConverter` resources, converter invocation for selected-item binding, and composite status/selection multi-binding updates during `Application.Run()`. ProGPU/Silk.NET should render converted values and observe invalidation, not duplicate converter lookup or binding aggregation.
- Keep default-item routed command and input-binding metadata in WPF's command manager. The generated switch-only app now validates `Window.CommandBindings`, `KeyBinding`, `Button.Command`, `RoutedUICommand.CanExecute`, execution routing, and command parameter flow during `Application.Run()`. ProGPU/Silk.NET should deliver native keyboard/mouse input into WPF and render command-driven state changes, not duplicate command tables or require app-side command rewrites.
- Keep the SDK nupkg layout itself in the migration-contract lane. The external SDK smoke now preflights `ProGPU.Wpf.Sdk.11.0.0-dev.nupkg` before app creation and validates the `MSBuildSdk` nuspec type, root `Sdk/` imports, portable WPF markup defaults, ProGPU/Silk.NET/support package references, automatic bootstrap source, managed/native runtime copy targets, and absence of accidental `lib`, `ref`, `build`, `buildTransitive`, `contentFiles`, or `tools` payloads. The dedicated SDK CI gate now promotes this package preflight together with the external app smoke so SDK layout regressions fail before a user sees restore/build noise.
- Keep the SDK package/smoke validation in CI. `eng/progpu-wpf-sdk-ci.sh` refreshes the exact local nupkgs consumed by `ProGPU.Wpf.Sdk`, runs the package-mode SDK switch build/runtime smoke, runs the generated external no-source-change SDK smoke, and runs the focused graph guard. `.github/workflows/progpu-wpf-sdk.yml` executes that gate on macOS for the port branch so SDK layout, transport package identity, ProGPU package identity, runtime asset copy, and external migration-contract regressions are caught before merge.
- Keep the SDK smoke render-offset guard at the ProGPU logical/physical render boundary. Both package-mode SDK harnesses now render a full `420x840` DIP retained WPF rectangle into an `840x1680` physical target and require red pixels at upper-left and lower-right interior positions, so stale viewport, retained-layer scale, or positive-offset regressions fail in package validation instead of relying on visual screenshots. WPF should still own layout/client-size negotiation; ProGPU/Silk.NET should own the swapchain viewport, DPI scale, and retained-layer placement.
- Keep the packaged host startup-resize path in the same SDK gate. The runtime and external package-mode harnesses now instantiate the shipped `ProGpuWpfWindowHost`, resolve a `2.0` Retina fallback through `ProGPU.Backend.DisplayScaleResolver`, replay an `840x1680` native resize for a `420x840` DIP window, and assert logical host dimensions plus render-surface geometry remain `420x840` DIPs over `840x1680` physical pixels. Future ProGPU branch work should improve that native host/display-scale boundary directly rather than compensating for Retina offsets in managed WPF layout code.
- Keep WPF transport package freshness in the same SDK gate. The package-mode runtime and generated external smokes now compare required `Microsoft.DotNet.Wpf.GitHub` assemblies from local nupkgs and package-built output against available repository-built WPF transport assemblies, because a stale `PresentationCore` or `PresentationFramework` can reproduce upper-left/offset smoke rendering even when the ProGPU compositor package and source-level HiDPI pixel tests are current.
- Build the managed WPF transport payload from source before packaging it in the SDK CI gate. The gate now rebuilds the source `WindowsBase`, `System.Xaml`, UIAutomation, input, `PresentationCore`, `ReachFramework`, `PresentationUI`, `PresentationFramework`, the full managed theme assembly set (`Aero`, `Aero2`, `AeroLite`, `Classic`, `Fluent`, `Luna`, `Royale`), and `System.Windows.Controls.Ribbon` before packing `Microsoft.DotNet.Wpf.GitHub.11.0.0-dev`, so no-source-change SDK validation exercises current core/framework/XAML/theme/Ribbon output instead of stale Release artifacts.
- Run the real managed WPF harnesses before packing the SDK. The SDK CI gate now requires the compiled-XAML, XAML runtime, real `Application.Run`, and Fluent theme runtime harnesses to pass against the source-built transport payload in Release before package-mode app validation starts. Keep extending these harnesses when porting WPF core/framework/XAML/theme/compiler manager code, because they exercise reused managed WPF code directly while the SDK apphost smokes exercise the final ProGPU/Silk.NET user deliverable.
- Keep public rich-text editing commands in the external SDK migration-contract lane. The generated no-source-change app now validates `RichTextBox.Selection`, `EditingCommands.ToggleBold`, `ToggleItalic`, `ToggleUnderline`, paragraph alignment commands, and list marker commands from package-built output; ProGPU/Silk.NET should render the mutated rich document state and provide native text/input services, not own a parallel rich-text command manager.
- Keep rich-text clipboard commands on that managed WPF lane. The runnable MVP app now validates `ApplicationCommands.Copy` and `ApplicationCommands.Paste` against a compiled `RichTextBox`, so ProGPU/Silk.NET should keep improving text rendering/input/native clipboard plumbing behind WPF's `TextEditorCopyPaste` path rather than introducing app-facing copy/paste APIs.
- Treat text-editor command availability as a managed WPF responsibility even when native clipboard state is absent. `TextEditorCopyPaste.GetPasteApplyFormat(...)` now returns no format for a null portable clipboard data object, so ProGPU/Silk.NET only needs to supply native clipboard contents when available and should not paper over empty-clipboard command-state behavior in the platform host.
- Keep plain `TextBox` editing commands in the same managed text-editor lane. The runnable MVP app now validates `ApplicationCommands.Copy`, `Cut`, `Paste`, `Undo`, and `Redo` behavior against a compiled `TextBox` after its stock template creates `PART_ContentHost`, so ProGPU/Silk.NET should deliver native key/text/clipboard events into WPF's existing text editor instead of owning a parallel text editing command stack. Undo-unit creation remains tied to WPF's managed `TextView`/`ChangeBlockUndoRecord` lifetime rather than a platform-host shortcut.
- Keep external SDK `BindingGroup` coverage in the migration-contract lane. The generated external app now validates group-level rules, proposed values, rejected and accepted `CommitEdit()` calls, validation error state, and explicit source updates from a project that only changes `Project Sdk`; future ProGPU work should render the resulting WPF validation visuals and leave `BindingGroup`, binding expressions, validation-rule execution, and source update ownership in WPF.
- Keep external SDK binding transfer-event coverage in the migration-contract lane. The generated external app now validates `NotifyOnSourceUpdated`, `NotifyOnTargetUpdated`, `SourceUpdated`, `TargetUpdated`, explicit `UpdateTarget()`/`UpdateSource()` behavior, and `DataTransferEventArgs` sender/target/property/routed-event metadata from a project that only changes `Project Sdk`; future ProGPU work should consume the resolved dependency-property state and render invalidations while leaving binding transfer routing and expression metadata in WPF.
- Keep external SDK binding fallback/null substitution in the managed binding lane. The generated no-source-change app now validates `FallbackValue` for a missing binding path and `TargetNullValue` for a null source property from SDK-built BAML; future ProGPU/Silk.NET work should render the resolved target values and leave fallback policy in WPF's binding engine.
- Keep external SDK delayed source-update coverage in the managed binding lane. The generated no-source-change app now validates a `TextBox` binding with `UpdateSourceTrigger=PropertyChanged` and `Delay=25`, including delayed source-update behavior after dispatcher drain; future ProGPU work should render resolved values and observe WPF invalidation without owning binding timers.
- Keep external SDK binding-mode coverage in the managed binding lane. The generated no-source-change app now validates `Mode=OneTime` retention and `Mode=OneWayToSource` target-to-source updates from SDK-built BAML; future ProGPU work should render resolved values without owning WPF binding-mode policy.
- Keep external SDK indexed `PropertyPath` coverage in the managed binding lane. The generated no-source-change app now validates `ExternalIndexedItems[1].Name` path metadata and item-property notification from SDK-built BAML; future ProGPU work should render updated text without parsing indexed binding paths.
- Keep external SDK `Binding.StringFormat` coverage in the managed binding lane. The generated no-source-change app now validates direct binding string-format output and metadata from SDK-built BAML; future ProGPU work should render formatted text supplied by WPF without duplicating binding formatting.
- Keep external SDK `RelativeSource PreviousData` coverage in the managed binding/item-template lane. The generated no-source-change app now validates first-item fallback plus second/third previous-item lookup from SDK-built item containers after an `ObservableCollection` mutation; future ProGPU/Silk.NET work should render the realized item visuals without owning previous-item lookup or item-template binding context.
- Keep external SDK missing-resource behavior in the managed resource lane. The generated no-source-change app now validates `TryFindResource(...)` null return and `FindResource(...)` `ResourceReferenceKeyNotFoundException` key preservation from SDK-built output; future ProGPU work should render resolved resources and observe WPF invalidations without adding a parallel app resource resolver.
- Keep external SDK runtime dynamic-resource expressions in the managed resource lane. The generated no-source-change app now validates `SetResourceReference(...)` on a runtime-created `TextBlock` and application-resource mutation propagation from SDK-built output; future ProGPU work should render the updated brush and consume WPF invalidation without owning dynamic-resource expression lookup.
- Keep external SDK XAML language intrinsic coverage in the managed XAML/compiler lane. The generated no-source-change app now compiles an `x:Null` property value and an `x:Array` resource, resolves the produced string array through application resources, and binds the array to an `ItemsControl`; future ProGPU work should render realized items and null-driven dependency-property state without owning XAML intrinsic object construction.
- Keep external SDK loose `XamlWriter` style/template/element/document/theme-key coverage in the migration-contract lane. The generated external app now validates system resource-key dictionary serialization, `MenuItem.SeparatorStyleKey` round-tripping, style dictionary serialization, `BasedOn`, setter values, `ControlTemplate` triggers, `DataTemplate` and `HierarchicalDataTemplate` triggers, hierarchical `ItemsSource`, `ItemsPanelTemplate` generated panel metadata, `GroupStyle` header and panel metadata, named template parts, generated content names, live `FrameworkElement` child graphs, live `FlowDocument` rich text graphs, dependency-property values, and template application from a project that only changes `Project Sdk`; future ProGPU work should render the resulting template, element, and document visuals and leave XAML serialization, system/theme resource-key conversion, style resource resolution, item-template metadata, item-panel and group-style metadata, element child-content reconstruction, rich-document text-container reconstruction, template namescopes, and trigger metadata in WPF.
- Keep external SDK runtime namescope coverage in the migration-contract lane. The generated no-source-change app now validates `RegisterName`, duplicate-name rejection with original mapping retention, `UnregisterName`, and replacement registration against the BAML-created window namescope; future ProGPU work should render and invalidate the resolved tree while leaving runtime namescope ownership in WPF.
- Keep external SDK `Application.LoadComponent(...)` coverage in the migration-contract lane. The generated no-source-change app now loads a default-item compiled `UserControl` by component URI, then validates app-resource lookup, generated namescope lookup, and referenced-library control instantiation; future ProGPU work should render the loaded view while leaving BAML URI resolution and generated component creation in WPF.
- Keep external SDK collection-view coverage in the migration-contract lane. The generated external app now validates `CollectionViewSource` sorting/grouping and `GroupStyle.HeaderTemplate` from a project that only changes `Project Sdk`; future ProGPU work should render the realized grouped item visuals and leave collection-view creation, sort/group descriptions, group collections, and grouped-header template selection in WPF.
- Keep external SDK item-container alternation coverage in the migration-contract lane. The generated no-source-change app now validates SDK-built `ListBoxItem` containers expose WPF-owned `ItemsControl.AlternationIndex` values after collection mutation and `Application.Run()`; future ProGPU/Silk.NET work should render the generated containers without maintaining alternation state.
- Keep external SDK multi-selection coverage in the migration-contract lane. The generated no-source-change app now validates `SelectionMode=Multiple`, generated `ListBoxItem.IsSelected` selection/unselection, `SelectedItems` membership/order, and `SelectionChangedEventArgs` added/removed payloads after `Application.Run()`; future ProGPU/Silk.NET work should render selected visuals and deliver input without owning selector state.
- Keep external SDK `x:Shared="False"` coverage in the migration-contract lane. The generated external app now validates distinct unshared brush instances from both `StaticResource` consumers and repeated `Application.Resources` lookup in a project that only changes `Project Sdk`; future ProGPU work should render the resolved brush state and leave shared/unshared resource policy in WPF.
- Keep external SDK `SystemParameters` coverage in the migration-contract lane. The generated external app now validates representative system metrics, boolean/value properties, and `SystemParameters.*Key` resource lookup from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should supply durable cross-platform values behind WPF's existing cache/resource-key surface instead of requiring controls or apps to bypass `SystemParameters`.
- Keep external SDK `MessageBox.Show(...)` coverage in the migration-contract lane. The generated external app now validates SDK bootstrap registration of WPF's portable message-box service, then installs a deterministic test handler for no-owner/owner overload fallback assertions from a project that only changes `Project Sdk`; ProGPU.Wpf now provides a process-backed native message-box adapter behind `IWpfMessageBoxService` for real app runs. Future ProGPU/Silk.NET work should improve only the native dialog implementation behind WPF's public message-box API.
- Keep external SDK launcher coverage in the migration-contract lane. The generated external app now validates SDK bootstrap registration of WPF's portable launcher service, then installs a deterministic test handler for URI/target-frame/top-level launch request assertions from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should improve only the native launcher implementation behind `IWpfLauncher` while leaving WPF `AppSecurityManager`, hyperlink, and navigation launch policy in managed WPF.
- Keep external SDK `Clipboard` coverage in the migration-contract lane. The SDK bootstrap now registers WPF's portable clipboard service against ProGPU's platform clipboard abstraction, and the generated external app validates normal WPF clipboard text/data-object/current/flush/clear behavior from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should improve native clipboard fidelity behind `IWpfClipboard` without requiring app code to bypass `System.Windows.Clipboard`.
- Keep the runnable MVP app on the same public Clipboard lane. The manual SDK app now exposes `Clipboard` text/data-object/current/flush behavior beside `DataObject` in the Editing tab, so quick app bring-up exercises the same service-backed WPF API that external no-source-change projects use.
- Keep external SDK common file/folder dialog coverage in the migration-contract lane. The generated external app now validates SDK-bootstrap portable file-dialog service availability and `OpenFileDialog`, `SaveFileDialog`, and `OpenFolderDialog` managed state from a project that only changes `Project Sdk`; ProGPU.Wpf now selects `zenity` with `kdialog` fallback on Linux behind `IWpfFileDialogService`. Future ProGPU/Silk.NET work should continue improving native picker fidelity behind that boundary without requiring app code to bypass `Microsoft.Win32` WPF dialog APIs.
- Keep external SDK custom routed-event coverage in the migration-contract lane. The generated external app now validates package-built custom bubble and tunnel `RoutedEvent` registration, compiled XAML handler hookup, `AddHandler` parent route delivery, `RaiseEvent(...)`, routing strategy metadata, and sender/original-source propagation from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should deliver native input into WPF's existing input/event route and avoid introducing a parallel routed-event manager.
- Keep external SDK dependency-property coverage in the migration-contract lane. The generated external app now validates inheritable attached properties, local value precedence, dependency-property coercion, `DependencyPropertyHelper.GetValueSource(...)`, coercion flags, and change callbacks from package-built XAML; future ProGPU/Silk.NET work should consume resolved dependency-property values and invalidation notifications instead of creating a parallel property system.
- Keep external SDK collection-view filter and currency coverage in the migration-contract lane. The generated external app now validates `CollectionViewSource.Filter`, filtered view refresh, synchronized current-item selection, and `ICollectionView.MoveCurrentToPosition(...)` from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render the realized item visuals and consume WPF selection/current-item state rather than adding native collection-view filtering or currency managers.
- Keep external SDK live collection-view coverage in the migration-contract lane. The generated external app now validates `CollectionViewSource` live filtering, live sorting, and live grouping over `INotifyPropertyChanged` items from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render the resulting item/container/group state and leave live view membership/order/group management in WPF.
- Keep external SDK implicit typed `DataTemplate` lookup in the managed template/resource lane. The generated no-source-change app now validates an unkeyed `DataTemplate` for `ExternalItem`, `DataTemplateKey` lookup, presenter content, and resolved template binding output from SDK-built BAML; future ProGPU/Silk.NET work should render WPF-selected template visuals instead of owning type-template selection.
- Keep external SDK implicit type-style lookup in the managed style/resource lane. The generated no-source-change app now validates a scoped unkeyed `TextBlock` style with normal and dynamic-resource setters from SDK-built BAML; future ProGPU/Silk.NET work should render resolved styled properties instead of owning type-style selection.
- Keep external SDK template-selector coverage in the migration-contract lane. The generated external app now validates compiled `DataTemplate` resources, `DataTemplateSelector` resource wiring, `ContentTemplateSelector`, `ItemTemplateSelector`, selected-template decisions, generated template binding text, and selector-backed item collection propagation from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render WPF-selected templates instead of selecting templates in platform/rendering code.
- Keep external SDK `ItemContainerStyleSelector` coverage in the same migration-contract lane. The generated external app now validates selector-owned `ListBoxItem` styles, compiled `ItemContainerStyleSelector` wiring, generated container tags, and item-template text after `Application.Run()` from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render WPF-selected containers instead of choosing container styles in platform/rendering code.
- Keep external SDK selector text-search metadata in the managed WPF lane. The generated no-source-change app now validates `IsTextSearchEnabled` and `TextSearch.TextPath` on a package-built `ComboBox`; future ProGPU/Silk.NET work should deliver native text/key input into WPF's existing selector/search path instead of introducing a parallel text-search policy.
- Keep external SDK validation routed-event coverage in the migration-contract lane. The generated external app now validates `NotifyOnValidationError`, `Validation.Error` handler hookup, `Validation.GetErrors(...)`, added/removed `ValidationErrorEventArgs`, routed-event identity, sender, action, and error content from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render validation state supplied by WPF rather than owning a parallel validation-event manager.
- Keep external SDK `IDataErrorInfo` coverage in the managed binding lane. The generated external app now validates `ValidatesOnDataErrors=True`, `IDataErrorInfo` error lookup, invalid-source update, recovery, and routed validation events from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render WPF-supplied validation state instead of owning data-error querying.
- Keep external SDK `INotifyDataErrorInfo` coverage in the managed binding lane. The generated external app now validates `ValidatesOnNotifyDataErrors=True`, `ErrorsChanged`-driven validation, `HasErrors`, invalid-source update, recovery, and routed validation events from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render WPF-supplied validation state instead of owning notify-data-error subscriptions.
- Keep external SDK `ExceptionValidationRule` and `UpdateSourceExceptionFilter` coverage in the same managed binding lane. The generated external app now validates exception-to-validation conversion, filter callback invocation, filtered error content, rejected source preservation, recovered source update, and added/removed `Validation.Error` routing from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should render the resulting validation state and leave exception handling policy in WPF's binding engine.
- Keep external SDK mouse input-binding coverage in the migration-contract lane. The generated external app now validates compiled `MouseBinding` and `MouseGesture` metadata plus routed command execution from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should deliver native mouse input into WPF's existing input-binding and command managers instead of asking apps to replace XAML mouse gestures.
- Keep external SDK class command/input binding coverage in the same migration-contract lane. The generated external app now validates `CommandManager.RegisterClassCommandBinding(...)`, `RegisterClassInputBinding(...)`, class-command routing from a compiled button, and class key-gesture execution through normalized portable key input after `Application.Run()` from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should deliver native key input into WPF's existing class binding tables instead of owning parallel command gesture registries.
- Keep the repo-local SDK switch smoke on the same mouse input-binding lane. The package-mode smoke app now declares a `LeftDoubleClick` `MouseBinding` beside the existing `KeyBinding`, and the runtime harness validates the loaded `MouseGesture`, command parameter, routed command identity, and command execution from shipped assemblies. This gives the fast SDK smoke a local guard before the larger generated external app runs.
- Keep external SDK virtualization metadata in the managed WPF layout/item-control lane. The generated no-source-change app now validates `VirtualizingPanel.IsVirtualizing`, `VirtualizingPanel.VirtualizationMode=Recycling`, logical scrolling, and a compiled `VirtualizingStackPanel` items panel from SDK-built output. Future ProGPU/Silk.NET work should render realized containers and supply input/scrolling integration, not replace WPF's virtualization policy or item-panel metadata.
- Keep the repo-local SDK switch smoke usable through direct `dotnet run` as well as separate build/harness steps. The app project owns the mutable `11.0.0-dev` cache refresh, while the referenced library no longer clears the shared repo-local package cache during restore; this prevents the SDK deliverable smoke from losing `Microsoft.DotNet.Wpf.GitHub` package assets between restore and apphost launch.
- Keep MessageBox, Clipboard, and common file/folder dialogs in WPF's public managed API surface while moving only the OS integration behind portable services. The SDK runtime harness now validates `MessageBox.Show(...)` no-owner and owner-window overloads, `Clipboard.Clear`, `SetText`, `ContainsText`, `GetText`, `GetDataObject`, `IsCurrent`, and `Flush`, plus no-owner and owner-window `OpenFileDialog`, `SaveFileDialog`, and `OpenFolderDialog` calls from SDK-produced package output. ProGPU/Silk.NET now provides process-backed message-box, clipboard, and file/folder dialog adapters behind those service registrations; it should not require application projects to replace `System.Windows.MessageBox`, `System.Windows.Clipboard`, or `Microsoft.Win32` dialog calls.
- Keep `System.Windows.SplashScreen` in the no-source-change SDK compatibility lane. Non-Windows now validates the compiled splash resource, including the real WPF `<SplashScreen Include="...">` build-action path that generates startup `Show(true)` code, and treats show/close as a no-op native presentation path so generated WPF startup code can run without Win32 layered windows or WIC. Future ProGPU/Silk.NET work should provide a reusable visible splash/native-startup window service behind this API, not require apps to replace `SplashScreen` with platform-specific code.
- Keep `System.Windows.Controls.PrintDialog` in the public WPF API compatibility lane. Non-Windows now preserves managed page-range/settings state, avoids Win32 print-dialog/default-printer probing, returns `false` from `ShowDialog()`, and exposes fallback printable-area defaults so apps can follow the normal `if (dialog.ShowDialog() == true) { ... }` pattern without crashing. Future ProGPU/Silk.NET or per-OS work should provide printer discovery, native print UI, and print-job submission behind the WPF API rather than requiring app projects to bypass `PrintDialog`.
- Keep `System.Printing` as a WPF transport/package-surface dependency while printing is incomplete. The SDK transport now ships the reference assembly and portable API-cycle runtime stub so public WPF printing signatures compile for SDK-switched apps; any use of missing Windows-only print-server/print-queue/XPS writer members must stay behind Windows-only no-inline helper methods until ProGPU/Silk.NET or per-OS platform services implement real printer discovery, print UI, and print submission. Document host templates should keep exposing the normal WPF print commands, but on non-Windows those commands must report unavailable before touching `System.Windows.Xps.XpsDocumentWriter` or print-state fields, including indirect command-query paths such as button template hookup.
- Keep `SystemParameters` and system resource-key lookup in WPF's managed theme/control surface while moving durable metric sources behind platform services. The SDK runtime and external SDK harnesses now validate representative metrics and `SystemParameters.*Key` resource lookup from SDK-produced package output, including focus borders, primary screen size, work area, scrollbar metrics, caret width, high contrast, drop shadow, wheel scroll lines, menu/UI effect policies, foreground flash count, drag-full-window state, and tooltip popup animation resource paths. ProGPU/Silk.NET should supply monitor/DPI/theme/high-contrast/animation-policy data behind this boundary rather than requiring apps or controls to bypass WPF's `SystemParameters` cache/resource-key model.
- Keep resource lookup, `DynamicResource` expression invalidation, merged dictionaries, and generated BAML resource-provider behavior in managed WPF. The SDK runtime harness now mutates `Application.Resources` during the portable `Application.Run()` smoke and validates existing dynamic-resource expressions on generated elements update after dispatcher draining. The external SDK gate also validates late `Application.Resources.MergedDictionaries` attachment and subsequent merged-dictionary mutation for BAML-created dynamic text/brush resources. ProGPU/Silk.NET should receive the changed brush/property state through WPF's normal visual/resource invalidation flow rather than duplicating resource dictionaries or expression invalidation.
- Keep `Application` startup, exit, current-instance, shutdown-mode, main-window, window-collection, owner/owned-window, modal dialog, and window close sequencing state in managed WPF. The SDK smoke now wires `Startup`, `Exit`, `Closing`, and `Closed` handlers from XAML, and the runtime/external harnesses validate generated handler hookup, event counts, startup args, exit code, `Application.Current`, default `ShutdownMode`, public `Application.Windows` startup-window membership, secondary-window tracking/removal, default `OnLastWindowClose` retention while the main window remains open, portable active-window acceptance in `Window.Owner`, `Owner`/`OwnedWindows` tracking, parent-close owned-window cleanup with ignored child cancellation, modal `ShowDialog()` result/dispatcher-frame behavior, canceled main-window close, `ShutdownMode.OnMainWindowClose`, `Application.MainWindow`, and cleared post-shutdown lifetime state after portable `Application.Run()`. ProGPU/Silk.NET should supply the native run-loop boundary, not a replacement app lifetime/window collection manager.
- Keep startup-time `Application.Resources` mutation in managed WPF. The SDK smoke now adds brush/text resources during `Application.Startup`, consumes them from the startup window through `DynamicResource`, and validates the resolved values after portable `Application.Run()`. ProGPU/Silk.NET should render the resulting property values without requiring an app-side ProGPU resource registration path.
- Keep Freezable resource lifecycle in managed WPF. The SDK smoke now validates BAML-created `SolidColorBrush` and `LinearGradientBrush` resources through `CanFreeze`, `Freeze()`, `IsFrozen`, deep `Clone()`, `CloneCurrentValue()`, frozen gradient stop collections, and mutable cloned `GradientStop` offsets. ProGPU should consume resolved brush/gradient state and own native gradient rendering, but it should not replace WPF's Freezable graph or clone/freeze semantics.
- Keep external SDK Freezable coverage in the migration-contract lane. The generated external app now validates `SolidColorBrush` and `LinearGradientBrush` resource freezing, deep clone mutability, gradient-stop collection freezing, and `CloneCurrentValue()` from a project that only changes `Project Sdk`; future ProGPU work should render the resolved brush/gradient state and leave Freezable lifecycle semantics in WPF.
- Keep default-item SDK Freezable and `x:Shared="False"` coverage in the same migration-contract lane. The generated no-explicit-item app now validates compiled resource freezing/cloning plus distinct unshared brush instances from a project that only changes `Project Sdk`, so ProGPU/Silk.NET should render resolved brush state while WPF owns resource sharing and Freezable lifecycle.
- Keep external SDK managed imaging coverage in the migration-contract lane. The generated external app now validates raw in-memory `BitmapSource.Create(...)`, `BitmapFrame.Create(BitmapSource)`, `CopyPixels(...)`, predefined `BitmapPalettes`, `BitmapPalette(BitmapSource, maxColorCount)`, `BmpBitmapEncoder.Save(Stream)`, `BitmapDecoder.Create(Stream, ...)` for BMP streams, direct `BmpBitmapDecoder(Stream, ...)`, local-file BMP `BitmapDecoder.Create(Uri, ...)`, direct `BmpBitmapDecoder(Uri, ...)`, application pack-resource PNG `BitmapDecoder.Create(Uri, ...)`, direct `PngBitmapDecoder(Uri, ...)`, `BitmapImage(Uri)`, compiled-XAML `Image.Source` and `ImageBrush.ImageSource`, code-created `Image.Source` and `ImageBrush.ImageSource`, and `WriteableBitmap.WritePixels(...)` from a project that only changes `Project Sdk`, including an `Indexed8` BMP palette round-trip, and `PresentationCore` now backs those raw in-memory sources, predefined/source-derived palettes, and frame wrappers with managed state off Windows instead of loading MIL/WIC. The first non-Windows encoder path writes BMP files from managed in-memory frames through the WPF encoder API, and the first non-Windows decoder paths create managed frames from 1/4/8/24/32-bit BI_RGB BMP streams, local file and WPF application pack-resource URIs, common PNG including Adam7 interlace, JPEG/ICO inputs, multi-frame GIF files with WIC-compatible frame delay/disposal metadata, and baseline uncompressed multi-frame TIFF files with `/ifd/{ushort=274}` orientation metadata while preserving the public WPF decoder API shape; future ProGPU/platform work should replace WMP codecs, compressed TIFF, animated GIF playback/frame caching beyond public frame-control metadata, HTTP image URI download/cache behavior, bitfield/RLE BMP variants, texture-backed bitmap resources, color management, broader metadata/cache state, and upload/rendering internals behind the WPF bitmap APIs without requiring app source changes.
- Keep SDK loose XAML loading and serialization in managed WPF. The SDK runtime harness now validates `XamlReader.Parse(...)` for local resources, styles, `StaticResource`, namescope lookup, and `RelativeSource Self` binding, plus `XamlWriter.Save(...)` round-tripping for a `LinearGradientBrush` and gradient stops from SDK-produced package artifacts. ProGPU/Silk.NET should not introduce a parallel XAML parser/serializer or require app source changes for loose XAML; missing acceleration should go into native rendering/vector/text/image/shader/platform APIs.
- Keep external SDK loose XAML coverage in the migration-contract lane. The generated external app now validates runtime `XamlReader.Parse(...)` and `XamlWriter.Save(...)` from a project that only changes `Project Sdk`; future ProGPU work should render the parsed/serialized object graphs and leave runtime XAML parser/writer, namescope, resource, binding, style, and Freezable serialization semantics in WPF.
- Keep external SDK VisualStateManager coverage in the migration-contract lane. The generated external app now validates `VisualStateManager.VisualStateGroups`, BAML-created `VisualState`/`Storyboard`/`DoubleAnimation` metadata, and loaded `GoToState(...)` Pressed/Normal transitions from a project that only changes `Project Sdk`; future ProGPU work should render the resulting dependency-property changes and leave visual-state, storyboard, and timeline ownership in WPF.
- Keep ProGPU shader verification process-safe until the native diagnostics API is available. The current wgpu-native build aborts from `wgpuShaderModuleGetCompilationInfo`, so `WgpuContext.VerifyShaderModule(...)` must not call it on the WPF SDK path. ShaderEffect and generated-shader work should continue moving native rendering into ProGPU shaders, but diagnostics should come from pipeline creation/device error callbacks or a supported backend API rather than a managed workaround that risks crashing SDK-switched apps.
- Keep external SDK toolbar/status, password, date-selection, and range-control coverage in the migration-contract lane. The generated external app now validates `ToolBarTray`/`ToolBar`, `StatusBar`, `PasswordBox`, `Calendar`, `DatePicker`, `RepeatButton`, `ScrollBar`, `Slider`, and `ProgressBar` from a project that only changes `Project Sdk`, including repeat-button click routing and scroll command/event delivery; future ProGPU work should provide native input/rendering/platform integration and leave command routing, status content, password state, selected-date collections, range events, scroll commands, and element-name binding in WPF.
- Keep external SDK `Thumb` drag coverage in the migration-contract lane. The generated external app now validates direct `DragStarted`/`DragDelta`/`DragCompleted` handlers plus bubbled attached `Thumb.DragDelta` routing from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should feed native pointer/capture state into WPF's existing drag manager instead of adding a parallel drag-event table.
- Keep external SDK `GridSplitter` coverage in the migration-contract lane. The generated external app now validates splitter grid metadata before show and WPF class-handler column resizing after portable `Application.Run()` from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should deliver pointer/capture input and render invalidations while WPF keeps splitter layout ownership.
- Keep SDK access-key manager coverage in the migration-contract lane. The generated external app validates `Label.Target` resolution, standalone `AccessText` content, `AccessKeyManager` registration, key processing, and focus transfer from a project that only changes `Project Sdk`; the runnable MVP app now validates the same live access-key focus transfer for its compiled input label during `Application.Run()`. Future ProGPU work should deliver normalized native key/text input and leave label targets, access text metadata, mnemonic routing, and access-key manager ownership in WPF.
- Keep SDK keyboard-navigation coverage in the migration-contract lane. The generated external app validates nested `KeyboardNavigation.TabNavigation="Cycle"` metadata plus `Keyboard.Focus(...)` and `MoveFocus(new TraversalRequest(...))` next/previous cycle behavior from a project that only changes `Project Sdk`; the runnable MVP app now drives the same focus traversal on its Input tab during `Application.Run()`. Future ProGPU work should deliver native focus/key events and leave traversal policy, tab loops, and logical focus movement in WPF.
- Keep external SDK `InputScope` coverage in the migration-contract lane. The SDK smoke and generated external app now validate compiled BAML and loose `XamlReader.Parse(...)` `InputMethod.InputScope` metadata, including `InputScopeName`, `InputScopePhrase`, `RegularExpression`, `SrgsMarkup`, and WPF's non-generic names/phrase collections from projects that only change `Project Sdk`; future ProGPU/Silk.NET work should consume that metadata behind IME/touch-keyboard platform services rather than requiring app or control code to bypass WPF's input-scope APIs.
- Keep external SDK `InputLanguageManager` and `InputMethod` state coverage in the migration-contract lane. The generated external app now validates compiled input-language and preferred-IME attached metadata plus portable non-Windows manager state mutation/defaults from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should consume those WPF-owned language and IME hints behind native IME/language-profile services rather than replacing the WPF managers.
- Keep `SpellCheck`, `SpellingReform`, custom dictionary collections, and public `TextBox` spelling-query APIs in WPF's managed text manager. Non-Windows now uses a no-op speller interop so SDK-switched apps can enable spell checking and keep querying WPF's public spelling APIs without crashing when WinRT/NLG proofing services are unavailable. Future ProGPU/Silk.NET work can provide native/platform proofing services behind this interop boundary, but app code should not bypass WPF's spell-check attached properties or text-manager APIs.
- Keep external SDK drag/drop coverage in the migration-contract lane. The generated external app now declares normal WPF `AllowDrop`, `PreviewDragEnter`, `DragEnter`, `PreviewDragOver`, `DragOver`, `PreviewDragLeave`, `DragLeave`, `PreviewDrop`, and `Drop` handlers, then drives the portable activation-service `ProcessDragDropEvent(...)` path with file and text payloads from a project that only changes `Project Sdk`; the smoke validates WPF routed-event delivery, `IDataObject` file/text formats, allowed-effect metadata, handler-selected effects, fallback accepted effects, and DIP drop coordinates. Future ProGPU/Silk.NET work should deliver native file/text drag payloads into WPF's existing `DragDrop` routed-event manager, not create a parallel app-facing drop API.
- Keep external SDK adorner coverage in the migration-contract lane. The generated external app now validates `AdornerDecorator`, managed `Adorner` subclass creation, `AdornerLayer` lookup, and add/get/remove behavior from a project that only changes `Project Sdk`; future ProGPU work should render adorner visuals and leave adorner-layer ownership in WPF.
- Keep external SDK UI Automation pattern coverage in the migration-contract lane. The generated external app now validates WPF peer-provided `Invoke`, `Value`, `Toggle`, and `RangeValue` patterns for package-built button, text-box, check-box, and slider controls from a project that only changes `Project Sdk`, while WPF suppresses provider-side `UIAutomationCore` listener probing on non-Windows until a native accessibility bridge exists; future ProGPU/Silk.NET work should bridge these WPF peers/providers to native accessibility stacks instead of duplicating WPF's command, value, toggle, or range state.
- Keep binding fallback selection in managed WPF. The SDK smoke now covers compiled `PriorityBinding` with a missing first path and a valid fallback path, and the runtime harness verifies the `PriorityBindingExpression`, child binding count, active binding expression, and active fallback path. ProGPU/Silk.NET should consume resolved dependency-property values and invalidation notifications rather than adding a parallel priority-binding engine.
- Keep external SDK `TextBox` editing coverage in the migration-contract lane. The generated external app now validates `TextChanged`, validation failure/recovery, selected text, selection start/length, selected-text replacement, caret positioning, `AppendText(...)`, `IsUndoEnabled`/`UndoLimit` and empty undo/redo behavior for programmatic edits, and explicit binding source updates from a project that only changes `Project Sdk`; future ProGPU/Silk.NET work should improve native text-input delivery and rendering while leaving text-box selection/editing/undo/validation/binding ownership in WPF.
- Keep relative-source lookup in managed WPF. The SDK smoke now covers compiled `{RelativeSource Self}`, compiled `{RelativeSource AncestorType={x:Type Border}}` ancestor binding, and compiled `{RelativeSource TemplatedParent}` binding inside a `ControlTemplate`, and the runtime harness validates the `BindingExpression`, binding path, relative-source mode/type metadata, and resolved values after portable dispatcher draining. ProGPU/Silk.NET should not duplicate self, logical/visual tree, or templated-parent binding lookup.
- Keep theme component-resource-key lookup in managed WPF. The SDK smoke default `Themes/Generic.xaml` now declares a `ComponentResourceKey` brush for the custom themed control and consumes it through a `DynamicResource` inside the default `ControlTemplate`; the runtime harness applies the template and validates the resolved brush and border metadata. ProGPU/Silk.NET should render resolved theme values rather than adding a parallel theme-resource-key lookup path.
- Keep `Generic.xaml` default-template serialization in managed WPF as well. The external package-mode SDK smoke now serializes the referenced library's loaded themed `ControlTemplate` with `XamlWriter.Save(...)`, parses it back, applies it to a new custom control, and validates `TemplateBinding` plus `ComponentResourceKey` dynamic-resource lookup. This keeps default-style probing, template object graphs, and XAML serialization in WPF's framework/tooling layer instead of adding ProGPU-specific theme/template export code.
- Keep implicit typed data-template lookup in managed WPF. The SDK smoke now declares an unkeyed `DataTemplate` for the smoke item type and presents an item without an explicit template; the runtime harness validates the generated `DataTemplateKey`, binding metadata, presenter content, and resolved template text after portable dispatcher draining. ProGPU/Silk.NET should render realized template visuals instead of selecting templates in native/platform code.
- Keep implicit type-style lookup in managed WPF. The SDK smoke now declares a scoped unkeyed `Style TargetType="{x:Type TextBlock}"` with normal setters and a dynamic-resource setter, and the runtime harness validates that the generated element receives the style, target type, setter values, and resolved brush. ProGPU/Silk.NET should consume resolved styled properties instead of adding a native type-style lookup path.
- Keep style `EventSetter` hookup in managed WPF. The SDK smoke now declares a `Button` style with a `Click` event setter, and the runtime harness validates the BAML-created `EventSetter`, WPF `ButtonBase.Click` routed-event metadata, and generated handler invocation through `ButtonBase.OnClick()`. ProGPU/Silk.NET should provide native input events and render resulting state, not attach style handlers in a native side table.
- Keep style `DataTrigger` and `MultiDataTrigger` evaluation in managed WPF. The external SDK smoke now declares `TextBlock` styles with binding-driven single-condition and multi-condition triggers, drives them through `INotifyPropertyChanged` from SDK-built BAML, and validates inactive, partial-match, active, and exit states. ProGPU/Silk.NET should render the resulting dependency-property values, not duplicate WPF binding-trigger state machines.
- Keep style trigger actions in managed WPF. The external SDK smoke now declares `DataTrigger.EnterActions` and `DataTrigger.ExitActions` with `BeginStoryboard`/`Storyboard` opacity animations, validates the BAML action/timeline graph, then toggles the bound property during real `Application.Run()` and verifies enter/exit property-clock values. ProGPU/Silk.NET should render the resulting properties, not own trigger action dispatch or timeline clocks.
- Keep multi-condition trigger actions in managed WPF. The external SDK smoke now declares `MultiDataTrigger.EnterActions` and `MultiDataTrigger.ExitActions`, validates both bound conditions plus the action/timeline graph, verifies a partial-condition state remains inactive, and then verifies enter/exit property-clock values after both conditions are toggled during real `Application.Run()`.
- Keep storyboard/event-trigger hookup in managed WPF. The SDK smoke and external SDK smoke now declare `Loaded` `EventTrigger` entries with `BeginStoryboard`, `Storyboard`, target-name/property metadata, and zero-duration `DoubleAnimation` timelines; the harnesses validate both BAML metadata and post-`Loaded` opacity application after portable dispatcher draining or real `Application.Run()` startup. ProGPU/Silk.NET should provide render wakeups and render changed dependency-property values, not own WPF timeline parsing, namescope target resolution, property-clock application, or event-trigger action tables.
- Extend the SDK-only smoke coverage by manager area, not by ProGPU-side replacements. The current menu, context-menu, tooltip, button-toggle-choice, password/date-selection, selector, toolbar, status-bar, content, scroll, range, layout-panel, item-presentation, list-view, and grid slices validate `Menu`/`MenuItem` hierarchy loading, separator realization, command item metadata/execution, `ClickEvent` routing, checkable `MenuItem.IsChecked`/`Checked`/`Unchecked` state, `ContextMenu` item loading, tooltip content/placement, `CheckBox.IsChecked` and checked/unchecked routing, `RadioButton.GroupName` mutual exclusion, `PasswordBox.Password`/`SecurePassword` plus `PasswordChanged`, `Calendar` display/selected-date metadata, `DatePicker` selected-date metadata, `ComboBox.DisplayMemberPath`/`SelectedValuePath`/`SelectedValue` behavior, `TabControl.SelectedItem` state, generated `SelectionChanged` handlers, `ToolBarTray`/`ToolBar` item hosting, toolbar routed-command execution, `ToggleButton.IsChecked` state, `StatusBar` item hosting, `StatusBarItem`/status text binding, `GroupBox` content hosting, `Expander.IsExpanded` and expanded/collapsed routed events, `ScrollViewer` content and scrollbar visibility metadata, `RepeatButton` routed click delivery, `ScrollBar` range/viewport metadata, routed scroll commands and `ScrollEvent` values, `Slider.ValueChanged`, `ProgressBar.Value` element-name binding, `Grid` row/column metadata, `DockPanel.Dock`, `Canvas.Left`/`Top`, `UniformGrid` rows/columns, `ItemsPanelTemplate` loading, `ItemContainerStyle` metadata, alternation count, item string formatting, `ListView`/`GridView` display-member column binding metadata, list-view selected-item state, list-view item collection propagation, `DataGrid` explicit text/checkbox column binding metadata, selected row state, and data-grid item collection propagation through WPF's managed control, event, command, selector, layout, item-control, popup-service, content, scroll, range, item-container, item-panel, list-view, grid-view, data-grid, binding, and collection managers. The current package-mode renderer fix keeps `ProGpuCompositionCommandSink.DrawGeometry(...)` on replay/reflection bounds, transforms, path figures, path segments, and reflection-only shim geometry draw fallback instead of direct shim `Geometry.Bounds`, `Transform.Value`, `PathGeometry.Figures`, or `Geometry.Draw(...)` metadata, matching the rule that SDK-switched apps should load real WPF assemblies and use ProGPU only at the rendering/platform boundary.
- Add focus/access-key/keyboard-navigation metadata to the same SDK-only manager coverage lane. WPF should continue owning `FocusManager` focus scopes, logical focused elements, `Label.Target`, `AccessText`, `KeyboardNavigation` traversal metadata, keyboard navigation, and access-key routing; ProGPU/Silk.NET should provide native focus notifications, normalized keyboard/text input, and rendered output only. The SDK smoke now has a focused package-mode slice for `FocusManager.FocusedElement`, `FocusManager.IsFocusScope`, `KeyboardNavigation.TabNavigation`, `KeyboardNavigation.ControlTabNavigation`, `KeyboardNavigation.DirectionalNavigation`, label target binding, and access-text content so future ProGPU branch work does not drift into parallel focus/access-key/navigation state.
- Keep `CommandManager.RequerySuggested` in that SDK-only command-manager lane as well. The repo-local SDK smoke now validates package-built `ICommand.CanExecuteChanged` delegation through WPF requery, `CommandManager.InvalidateRequerySuggested()`, dispatcher-drained button enablement, and command execution, matching the external SDK gate. Future ProGPU branch work should deliver input/render wakeups and avoid owning a parallel command-source refresh policy.
- Keep rich document graph and host-control ownership in WPF while expanding ProGPU text/rendering support. The SDK smoke now validates SDK-produced BAML for `FlowDocument` paragraphs, sections, hyperlinks, inline UI containers, lists, tables, block UI containers, and `Hyperlink.RequestNavigate` routing through WPF's `System.Windows.Documents` model. The external SDK smoke also validates `FlowDocumentScrollViewer`, `FlowDocumentPageViewer`, and `FlowDocumentReader` host controls, including hosted `Document` graphs, zoom/viewing/toolbar metadata, document sizing metadata, list marker metadata, and text content. ProGPU branch work should focus on native text shaping, glyph/vector rendering, child-visual composition, input services, and navigation-host integration rather than replacing WPF's document tree, list/table models, inline/block collections, hyperlink routed events, or rich-document host managers.
- Prioritize replacing the native PTS/LineServices pagination dependency with a ProGPU/Silk.NET text-layout backend instead of adding managed app-side workarounds. `FlowDocumentPageViewer` now exposes that boundary in the external SDK lane: the portable `FlowDocumentPaginator` can keep SDK-switched apps alive and preserve managed object/API state without loading `PresentationNative_cor3.dll`, but real page formatting must be implemented behind the WPF paginator/document-viewer APIs so existing apps still use `FlowDocumentPageViewer`, `FlowDocumentReader`, and print/document commands unchanged.
- Treat the managed `SimpleTextLine` non-Windows retry in `TextFormatterImp` as a temporary bring-up bridge only. It keeps basic `TextBlock` template layout from loading native LineServices while SDK-smoke document hosts are instantiated, but the ProGPU branch should provide the full-performance text backend for wrapping, complex shaping, bidi, text decorations, glyph fallback, and line/page formatting rather than expanding managed approximations inside WPF.
- Keep `Frame`/`NavigationService` ownership in WPF while expanding native host-loop integration. The SDK smoke now validates compiled `Page` loading through `Frame.Source` plus `Navigating`, `Navigated`, and `LoadCompleted` event delivery, URI metadata, navigation mode, loaded content type, `Frame.Navigate(...)` to a second named compiled page, `FindName` namescope lookup after navigation, `CanGoBack`, and `Frame.GoBack()` journal return. The external SDK smoke also validates `NavigatingCancelEventArgs.Cancel` for a blocked `Frame.Navigate(...)` request, including unchanged content and journal state after cancellation. WPF's nested-BAML load detection now normalizes versioned and unversioned component resource URIs for the same assembly/resource so programmatic navigation does not duplicate generated page initialization. ProGPU/Silk.NET should supply windowing, input, render wakeups, and host navigation integration points; it should not replace WPF's page loading, namescope registration, cancellation, journal/event sequencing, or navigation event managers.
- Keep package-mode rendering bridges independent of shim-only WPF assembly APIs. The WPF transport package owns real `WindowsBase`/`PresentationCore`/`PresentationFramework` identities; ProGPU compatibility belongs behind native `ProGPU.Scene.DrawingContext` sinks, retained scene descriptors, SDK bootstrap, or ProGPU backend APIs. The legacy flat shim `DrawingContext` path may stay for early explicit `Draw` callbacks, but normal WPF root visual replay should continue to run against native ProGPU command sinks so existing apps do not need code changes or shim assembly references.
- Keep the custom SDK deliverable compatible with normal WPF project shape. Existing apps should be able to keep `OutputType=WinExe` and `UseWPF=true` after changing `Project Sdk` to `ProGPU.Wpf.Sdk`; the SDK should use `UseWPF=true` as WPF markup intent while internally suppressing the base WindowsDesktop framework reference and selecting the portable WPF transport plus ProGPU/Silk.NET package graph. Do not require app projects to add ProGPU-specific properties for ordinary WPF/XAML scenarios.
- Keep repo-local harness/analyzer settings outside SDK-switched app project files. The current smoke can use a sibling `Directory.Build.props` to satisfy this repository's build hygiene, but that must not become part of the customer migration contract. Any setting needed by ordinary WPF apps should move into `ProGPU.Wpf.Sdk`; any setting needed only by this repository should stay in repository infrastructure outside the app `.csproj`.
- Keep retained/effect/cache bridge contracts package-mode neutral. Bounds and other primitive state that cross the `ProGPU.Wpf` retained replay boundary should use adapter-owned value types or ProGPU scene structs instead of shim-owned WPF primitives; when real WPF values are reflected, read their properties by shape. Visual opacity masks, visual effects, visual cache scopes, and drawing-group cache/effect scopes should prefer native-bounds sink interfaces over managed `Rect?` overloads, with no-inline managed fallbacks kept cold for compatibility. This prevents SDK-switched apps from loading transport-package WPF assemblies and then JITting bridge metadata that points at shim-only `System.Windows` primitives.
- Keep package-mode primitive and text replay free of shim-only WPF media APIs. The native decoder should pass neutral primitive payloads and raw reflected resource objects into ProGPU command sinks; brushes, pens, and glyph runs should be lowered by property shape into ProGPU vector/text data until `ProGPU.Wpf` can compile directly against the ported WPF assembly identity. Do not require app code to reference shim `Brush.ToNative(...)`, `Pen.ToNative(...)`, or shim `GlyphRun` APIs.
- Keep WPF `ImageBrush`, `DrawingBrush`, and `VisualBrush` on the shared tile-brush replay path before generic media-brush lowering. Cross-assembly real WPF brush objects should be read by property shape, image sources should flow through the active `IWpfImageSourceAdapter`, and successful fills should replay into native ProGPU command sinks; only unsupported tile-brush state should fall back to stroke-only partial rendering or explicit unsupported accounting.
- Keep reflected real WPF transforms flowing into native ProGPU transform scopes when shim `Transform` type identity is unavailable. Object render-data replay, generated drawing contexts, drawing-group replay, and tile-brush replay should use `IWpfNativeTransformCommandSink` for property-shape transforms when available, and any local bridge `MatrixTransform` fallback must be created from reflected matrix values when a real PresentationCore-style assembly shadows `System.Windows.Media.MatrixTransform` with a non-assignable type, so cross-assembly PresentationCore objects do not lose transform state.
- Replace the local `PortableTextInterface` text stack with signed `ProGPU.Text` services incrementally: face-offset/table/cmap/glyph-metric discovery, the simple one-scalar-to-one-glyph fallback, and glyph-ID-preserving TrueType subsetting now use ProGPU-owned source-shared helpers. ProGPU also owns compact glyph-remapping TrueType subsetting, but WPF `GlyphTypeface.ComputeSubset` should remain glyph-ID-preserving until the embedding layer can carry the returned source-to-subset glyph map. Full shaping, fallback-chain policy, and CFF subsetting remain open text work.
- Use the portable WPF MCG path before editing generated render-data files by hand: `mcg.proj` can now run `Resources.rsp` and `Elements.rsp` through the Roslyn-backed `net10.0` CSP target on non-Windows, and the Elements XML serializer model is source-owned instead of regenerated by `xsd.exe`.
- Use the new object-sink render-data provider path as the immediate integration bridge for real WPF `PresentationCore`: `PushObjectSinkFactory(...)` can pass real WPF media objects to ProGPU.Wpf for reflection adaptation while the shim and real WPF type identities are still separate.
- Build on the real non-Windows `Window.Show`, `Hide`, `WindowState`, live `Title`/client-size updates, focus-derived `Activated`/`Deactivated`, `Closing` cancellation, routed keyboard/text/mouse/wheel promotion through `PortableWindowActivationService` and `InputManager`, compiled `KeyBinding` activation through the WPF `CommandManager`, basic text insertion through WPF `TextCompositionManager`/`TextEditorTyping`, first portable `ButtonBase.Click` activation plus `GotMouseCapture`/`LostMouseCapture` routed events and first `MouseWheel` routed-event activation through WPF `MouseDevice`, non-Windows portable keyboard/mouse devices, and `Application.Run` activation/run-loop boundary now provided by `PortableWindowActivationService` and `WpfPortableWindowActivation.TryRegisterPresentationFrameworkActivation(...)`. `PortablePresentationSource` now exposes registered keyboard and mouse input providers so `Keyboard.Focus(...)`, child `TextBox` focus after portable `Window.Show()`, cursor setting, `Mouse.Capture(...)`, and deactivation-driven capture cancellation can use the normal WPF provider contracts without `HwndSource`. Portable `Application.Run` now drains WPF dispatcher startup and shutdown work around the Silk.NET native loop so `Startup`, `DoStartup`, `Shutdown`, and `Exit` ordering are not skipped when WPF does not own the outer dispatcher loop. The real XAML harnesses now also keep loose `XamlReader.Parse(...)`, `XamlWriter.Save(...)` Freezable, live `Style`, live `ControlTemplate`, live `DataTemplate`, live `HierarchicalDataTemplate`, live `ItemsPanelTemplate`, live `GroupStyle`, live `FrameworkElement`, live `FlowDocument`, and `ResourceDictionary` serialization for `MenuItem.SeparatorStyleKey` in WPF-managed code. `MediaContext` now has a first non-Windows render-wakeup bridge that drains WPF render-priority dispatcher work before the ProGPU host render pass, carries delayed animation tick scheduling through `IWpfDelayedRenderScheduler`, and applies the first zero-duration `Loaded` and `Button.Click` `BeginStoryboard` property-clock values while running managed `TimeManager` ticks without DUCE channels. The narrow non-Windows `RectangleGeometry` fill hit-test fallback is only a bring-up guard for WPF input hit testing; full-performance WPF-compatible geometry hit tests, transformed-arc bounds, native shader effects, and shader bytecode import should be implemented in ProGPU native vector/shader APIs instead of managed WPF workarounds. Remaining work belongs in WPF lifetime, text input, and event-loop code: IME/stylus, remaining caret/selection service parity beyond the current `TextBox` smoke, advanced capture edge parity for menus/text selection/drag gestures, MediaContext channel notifications, app shutdown edge cases, native OS drag/drop source/target lifecycle parity beyond the current portable routed-event bridge, dirty-region scheduling, broader live control/theme `XamlWriter.Save(...)` graph serialization over default-style object graphs now that static system resource keys round-trip, and remaining animation timing/control-action edge cases.
- Keep `src/ProGPU.Wpf.RealPresentationCoreHarness` green as the real-provider compatibility check for `WpfRenderDataSinkProviderBridge.TryRegisterRenderDataSinkProvider(Assembly, ...)` and owner-scoped retained `RenderOpen()` branch routing until type identity is unified.
- Keep `src/ProGPU.Wpf.RealPresentationFrameworkHarness` green as the real managed-framework smoke check for code-only `Application`/`Window`/controls/resources/rich text plus real `DrawingVisual.RenderOpen()` retained owner routing. When this harness exposes a Windows-only dependency, prefer a real platform-service boundary or ProGPU/Silk.NET backend feature over a managed-framework rewrite.
- Keep `src/ProGPU.Wpf.RealApplicationRunHarness` green as the real `Application.Run()`/`StartupUri` smoke check for WPF-owned app lifetime, startup dispatcher draining, portable window activation, run-loop handoff, loose `XamlReader.Parse(...)` loading, loose `XamlWriter.Save(...)` Freezable, style, control-template, data-template, hierarchical-data-template, items-panel-template, group-style, framework-element, and rich-document serialization, compiled XAML merged-dictionary lookup, compiled `x:Shared=false` resource instance behavior, implicit type-key style lookup, generated event hookup, dynamic binding, direct-command/routed-command/input-binding activation, control-template, `VisualStateManager`, dynamic-resource, style/data-trigger activation, collection binding, `TextBox` selection, rich `FlowDocument` inline/block UI content, storyboard `Loaded` and `Button.Click` trigger/property-clock application, item-template binding-path activation, read-only layout collections, attached layout properties, and shutdown ordering. Treat failures here as WPF lifetime/windowing/framework portability gaps first, not reasons to replace managed `Application`, loose XAML loading/serialization, binding, resource dictionaries, resource expressions, routed events, style triggers, collection views, layout metadata, templating, template visual states, commanding, input binding, selection, rich-document managers, animation object model, or XAML startup code in ProGPU.
- Keep `src/ProGPU.Wpf.RealThemeRuntimeHarness` green as the real Fluent theme/XAML smoke check for managed theme resources, `Style.BasedOn` inheritance, implicit type-key style lookup for both XAML-created and runtime-created controls, dynamic style-resource values, measured control layout, `UIElement._drawingContent` render-data replay, and retained ProGPU command output from real themed content. The measured replay now keeps the implicit-style CheckBox visible by routing Fluent private-use icon glyphs through the portable simple nominal-glyph text path and WPF's null-glyph unshaped placeholder when the Windows icon font is absent, and native ProGPU vector parsing accepts WPF `F0`/`F1` path fill-rule prefixes used by theme geometry; remaining exact icon fallback, full shaping, fallback-chain, bidi/caret, paragraph-layout, and native vector fidelity work still belongs in the ProGPU/cross-platform text and vector stacks, not in managed WPF theme workarounds.
- Treat Fluent window backdrop/material effects as native window-decoration services. WPF should keep owning theme dictionaries, style application, theme-mode state, and window dependency properties; ProGPU/Silk.NET should eventually provide cross-platform decoration/backdrop support behind the window platform service instead of letting theme code call `WindowInteropHelper`, `HwndSource`, or DWM APIs off Windows.
- Treat `System.Windows.Shell.WindowChrome` as WPF-owned metadata plus a native decoration service boundary. SDK-switched apps should keep using XAML `WindowChrome.WindowChrome`, `WindowChrome.SetWindowChrome`, resize-grip/hit-test attached properties, caption height, resize border, glass-frame, and non-client-frame metadata without source changes; ProGPU/Silk.NET should provide native custom frame, caption-button, resize-hit-test, system-menu, rounded-region, and DWM-equivalent behavior behind the portable window service instead of requiring WPF apps to bypass `WindowChrome`. The runnable MVP SDK app now validates the XAML-declared metadata path directly, including hit-test and resize-grip attached properties, so future custom-frame failures should be fixed by extending the portable decoration service rather than asking apps to remove `WindowChrome`.
- Keep `SystemCommands` in WPF's public command surface for custom chrome and templates. Non-Windows should route close, minimize, maximize, and restore through WPF `Window` state/lifetime and leave native system-menu presentation to a ProGPU/Silk.NET window-decoration service, without requiring app templates to bypass `SystemCommands`. The generated external SDK app validates XAML `CommandBinding` and button-command usage for maximize, minimize, restore, and system-menu commands from a project that only changes `Project Sdk`, and the runnable MVP app now validates the same command route through ordinary menu items targeting the `Window`.
- Implement native window drag/move initiation behind ProGPU's `IWpfWindowDecorationService`. WPF now routes `Window.DragMove()` through `PortableWindowActivationService.TryDragMove(...)` on non-Windows, and the Silk.NET bridge can use the native Win32 move loop through `INativeWindowSource.Native.Win32` HWND extraction on Windows, the native Cocoa move loop through `INativeWindowSource.Native.Cocoa`, current `NSApplication` event lookup, and `NSWindow.performWindowDragWithEvent:` on macOS, plus the native X11/EWMH `_NET_WM_MOVERESIZE` client-message path through `INativeWindowSource.Native.X11`. Wayland still needs a real compositor-supported move-loop implementation behind the same service because Silk.NET currently exposes the display/surface but not the seat and serial required by `xdg_toplevel.move`; app templates should not replace `DragMove()` with platform-specific code.
- Make the ProGPU-backed WPF `DrawingContext` live in the real WPF type identity or a signed friend assembly later so `RenderDataDrawingContextSinkProvider.PushDrawingContextFactory(...)` can replace the object/reflection bridge.
- Unify `ProGPU.Wpf` source-level drawing API primitive identities with the real SDK transport before treating typed drawing contexts as a public no-source-change SDK surface. Until `WpfCompositionDrawingContext` binds to the same `WindowsBase`/`PresentationCore` split as real WPF, package-mode validation should prefer visual replay or object/reflection paths that accept real WPF instances and route drawing into native ProGPU retained branches.
- Add root WPF project references to signed ProGPU assemblies only when the referenced APIs are actually used by the non-Windows WPF implementation.
- Treat WinRT `InputPane`/`TipTsfHelper` touch-keyboard invocation as a platform-service boundary. The WPF focus and input-method managers should stay active for SDK-switched apps, while non-Windows avoids HWND/InputPane lookup until ProGPU/Silk.NET has a native keyboard/IME service behind the portable window/input stack.
- Keep `AutomationProperties` and WPF automation peers in the managed WPF layer. SDK-switched apps should preserve attached automation metadata, peer creation, and peer property projection without source changes; ProGPU/Silk.NET should provide the native accessibility/provider bridge for macOS, Linux, and Windows instead of moving WPF's automation dependency properties or control-peer classes into app code or managed rendering workarounds. The first SDK smoke now covers attached metadata plus cached peer-level automation id/name/help/labeled-by projection; native OS accessibility provider exposure remains a platform-service follow-up.
- Implement durable ProGPU/Silk.NET platform services for the temporary WPF source guards opened by the real framework harness: dispatcher wake/message pump, system metrics/colors/theme/high-contrast changes, DPI/monitor data, cursor handles, font discovery, keyboard layout, text services/IME, caret metrics, and theme/resource change notifications.
- Prefer extending ProGPU's native shader/effect APIs directly for WPF compatibility gaps, including bytecode import, sampler resource binding, and shader cache keys, instead of adding managed WPF-side shader workarounds.
- Keep shader module verification status explicit until ProGPU has a process-safe native diagnostics path. The current wgpu-native `wgpuShaderModuleGetCompilationInfo` entry point can abort the process, so `WgpuContext.VerifyShaderModule(...)` must not report unchecked WPF/ShaderToy shader modules as valid; shader-effect callers should distinguish unavailable preflight diagnostics from invalid modules and rely on pipeline/device error callbacks or a future safe backend API for diagnostics.
- Keep ShaderToy and WPF shader control-flow compatibility in ProGPU's transpiler/native shader layer. The WPF bridge should not rewrite shader source or emulate shader control flow in managed code.
- Keep ShaderToy declaration compatibility in ProGPU's transpiler/native shader layer. Fixed-size GLSL arrays must preserve their element count in WGSL and semantic analysis, and unsupported dynamic array sizes should fail as backend shader limitations instead of being rewritten or emulated by the WPF bridge.
- Keep process-safe shader verification separate from pipeline usability. The ProGPU submodule now allows WPF and ShaderToy shader pipelines to build when module verification is unavailable, while keeping unsafe `wgpuShaderModuleGetCompilationInfo` calls out of the process. WPF should consume the backend verification status and device/pipeline diagnostics instead of adding managed shader fallbacks only because local verification is unavailable.
- Preserve shader source coordinates in ProGPU draw compilation. Partial clips should either interpolate native quad UVs/fragment coordinates from the original command rectangle or rely on the compositor scissor for transformed quads; WPF bridge code should not compensate by rewriting shader inputs.
- Keep WPF dash arrays in WPF thickness-relative units when lowering shim pens into ProGPU vector pens. The native dash pattern builder already multiplies by final stroke thickness, including transform-scaled thickness, so bridge code must not pre-scale dash entries and square the effective interval length.
- Keep the explicit WPF logical-scene versus physical-render-target overloads stable in ProGPU packages. The SDK runtime and external SDK smoke now inspect the loaded `ProGPU.Wpf`/`ProGPU.Scene` output assemblies for those APIs before native apphost validation, because stale fixed-version `11.0.0-dev` packages can otherwise reproduce the Retina/HiDPI upper-left quadrant rendering even when the source tree has the correct code.
- Keep portable `PresentationSource` client-size and DPI changes as retained-WPF replay invalidations, not compositor coordinate offsets. When the WPF host reconciles logical DIPs with a physical Retina/HiDPI render target, any retained WPF root captured under the previous source geometry must replay from the managed WPF source tree before ProGPU composes it. The package-mode SDK smokes now validate this by rendering a `420x840` DIP retained WPF frame into an `840x1680` physical target and reading the lower-right physical pixel from the shipped assemblies.
- Keep mutable SDK package output refresh deterministic. `ProGPU.Wpf.Sdk` now clears known WPF and ProGPU runtime DLLs from `$(TargetDir)` before recopying package runtime assets when any selected WPF/ProGPU package version is a mutable `-dev` version, so `SkipUnchangedFiles` cannot leave an old bridge or compositor assembly in a package-built apphost after local nupkgs are refreshed.
- Keep package-mode smoke app output refresh deterministic as well. The visible SDK smoke can look like a renderer regression if an old apphost output or repo-local package extraction keeps stale ProGPU/WPF bridge assemblies after the mutable `11.0.0-dev` package feed has been refreshed, so `eng/progpu-wpf-sdk-ci.sh` now deletes the generated SDK smoke app and harness `artifacts/bin`/`artifacts/obj` folders plus `artifacts/nuget/ProGPU.Wpf.SdkSwitchSmoke` before rebuilding. Do not compensate for this stale-output class by shifting WPF managed layout, retained replay roots, or ProGPU compositor coordinates.
- Keep WPF dispatcher startup and idle work on the WPF side of the native-loop boundary. The ProGPU/Silk.NET host should expose a blocking run loop and render/input wakeups, while `WpfPortableWindowActivation.Run()` drains WPF dispatcher work through `ApplicationIdle` before entering that loop so SDK-switched apps can run ordinary startup, resource, validation, and shutdown callbacks without a ProGPU-specific startup API.
- Push resolved DPI into the portable presentation source before the first frame, not only during render. WPF layout, actual-size propagation, and manager code should not observe an initial 1x source when Silk.NET has already exposed a physical Retina/HiDPI framebuffer.
- Keep evolving `ProGPU.Backend.DisplayScaleResolver` as the shared ProGPU/Silk.NET display-scale service for host integrations. The initial implementation covers monitor-scale normalization plus macOS Cocoa `backingScaleFactor` fallback for WPF's SDK smoke Retina path; future ProGPU work should extend this same service for any Windows/Linux display-scale gaps instead of adding per-host platform P/Invokes or managed layout compensations.
- Do not add a ProGPU branch feature for ordinary WPF `Window.Left`/`Window.Top` placement yet: Silk.NET already exposes `WindowOptions.Position` and `IWindow.Position`, and the WPF port now routes the managed placement properties through that surface. Keep future ProGPU work focused on gaps Silk.NET does not directly cover, such as custom chrome, native drag loops, system menu/caption buttons, and display-scale service reuse.
- Do not add a ProGPU branch feature for ordinary WPF `Window.Topmost` placement either: Silk.NET already exposes `WindowOptions.TopMost` and `IWindow.TopMost`, and the WPF port now routes the managed `Window.Topmost` dependency property through that surface. Keep future ProGPU branch work focused on durable platform gaps such as custom chrome, native drag loops, system menu/caption buttons, modal disable/enable parity, and reusable display-scale services.
- Do not add a ProGPU branch feature for ordinary WPF `ResizeMode`/`WindowStyle` native-border mapping yet: Silk.NET already exposes `WindowOptions.WindowBorder` and `IWindow.WindowBorder`, and the WPF port now translates initial and live managed `ResizeMode`/`WindowStyle` state into that surface. Keep future ProGPU branch work focused on custom chrome, native resize hit-testing, system menus, caption buttons, and platform decoration parity that Silk.NET does not supply directly.
- Keep filtered layer semantics in ProGPU native/shim rendering rather than WPF-side workarounds. The ProGPU branch now carries active Skia clip pushes into `SaveLayer(...)` offscreen rendering before blur/drop-shadow image filters run, which is the same ownership model needed for WPF `DrawingGroup`, `OpacityMask`, `Effect`, and cache/layer replay: clips must shape the offscreen source before native shader sampling, not only clip the final restored texture.
- Keep image-effect GPU resource teardown on the deferred `WgpuContext` disposal queues. Image-effect bind groups, bind-group layouts, and pipeline layouts are now queued on prune/dispose paths instead of direct WebGPU release, matching the WPF host requirement that UI-side teardown, compositor teardown, and render-thread cleanup cannot release native handles from the wrong thread or while work may still be in flight.
- Keep nested offscreen render targets authoritative inside ProGPU. When WPF, Skia, effects, masks, cached layers, or retained visual branches render into an intermediate texture while an outer logical/physical Retina swapchain target is active, `RenderOffscreen(...)` must switch `CurrentCanvasPixelWidth/Height`, masks, scissor clamps, and DPI uniforms to the offscreen texture size and restore the outer explicit target afterward. Do not compensate for this class of SDK smoke offset by moving WPF visual roots or changing managed layout coordinates.
- Keep deferred save-layer textures owned by recorded command contexts. Skia-compatible `SaveLayer(...)` replay may record `DrawTexture` or image-effect commands into a caller-provided `DrawingContext`; those textures must be retained by `DrawingContext.Clear()`/consumer lifetime, while only intermediate source textures not referenced by recorded commands remain canvas-owned. This same retained-resource rule applies to WPF effect/cache layers and avoids disposing GPU textures before ProGPU consumes deferred command lists.
- Keep failure cleanup on the native ProGPU side for Skia/WPF compatibility shims. If a save-layer or WPF retained/effect branch allocates an offscreen texture and then native `RenderOffscreen(...)` fails before a deferred command retains that texture, the allocation must be disposed and the source drawing context cleared by ProGPU/Skia shim code. WPF bridge code should not compensate by suppressing layer creation or holding unmanaged texture references after a failed native render.
- Keep recorded-command cleanup and texture context ownership in ProGPU shims/backends rather than WPF bridge workarounds. GDI-compatible bitmap flushes must clear command streams and retained resources from `finally` paths on failed native renders, WPF shim image draws must fail or copy before recording when a `BitmapSource` belongs to another `WgpuContext`, and host zero-copy fallback cleanup must tolerate partially initialized native image arrays. The WPF port should surface these failures as backend/shim bugs, not keep extra managed references or shift visual coordinates to hide them.
- Keep deferred Skia-compatible image draws tied to the active render target context. When WPF or Skia records into a deferred `DrawingContext` under a compositor-pushed `WgpuContext.Current`, ProGPU should retain `SKImage` textures against that target context and reject cross-context images before command recording instead of letting WPF bridge code hold extra references or add managed copy workarounds.
- Keep filtered save-layer/effect texture ownership in ProGPU as well. Drop-shadow and future filtered layer passes must clear their temporary replay contexts and dispose allocated filter output textures if native offscreen rendering fails before the texture is retained by a recorded command or canvas-owned cleanup list. The WPF bridge should not hold managed-side shadow/effect texture lifetimes to mask failed ProGPU filter renders.
- Keep WPF shader-effect GPU handle teardown on the deferred `WgpuContext` queues. WPF shader-effect bind groups, cached sampler/source bind groups, bind-group layouts, and pipeline layouts can be referenced by recently submitted work when a WPF host closes, so ProGPU extension disposal must queue them like ImageEffect and ShaderToy instead of calling native WebGPU release APIs directly.
- Keep WPF/Skia alpha and fill-rule semantics normalized at the backend/shim boundary. Opaque Skia destination pixmaps ignore source alpha and must receive 255 alpha bytes during `ScalePixels(...)` just as during `ReadPixels(...)`; ProGPU path-op readback must carry the input-derived fill rule into returned `PathGeometry` instances before WPF or Skia rasterizes the result.
- Keep premultiplied-to-opaque readback normalization in the ProGPU Skia shim as well. `SKImage.ReadPixels(...)` must unpremultiply premultiplied source RGB before clamping opaque destination alpha to 255, so WPF imaging and Skia interop do not need managed-side color correction after readback.
- Keep Skia paint-alpha modulation for shader brushes in the ProGPU Skia shim. `SKPaint.Color.A` should multiply shader-backed fill and stroke brush opacity before commands reach the compositor, so WPF and Skia callers get gradient/color-shader transparency parity without bridge-side managed opacity rewrites.
- Keep package-mode object render-data primitives on neutral/native descriptors while the shim and real WPF assembly identities are split. The SDK smokes now prove real `PresentationCore.SolidColorBrush` plus real `WindowsBase.Rect` can draw through `OpenObjectRenderDataSinkContext(...)` into native ProGPU retained branches without JITting the shim `Rect` fallback, and the object bridge now routes line, rectangle, rounded-rectangle, ellipse, image, and glyph-run primitives through `WpfReplayPoint`/`WpfReplayRect` or raw glyph-run resources plus `IWpfNativePrimitiveCommandSink` when available. Continue extending this native route for shader/effect descriptors and richer image/brush samplers before exposing typed `WpfCompositionDrawingContext` APIs as no-source-change SDK surface.
- Keep real WPF visual placement state at the WPF visual traversal boundary. `VisualOffset`/`VisualTransform` and their private backing fields should be replayed from WPF visuals into ProGPU retained visual state or scoped native transforms; ProGPU should not add compositor, shader, or swapchain coordinate shifts to compensate for missing WPF root margins or layout translations.
- Keep WPF-declared logical client size authoritative over transient physical native resize callbacks. The SDK smoke offset can be reproduced when package startup caches `840x1680` Retina pixels as if they were `420x840` WPF DIPs, so ProGPU/Silk.NET host integrations must preserve separate declared logical, portable presentation-source logical, native logical, and physical framebuffer dimensions. Future ProGPU backend work should improve reusable DPI/display-scale services and physical viewport validation, not compensate by shifting WPF visual roots or changing managed layout coordinates.
- Keep opacity-mask compilation and appended drawing-context transforms native in ProGPU. WPF visual opacity masks, drawing-group opacity masks, drag images, adorners, and retained branch appends should rely on ProGPU compiling masks with neutral opacity/blend state and translating Rect-backed mask bounds and geometry-clip transforms when command lists are appended. Do not work around stale masks or misplaced dragged/appended visuals by moving WPF managed layout, duplicating opacity-mask handling in the WPF bridge, or replaying content without native mask scopes.
- Keep WPF shader-effect texture identity and generation tracking in ProGPU's native shader/effect cache keys. Resized or reallocated primary and sampler textures must invalidate cached WPF shader-effect draw parameters and sampler bindings at the backend boundary, rather than forcing managed WPF replay workarounds or app-side cache busting.
- Keep normal deferred texture disposal release-only in ProGPU's backend queues. `TextureDestroy(...)` is unsafe for queued cleanup while recently submitted work, cached bind groups, texture views, or resized render-target handles may still reference the texture; release the handle and let WebGPU drain remaining references instead. Stale mask textures and mask bind groups should be purged at the compositor boundary, not compensated for by WPF bridge-side retained-scene resets or managed texture lifetime pinning.
- Keep the visible SDK smoke offset diagnosis tied to package freshness and live swapchain geometry. The current fresh package-mode build reports `420x840` logical DIPs over an `840x1680` Retina framebuffer and visually fills the client area; if the old half-width/half-height content shape reappears, refresh the mutable `11.0.0-dev` package output and generated apphost artifacts before adding compositor offsets or managed WPF layout changes. The live smoke validator now checks the last presented ProGPU frame and the WebGPU swapchain backing dimensions against the resolved physical pixels, so logical-sized present/swapchain regressions should fail in the SDK smoke instead of being diagnosed only from screenshots.
- Keep live apphost frame-state publication explicit at the ProGPU/WPF host boundary. `HasPresentedFrame` is read by async SDK smoke probes while the render loop records frames, so the host must publish it with volatile semantics after `LastPresentedFrameState` is updated and reset it with the same memory barrier during target disposal. If future smoke probes miss a presented frame while the swapchain renders, fix the host synchronization or package freshness first; do not add dispatcher-marshalled probes or WPF layout offsets to hide stale frame-state reads.
- Keep WPF affine append semantics, opaque alpha normalization, and texture-context ownership in ProGPU shims/backends. Matrix append operations must update offsets exactly where WPF appends transforms, opaque decode destinations must receive alpha 255 even when encoded input is transparent, and image-effect source/mask textures must be owned by the compositor `WgpuContext` or copied/imported before binding. The WPF bridge should not compensate by moving retained roots, preserving hidden alpha in opaque resources, or recording cross-context texture references that WebGPU cannot bind.
- Keep appended command placement in ProGPU. Rect-backed commands, point/point-buffer commands, and shader/effect payloads must translate in command-local space when no transform is active, but compose append translation into the existing command transform when a non-identity transform is already present so scale/rotation/shear do not double-apply offsets. Do not add WPF bridge-side offset fixes for retained drag/adornment/effect command lists unless the backend append contract is already correct and a real WPF visual traversal bug remains.
- Treat Silk.NET `IInputDevice.IsConnected` as advisory for devices already returned from an input context. WPF sample input should subscribe to all initially exposed mice/keyboards, detach on explicit disconnect notifications, and derive modifier state from actual key polling rather than filtering the whole keyboard out because the connection flag has not settled. If future ProGPU/Silk.NET input abstractions wrap Silk devices, preserve this contract so SDK-switched WPF apps receive mouse, keyboard, text, and routed key-gesture input without app-side retries or manual reattachment.
- Keep SDK/package rebuild flags scoped to the top-level command that requested them. Sample launchers and SDK CI may call each other for apphost validation, so child runner invocations must clear `PROGPU_WPF_HELLO_REBUILD_PACKAGES` and `PROGPU_WPF_MVP_REBUILD_PACKAGES` before delegating; otherwise a single rebuild request can recurse through package CI instead of reaching live ProGPU/Silk.NET input validation.
- Keep ordinary referenced WPF control libraries inside the custom SDK/MSBuild contract. A normal `ProjectReference` from an SDK-switched app to an SDK-switched WPF library should preserve compiled `UserControl` BAML, dependency properties, default item discovery in both projects, output copy, and deps registration without ProGPU-specific app properties. If referenced library controls fail to load, fix the SDK package/build/reference surface or managed WPF loader behavior before adding ProGPU bootstrap code to app projects.
- Keep referenced-library default theme lookup in that same contract. SDK-switched WPF libraries should be able to rely on default-discovered `Properties/AssemblyInfo.cs`, custom control source files, and `Themes/Generic.xaml` so app projects can consume default styles and `TemplateBinding` output through ordinary WPF resource probing. If default styles from referenced libraries fail, repair the SDK item graph, `ThemeInfo`/BAML packaging, or managed WPF resource lookup before adding app-side ProGPU properties or manual resource merges.
- Keep referenced-library component resource dictionaries in managed WPF and SDK packaging as well. SDK-switched apps should merge `/LibraryAssembly;component/...` dictionaries from SDK-switched WPF libraries and resolve static/dynamic resources from compiled BAML without ProGPU-specific metadata. If component URI resources fail, fix assembly resource naming, BAML copy/packaging, or WPF pack-URI lookup rather than adding renderer-side resource fallbacks.
- Keep `App.config` preservation in the SDK/MSBuild/.NET configuration layer. SDK-switched WPF apps should be able to include a normal default-discovered `App.config`, receive the generated `.dll.config` next to the apphost/runtime assembly, and read settings through `ConfigurationManager` without explicit item declarations or ProGPU project properties.
- Keep WPF data-provider resources in the managed WPF data/resource layer. SDK-switched apps should be able to declare `ObjectDataProvider` and `XmlDataProvider` in normal XAML resources, invoke provider methods, parse inline `x:XData`, and bind to provider data or XPath without ProGPU-specific app code. If provider-backed bindings fail, fix WPF data-provider behavior, XML/XPath support, BAML loading, or SDK packaging before adding renderer-side data-source shims.
- Keep `CollectionViewSource` ownership in WPF's managed collection-view layer. SDK-switched apps should be able to declare sorted views in XAML resources, bind item controls to those views, synchronize selection with the current item, and receive collection-change resorting without ProGPU-specific app code. If sorted views or current-item synchronization fail, fix WPF collection views, binding/resource inheritance, or SDK packaging before adding renderer-side item ordering.
- Keep `CompositeCollection` and `CollectionContainer` item composition in WPF's managed item-source layer. SDK-switched apps should be able to mix static XAML items, collection containers, and item controls in a normal `ItemsSource` declaration, with flattened order and collection changes reflected by WPF item controls. If composed items fail, repair WPF item-source handling, collection-view propagation, or SDK XAML packaging before adding renderer-side item composition.
- Keep grouped views and `GroupStyle` in WPF's managed collection-view/item-control layer. SDK-switched apps should be able to declare `PropertyGroupDescription`, bind item controls to grouped views, and realize group headers from normal `DataTemplate` resources without ProGPU-specific app code. If grouping or headers fail, repair WPF collection views, group realization, resource lookup, or SDK XAML packaging before adding renderer-side grouping.
- Keep `HierarchicalDataTemplate` and `TreeView` ownership in WPF's managed item-control layer. SDK-switched apps should be able to bind trees to child collections, expand generated `TreeViewItem` containers, and realize child items through normal hierarchical templates without ProGPU-specific app code. If tree realization fails, repair WPF item-container generation, hierarchical template loading, or SDK XAML packaging before adding renderer-side tree logic.
- Keep `ListView` and `GridView` column presentation in WPF's managed control layer. SDK-switched apps should be able to declare `GridViewColumn.DisplayMemberBinding`, bind list views to item collections, select rows, and observe collection changes without ProGPU-specific app code. If list-view columns fail, repair WPF control metadata, binding, item-container generation, or SDK XAML packaging before adding renderer-side column handling.
- Keep `DataGrid` explicit columns in WPF's managed control layer. SDK-switched apps should be able to declare text and checkbox columns, bind them to item properties, select rows, and observe collection changes without ProGPU-specific app code. If tabular views fail, repair WPF `DataGrid` control metadata, binding, item-container generation, or SDK XAML packaging before adding renderer-side table handling.
- Keep `TabControl` selection and `Expander` disclosure behavior in WPF's managed control layer. SDK-switched apps should be able to declare tab items, selected tab content, expander content, and expand/collapse handlers without ProGPU-specific app code. If tabs or disclosure controls fail, repair WPF control state, routed events, template loading, or SDK XAML packaging before adding renderer-side state handling.
- Keep `Menu`, `ContextMenu`, `ToolBar`, and `StatusBar` behavior in WPF's managed control and command layers. SDK-switched apps should be able to declare shell menus, context menus, command items, click/check handlers, toolbar items, and status-bound content without ProGPU-specific app code. If shell controls fail, repair WPF command metadata, routed events, item generation, popup/service ownership, or SDK XAML packaging before adding renderer-side shell-control state.
- Keep `CheckBox`, `RadioButton`, `Slider`, and `ProgressBar` behavior in WPF's managed control/binding layer. SDK-switched apps should be able to declare form toggles, radio groups, and range/progress bindings with normal two-way binding and routed handlers without ProGPU-specific app code. If form/range controls fail, repair WPF control state, binding updates, routed events, template loading, or SDK XAML packaging before adding renderer-side form-control state.
- Keep `ToolTip` and `Popup` control state in WPF while routing only the native popup-host boundary through ProGPU/Silk.NET services. SDK-switched apps should be able to declare tooltip content, standalone popups, placement targets, and open/close state without ProGPU-specific app code. If popup surfaces fail, repair WPF popup control state, placement target binding, portable popup service ownership, or SDK XAML packaging before adding renderer-side popup state.
- Keep `PasswordBox`, `Calendar`, and `DatePicker` behavior in WPF's managed control/binding layer. SDK-switched apps should be able to declare password-change handlers, calendar/date-picker selected-date bindings, display date ranges, and date format metadata without ProGPU-specific app code. If date/password controls fail, repair WPF control state, routed events, selected-date binding, culture/date parsing, templates, or SDK XAML packaging before adding renderer-side form-control state.
- Keep `Label`, `GroupBox`, and `ScrollViewer` behavior in WPF's managed control/layout layer. SDK-switched apps should be able to declare access labels with targets, headered groups with bound content, and scroll viewers with normal scrollbar/content metadata without ProGPU-specific app code. If grouping/scrolling/access-label controls fail, repair WPF target binding, headered-content state, scroll viewer metadata/layout, templates, or SDK XAML packaging before adding renderer-side layout state.
- Keep default-discovered XAML image resources in the SDK packaging and managed WPF image/brush layer. SDK-switched apps should be able to place common image/cursor assets under ordinary project folders and reference them from `Image` and `ImageBrush` using relative or pack URIs without explicit project items or ProGPU-specific app code. The SDK should own that by discovering default resource items after project evaluation so explicit `Resource`/`Content` declarations remain authoritative. If image resources fail, repair SDK resource discovery, pack URI lookup, codec loading, BAML URI resolution, or managed image/brush state before adding renderer-side resource fallbacks.
- Keep rich document and text-editor object models in managed WPF. SDK-switched apps should be able to declare `RichTextBox`, `FlowDocument`, formatted inline content, document lists, block UI content, `FlowDocumentScrollViewer`, and `TextRange` extraction without ProGPU-specific app code. If rich text fails, repair WPF document object-model portability, text editor manager state, command/input integration, or SDK XAML packaging before adding renderer-side document shims.
- Keep rich text editing commands and clipboard routing in WPF's managed command/input layer while exposing clipboard through the portable platform service. SDK-switched apps should be able to use XAML `ApplicationCommands.Copy`/`Paste`, `EditingCommands` formatting commands, `RichTextBox.Selection`, and `Clipboard` text operations without app-side ProGPU code. If editing commands fail, repair command routing, text editor command handlers, focus/selection state, or clipboard service registration before adding renderer-side editor fallbacks.
- Keep WPF spell-check and text-service metadata in managed WPF with native dictionary/IME depth behind platform services. SDK-switched apps should be able to declare `SpellCheck` attached properties, inspect `TextBox.SpellCheck`, manage custom dictionaries, and call spelling-error query APIs without ProGPU-specific app code. If spell-check state fails, repair WPF text-service manager portability or platform-service registration before adding renderer-side text fallbacks.
- Keep documenting each new backend requirement here before adding WPF-side compatibility code, so durable backend fixes are preferred over short-lived bridge workarounds.
