<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ProGPU.Wpf.SdkSwitchSmoke">
    <Style TargetType="{x:Type local:SmokeThemedControl}">
        <Setter Property="Background" Value="{DynamicResource MergedAccentBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SmokeAccentBrush}" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SmokeThemedControl}">
                    <Border
                        x:Name="ThemeRoot"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                        <TextBlock
                            x:Name="ThemeText"
                            Foreground="{TemplateBinding Foreground}"
                            Text="{TemplateBinding Text}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
